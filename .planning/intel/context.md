# Ingested Context

The TypeScript implementation at baseline `84784f5` is the behavioral reference. It already has exact + BM25 capability retrieval; embedding interfaces, Granite runtime, vector types, and RRF exist but are not wired into the production construction path. No Rust source exists at ingest time.

The architecture borrows typed Op/Event and thin surface ideas from OpenAI Codex, deterministic parity fixtures from claw-code, and immutable raw plus compiled Wiki maintenance from Karpathy's LLM Wiki approach. These are influences, not copied storage or dependency layouts.

source: docs/superpowers/specs/2026-07-15-rust-vault-rewrite-design.md
