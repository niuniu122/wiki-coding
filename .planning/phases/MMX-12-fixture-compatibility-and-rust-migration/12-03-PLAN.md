---
phase: 12-fixture-compatibility-and-rust-migration
plan: "03"
type: execute
gap_closure: true
wave: 10
depends_on:
  - "12-02"
files_modified:
  - crates/compat-harness/src/report.rs
  - crates/compat-harness/tests/compat_report.rs
autonomous: true
requirements:
  - RCMP-01
must_haves:
  truths:
    - "The compatibility gate derives the complete compiled compat-harness module closure from lib.rs and main.rs and proves it equals every regular Rust source below crates/compat-harness/src; no reachable module can evade inspection."
    - "Every derived module fails closed on executable TypeScript product imports, build hooks, or Node/npm/npx/tsc process launches while static fixture/authority literals remain inert evidence."
    - "A mutation in a module omitted by the old four-file list fails both the focused compatibility gate and the shared verify-candidate preflight."
    - "Compatibility verification remains fixture-only, deterministic, machine-readable, and runnable without src/, dist/, test/, Node, npm, or TypeScript execution per D-12-01 and D-12-03."
  prohibitions:
    - statement: "MUST NOT replace the four-file list with another hand-maintained partial list or silently ignore unresolved, orphaned, non-regular, or symlinked Rust modules."
      status: resolved
      verification: "The root-derived graph and recursive source inventory must be exact sets; additions, removals, unresolved module declarations, and symlink mutations fail closed."
    - statement: "MUST NOT treat inert fixture/authority strings as executable TypeScript merely because they name historical paths."
      status: resolved
      verification: "The scanner classifies executable include/read/build/process constructs, with existing static authority and fixture literals covered by a green regression."
    - statement: "MUST NOT execute Node, npm, npx, tsc, TypeScript, a Provider, network access, credentials, publication, push, or hosted verification."
      status: resolved
      verification: "All tests mutate temporary repository copies and invoke only the Rust compatibility harness over immutable fixtures."
    - statement: "MUST NOT refresh or claim hosted Windows MSVC/Linux GNU fingerprint evidence in this gap plan."
      status: resolved
      verification: "Hosted final fingerprint evidence remains explicitly deferred to Phase 14; local Rust commands are development-only evidence."
  artifacts:
    - path: crates/compat-harness/src/report.rs
      provides: "Root-derived compat-harness module closure and fail-closed executable TypeScript reference boundary"
    - path: crates/compat-harness/tests/compat_report.rs
      provides: "Previously omitted reachable-module mutations, closure-set failures, and hermetic fixture-only regressions"
  key_links:
    - from: crates/compat-harness/src/lib.rs
      to: crates/compat-harness/src/report.rs
      via: "lib.rs and main.rs are the executable roots used to derive local module reachability"
      pattern: "pub mod|mod "
    - from: crates/compat-harness/src/report.rs
      to: crates/compat-harness/tests/compat_report.rs
      via: "temporary-copy mutations in provider_eval.rs and migration_support.rs exercise the same shared source-boundary validator"
      pattern: "validate_compatibility_source_boundary"
    - from: crates/compat-harness/src/main.rs
      to: crates/compat-harness/src/report.rs
      via: "report and verify-candidate both pass through verify_fixture_compatibility before producing evidence"
      pattern: "verify_fixture_compatibility"
---

# Phase 12 Plan 03: Close the Compatibility Source Boundary

## Goal / Objective

<objective>
Make RCMP-01 inspect the complete executable Rust compatibility harness instead of four selected source files.

Purpose: Preserve the D-12-01/D-12-03 fixture-only migration boundary even when a forbidden TypeScript reference is introduced in any currently or newly compiled module.
Output: A root-derived, exact module-closure gate plus adversarial mutations in modules the old gate omitted.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-SPEC.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-CONTEXT.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-VERIFICATION.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-01-PLAN.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-01-SUMMARY.md
@crates/compat-harness/src/lib.rs
@crates/compat-harness/src/main.rs
@crates/compat-harness/src/report.rs
@crates/compat-harness/src/provider_eval.rs
@crates/compat-harness/src/migration_support.rs
@crates/compat-harness/src/source_authority.rs
@crates/compat-harness/tests/compat_report.rs
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Expose omitted reachable modules and incomplete closure discovery</name>
  <files>crates/compat-harness/tests/compat_report.rs</files>
  <read_first>
    - .planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-VERIFICATION.md — RCMP-01 gap and the exact seven modules omitted by the current four-file scan
    - crates/compat-harness/src/lib.rs and crates/compat-harness/src/main.rs — executable module roots and exports
    - crates/compat-harness/src/report.rs — current fixed list and forbidden process/source patterns
    - crates/compat-harness/tests/compat_report.rs — existing hermetic repository-copy and report mutation helpers
    - crates/compat-harness/src/provider_eval.rs and crates/compat-harness/src/migration_support.rs — reachable modules used for RED mutations
  </read_first>
  <behavior>
    - A temporary-copy mutation in provider_eval.rs that launches Node against dist/cli.js is rejected even though provider_eval.rs is absent from the old list.
    - A temporary-copy mutation in migration_support.rs that includes/reads a TypeScript product source or invokes a TypeScript build is rejected even though migration_support.rs is absent from the old list.
    - Adding an unresolved module declaration, an orphan Rust source, or a symlinked module below compat-harness/src is rejected rather than omitted.
    - Existing source-authority and baseline literals that describe forbidden legacy paths remain legal because they are static inspection evidence, not executable imports/build/process calls.
  </behavior>
  <action>
Add focused, table-driven tests that copy the repository fixture used by the compatibility suite, mutate at least provider_eval.rs and migration_support.rs with compiling-shaped forbidden executable references, and assert `verify_fixture_compatibility` fails with the mutated relative path in the diagnostic. Add closure mutations for unresolved/orphan/symlinked modules and retain a control proving current inert source-authority/baseline literals are accepted. Run the named RED test before changing report.rs; require failure because the old four-file list returns success, not because the fixture copy or Rust parser setup is malformed.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const r=spawnSync('cargo',['test','-p','minimax-compat-harness','--test','compat_report','--locked','compatibility_source_boundary_rejects_forbidden_references_in_derived_module_closure','--','--exact'],{stdio:'inherit'});process.exit(r.status===0?1:0)"</automated>
    <source_assertions>RED identifies provider_eval.rs and migration_support.rs as currently uninspected; the existing four report.rs mutations remain green controls.</source_assertions>
  </verify>
  <acceptance_criteria>
    - The focused test compiles and fails against the committed four-file implementation.
    - At least one forbidden mutation is in a currently omitted but reachable module, and the matrix covers process plus import/build reference classes.
    - The test operates only on temporary copies and does not require Node, TypeScript, network access, Provider calls, or credentials.
  </acceptance_criteria>
  <done>The exact RCMP-01 source-closure escape is reproducible as deterministic RED evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Derive and inspect the exact compat-harness module closure</name>
  <files>crates/compat-harness/src/report.rs, crates/compat-harness/tests/compat_report.rs</files>
  <read_first>
    - crates/compat-harness/tests/compat_report.rs — RED cases that must turn green without weakening
    - crates/compat-harness/src/report.rs — source-boundary implementation modified here
    - crates/compat-harness/src/lib.rs and crates/compat-harness/src/main.rs — authoritative closure roots, read-only
    - crates/compat-harness/src/source_authority.rs and crates/compat-harness/src/baseline.rs — static legacy-name evidence that must not become a false executable edge
    - crates/compat-harness/src/architecture.rs — compiled Command usage control that launches cargo, not a forbidden legacy runtime
  </read_first>
  <behavior>
    - The validator recursively inventories regular `src/**/*.rs`, derives local modules from lib.rs/main.rs declarations with normal Rust file resolution, and requires exact set equality.
    - Unsafe paths, unresolved/ambiguous declarations, orphan files, duplicate resolution, symlinked files/directories, and unreadable modules fail closed with a deterministic relative path.
    - A token/structure-aware scan rejects executable include/read edges to TypeScript product files and process/build edges through Node/npm/npx/tsc, including shell-wrapper arguments and constant concatenation used by an executable call.
    - Static strings used only to describe immutable fixtures, source authority, forbidden sentinels, or historical contract rows do not become execution edges.
  </behavior>
  <action>
Replace the four-path array with deterministic closure discovery rooted at lib.rs and main.rs. Recursively enumerate regular Rust files, resolve external local module declarations (`mod x;` and `pub mod x;`) to `x.rs`/`x/mod.rs`, follow nested modules with cycle/duplicate handling, and require the derived and inventoried sets to match exactly. Do not trust a second manual allowlist. Scan every derived source with a conservative structural classifier: reject forbidden product paths only when they flow through executable include/read/import primitives, and reject direct or shell-mediated Node/npm/npx/tsc build/process construction; keep static fixture and authority literals inert. Diagnostics must name the module and reference class. Route report, verify, and verify-candidate through this single validator, preserve deterministic JSON bytes and the existing no-src/no-dist/no-test hermetic test, and turn every RED mutation green without weakening the original report.rs cases.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test compat_report --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- report --format json &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - All current and new compat_report tests pass; restoring the four-file list makes the omitted-module mutation fail.
    - The derived closure covers lib.rs, main.rs, architecture.rs, baseline.rs, coverage.rs, manifest.rs, migration_support.rs, provider_eval.rs, report.rs, retrieval_eval.rs, and source_authority.rs.
    - Report JSON remains byte-deterministic and fixture-only verification succeeds in a copy without executable TypeScript product trees.
    - No manifest, evaluator, fixture, package, CI, product command, or hosted-evidence file changes.
  </acceptance_criteria>
  <done>RCMP-01 has a complete, fail-closed compiled-module boundary instead of a partial hand-maintained scan.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-compat-harness --test compat_report --locked`
- `cargo run -p minimax-compat-harness --locked -- report --format json`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- Re-run the focused mutation after temporarily restoring one omitted module/reference in a temporary copy; it must fail before report generation.
- If the workstation requires the installed gnullvm/rust-lld toolchain, treat those runs as local development-only evidence. Hosted Windows MSVC/Linux GNU fingerprint refresh remains deferred to Phase 14.

## Success Criteria

- RCMP-01 inspects the exact executable compat-harness Rust closure and cannot silently omit a newly reachable module.
- Forbidden TypeScript import/build/process mutations fail in formerly omitted modules while inert historical evidence remains accepted.
- Compatibility reports remain deterministic, machine-readable, and hermetic with no TypeScript runtime execution.

## Artifacts this phase produces

- `crates/compat-harness/src/report.rs`: modified complete module-closure discovery and executable legacy-reference validation.
- `crates/compat-harness/tests/compat_report.rs`: modified omitted-module, closure-integrity, and hermetic regression coverage.
