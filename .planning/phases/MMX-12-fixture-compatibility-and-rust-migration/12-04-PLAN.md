---
phase: 12-fixture-compatibility-and-rust-migration
plan: "04"
type: execute
gap_closure: true
wave: 10
depends_on:
  - "12-02"
files_modified:
  - crates/cli/src/migration.rs
  - crates/cli/tests/migration.rs
autonomous: true
requirements:
  - RCMP-02
must_haves:
  truths:
    - "Rollback authorization comes from an exact durable plan/operation/receipt ownership chain whose pre-write target disposition is captured before publication; a receipt self-hash or allowlist membership alone never authorizes deletion."
    - "A pre-existing byte-identical target is durably classified as reused and survives rollback even when an attacker recomputes a forged receipt and operation claiming it was created."
    - "Every existing ancestor of lock, staging, durable plan/operation/receipt, rollback report, and migrated artifact paths is rejected if it is a symlink, and every resolved destination remains below the canonical target root."
    - "A target .minimax or nested ancestor symlink fails before any external write while source immutability, idempotency, interruption recovery, and exact-byte narrow rollback remain intact per D-12-01 and D-12-04."
  prohibitions:
    - statement: "MUST NOT authorize created/reused classification from a caller-supplied receipt, a path allowlist, or any recomputable unkeyed self-hash alone."
      status: resolved
      verification: "Rollback and recovery load fixed durable plan/operation evidence and require exact disposition equality before inspecting receipt-owned targets."
    - statement: "MUST NOT delete a planned-reused target, a changed target, an unowned path, a missing target, or any artifact reached through a symlinked ancestor."
      status: resolved
      verification: "Table-driven forged-record, target-drift, ownership-partition, and symlink cases assert byte-for-byte survival and no external writes."
    - statement: "MUST NOT rely only on lexical starts_with checks or a final-file symlink check."
      status: resolved
      verification: "Each existing path component is inspected before directory creation and canonical containment is rechecked before lock/open/create/remove operations."
    - statement: "MUST NOT mutate migration source data, read real home-directory data, execute Provider/network/credential paths, publish, push, or refresh hosted evidence."
      status: resolved
      verification: "All adversarial cases use copied immutable fixtures and temporary target/external directories; hosted final evidence remains deferred to Phase 14."
  artifacts:
    - path: crates/cli/src/migration.rs
      provides: "Durable pre-write ownership dispositions, three-record validation, and symlink-safe canonical target path handling"
    - path: crates/cli/tests/migration.rs
      provides: "Recomputed forged created-claim matrix, target-ancestor symlink no-external-write cases, and preserved recovery/idempotency/rollback contracts"
  key_links:
    - from: crates/cli/src/migration.rs#MigrationPlan
      to: crates/cli/src/migration.rs#OperationManifest
      via: "the dry-run target disposition is persisted at apply and must equal the operation created/reused partition"
      pattern: "created|reused|plan_hash"
    - from: crates/cli/src/migration.rs#OperationManifest
      to: crates/cli/src/migration.rs#MigrationReceipt
      via: "receipt identity and ownership lists cross-reference the fixed durable plan and operation records"
      pattern: "operation|receipt"
    - from: crates/cli/src/migration.rs#safe_join
      to: crates/cli/src/migration.rs#write_new_file
      via: "component-wise no-symlink resolution and canonical containment gate every parent creation/open/write/remove path"
      pattern: "symlink_metadata|canonicalize"
    - from: crates/cli/tests/migration.rs
      to: fixtures/compat/migration/typescript-v1
      via: "temporary fixture copies prove source-tree byte identity across apply, recovery, verify, forgery, and rollback"
      pattern: "tree_hash"
---

# Phase 12 Plan 04: Bind Migration Ownership and Contain Target Writes

## Goal / Objective

<objective>
Close the RCMP-02 forged-ownership and target-ancestor symlink deletion/write escapes.

Purpose: Make created versus reused a durable pre-write fact and ensure every migration path stays inside the canonical target root without weakening recovery or narrow rollback.
Output: Cross-bound plan/operation/receipt ownership evidence, component-safe path resolution, and adversarial byte-preservation tests.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-SPEC.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-CONTEXT.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-VERIFICATION.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-02-PLAN.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-02-SUMMARY.md
@crates/cli/src/migration.rs
@crates/cli/tests/migration.rs
@fixtures/compat/migration/typescript-v1
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Reproduce forged created claims and target-ancestor symlink writes</name>
  <files>crates/cli/tests/migration.rs</files>
  <read_first>
    - .planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-VERIFICATION.md — exact forged ownership and target symlink gaps
    - crates/cli/src/migration.rs — MigrationPlan/Receipt, OperationManifest, apply/recover/rollback, safe_join, lock, and write_new_file behavior
    - crates/cli/tests/migration.rs — existing unowned-file forgery, recovery, reused-target, source-symlink, idempotency, and narrow rollback cases
    - fixtures/compat/migration/typescript-v1 — immutable source copied by every adversarial test
  </read_first>
  <behavior>
    - For each allowed migrated target, seed the exact expected bytes before dry-run, prove the plan sees it as reusable, then recompute a forged operation and receipt that move it to created; verify and rollback must reject the chain and preserve the seeded bytes.
    - The matrix covers .minimax/config.json, .minimax/runtime/v1/sessions.jsonl, and the migration-scoped capability-metadata.json rather than only an unallowlisted project file.
    - A pre-existing .minimax directory symlink to an external temporary directory causes plan/apply to fail before writer.lock, staging, durable record, receipt, rollback report, or artifact bytes appear outside the target root.
    - A nested existing target ancestor symlink receives the same no-external-write guarantee, and source-tree bytes remain unchanged in every case.
  </behavior>
  <action>
Add a table-driven ownership test that first obtains canonical artifact bytes from a seed migration, pre-creates each allowlisted target byte-identically in a fresh target, builds the plan, applies or stages the legitimate durable evidence, then rewrites both operation and receipt bodies/hashes to claim the reused target as created. Assert verify/rollback fail with Receipt/Recovery, the forged confirmation cannot delete anything, every pre-existing byte remains identical, and the copied source tree hash is unchanged. Add cross-platform directory-symlink helpers and tests for target `.minimax` plus one nested migration/artifact ancestor; snapshot the external directory before the operation and require exact equality afterward. Run the `gap_closure_` tests before modifying migration.rs and require RED due to current deletion/external-write behavior, not symlink privilege/setup failure.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const r=spawnSync('cargo',['test','-p','minimax-cli','--test','migration','--locked','gap_closure_'],{stdio:'inherit'});process.exit(r.status===0?1:0)"</automated>
    <source_assertions>RED reaches the current allowlisted ownership and safe_join paths; it does not pass merely because the forged plan hash is wrong or the path is outside the allowlist.</source_assertions>
  </verify>
  <acceptance_criteria>
    - The forged records use valid recomputed body hashes, the legitimate migration ID/plan hash, and allowlisted target paths.
    - At least one case demonstrates that current rollback would delete a pre-existing byte-identical target falsely moved from reused to created.
    - The symlink test proves no external lock/staging/receipt/artifact write, not only that the final target file is rejected.
    - Tests use temporary fixture copies only and preserve all existing source/recovery/rollback assertions.
  </acceptance_criteria>
  <done>The two RCMP-02 verifier escapes are captured as deterministic, destructive-action-safe RED tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Persist ownership provenance and gate every target path component</name>
  <files>crates/cli/src/migration.rs, crates/cli/tests/migration.rs</files>
  <read_first>
    - crates/cli/tests/migration.rs — all RED and existing migration lifecycle cases that must turn/remain green
    - crates/cli/src/migration.rs — plan/receipt schemas, build/apply/verify/rollback/recover functions, receipt ownership allowlist, operation cleanup, safe_join, lock, and write helpers
    - .planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-CONTEXT.md — D-12-01 Rust-owned migration and D-12-04 source immutability
    - fixtures/compat/migration/typescript-v1 — read-only source authority; never edit or migrate in place
  </read_first>
  <behavior>
    - Dry-run records for every target whether the destination was absent or already byte-identical; collision remains a separate fail-closed state and target drift invalidates apply.
    - Apply persists the exact plan snapshot and an operation ownership partition at the fixed migration-ID path with create-new semantics before publishing any migrated artifact; successful apply retains authoritative records and removes only ephemeral staging.
    - Receipt validation, verify, rollback, repeated apply, and interrupted recovery load the fixed durable plan/operation records and require identical migration ID, plan hash, target set, content hash/length, and created/reused disposition. Self-hashes are corruption checks, not deletion authority.
    - Recovery/rollback remove only targets whose durable plan disposition was absent, whose operation classified them created, and whose current exact bytes still match; planned-reused and changed targets are never removed.
    - Every existing path component is inspected without following symlinks, canonicalized under the target root, and revalidated immediately before lock/open/create/remove; parent directories are created one component at a time with containment rechecks.
  </behavior>
  <action>
Extend the deterministic plan body with a closed per-target pre-write disposition (`absent` or `byte-identical-existing`) captured by dry-run; include it in the plan hash and rebuild/drift comparison. At apply, before artifact publication, create a fixed migration-root plan snapshot and operation record containing the exact created/reused partition and plan identity. Retain both after success, delete only staging, and make idempotent apply/verify/rollback/recovery load these fixed records rather than trusting the receipt or a caller-selected operation file. Require exact three-way set/disposition/content equality; do not make an allowlist or recomputed unkeyed record hash the authorization primitive. A forged receipt/operation that reclassifies planned reuse must fail even with internally valid hashes. Preserve interruption recovery by using the pre-write disposition to distinguish an exact partially published created target from a pre-existing reused target, and preserve narrow rollback by rechecking exact bytes immediately before each removal.

Replace lexical-only safe_join use on every target-side path with a component resolver rooted at the already canonical project directory. Before acquiring writer.lock or creating directories, inspect each existing ancestor via symlink_metadata and reject symlinks (and Windows reparse-link equivalents exposed by the standard metadata path), non-directory intermediates, unsafe/unresolved components, and any canonical prefix outside the root. Create missing parents one component at a time, then canonicalize/recheck the parent immediately before every lock, staging, durable-plan, operation, receipt, rollback-report, artifact create/open/remove. Apply the same resolver to find_collisions, target verification, recovery, and rollback. Turn the RED matrix green, keep D-12-04 full source hashes unchanged, and retain all existing idempotency, collision, drift, changed-target, interruption, exact-claimed-target, and reused-target tests.
  </action>
  <verify>
    <automated>cargo test -p minimax-cli --test migration --locked &amp;&amp; cargo test -p minimax-compat-harness --test migration_support --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - All forged allowlisted created-claim cases fail and preserve byte-identical pre-existing targets despite recomputed receipt/operation hashes.
    - `.minimax` and nested target-ancestor symlinks fail before any external write on supported Unix/Windows test paths.
    - Repeated apply returns the same validated receipt; interrupted publication recovers only durable planned-created exact targets; repeated rollback is stable.
    - Changed, reused, unowned, missing, symlinked, or out-of-root targets are never deleted; source tree hashes remain byte-identical.
    - No fixture, support-window, package, CI, network, Provider, credential, publication, push, or hosted-evidence changes.
  </acceptance_criteria>
  <done>RCMP-02 deletion authority is durably provenance-bound and every migration write/remove is contained below the canonical target root.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo clippy -p minimax-cli --all-targets --locked -- -D warnings`
- `cargo test -p minimax-cli --test migration --locked`
- `cargo test -p minimax-compat-harness --test migration_support --locked`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- Repeat the forged created-claim matrix for all three allowlisted targets and both target-ancestor symlink cases; compare source and external directory tree hashes before/after.
- If local Windows uses gnullvm/rust-lld, treat it as development-only evidence. Hosted Windows MSVC/Linux GNU final fingerprint evidence remains deferred to Phase 14.

## Success Criteria

- RCMP-02 created/reused ownership is fixed before publication and cannot be changed by recomputing receipt/operation bodies.
- Target ancestor symlinks and canonical escapes fail before any lock, staging, receipt, rollback, or artifact write outside the target root.
- Migration remains source-preserving, idempotent, recoverable after interruption, and narrowly reversible only for exact durable created targets.

## Artifacts this phase produces

- `crates/cli/src/migration.rs`: modified durable ownership provenance, lifecycle validation, and canonical component-safe target resolver.
- `crates/cli/tests/migration.rs`: modified forged allowlisted ownership, symlink containment, recovery, idempotency, and rollback regression coverage.
