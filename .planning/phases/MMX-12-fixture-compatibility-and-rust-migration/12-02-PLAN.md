---
phase: 12-fixture-compatibility-and-rust-migration
plan: "02"
type: execute
wave: 9
depends_on:
  - "12-01"
files_modified:
  - fixtures/compat/migration/typescript-v1/manifest.v1.json
  - fixtures/compat/migration/typescript-v1/support-window.v1.json
  - crates/compat-harness/src/migration_support.rs
  - crates/compat-harness/src/lib.rs
  - crates/compat-harness/src/main.rs
  - crates/compat-harness/tests/migration_support.rs
  - crates/cli/src/migration.rs
  - crates/cli/tests/migration.rs
autonomous: true
requirements:
  - RCMP-02
must_haves:
  truths:
    - "Static TypeScript-era migration evidence is fingerprinted, source-preserving, and ineligible for removal until two subsequent public releases have been recorded per D-12-02."
    - "Inventory, dry-run, apply, verify, idempotency, collision, interruption recovery, and narrow rollback remain Rust-owned and release-gated."
    - "Source data is never modified or deleted automatically per D-12-04."
  prohibitions:
    - statement: "MUST NOT alter, rename, truncate, or delete migration source data."
      status: resolved
      verification: "Full-tree hashes are asserted before/after every operation and hostile path/collision/recovery case."
    - statement: "MUST NOT import secrets, private reasoning, derived caches, locks, databases, or unknown records."
      status: resolved
      verification: "Strict allowlist plus hostile fixture cases assert exclusions and fail closed on secret-bearing known records."
    - statement: "MUST NOT remove migration fixtures/support metadata early."
      status: resolved
      verification: "Machine-checkable support-window validation rejects eligibility until the cutover plus two public releases are recorded."
    - statement: "MUST NOT migrate real home-directory or user data in this plan."
      status: resolved
      verification: "Tests copy immutable fixtures into temporary directories only."
  artifacts:
    - path: fixtures/compat/migration/typescript-v1/manifest.v1.json
      provides: "Exact fixture file/hash/schema inventory and provenance"
    - path: fixtures/compat/migration/typescript-v1/support-window.v1.json
      provides: "Cutover release, minimum subsequent-release count, observed releases, and removal eligibility"
    - path: crates/compat-harness/src/migration_support.rs
      provides: "Fixture fingerprint and support-window release gate"
      exports: ["validate_migration_fixture_manifest", "validate_migration_support_window"]
  key_links:
    - from: crates/compat-harness/src/migration_support.rs
      to: fixtures/compat/migration/typescript-v1/manifest.v1.json
      via: "exact recursive path/hash validation"
      pattern: "sha256"
    - from: crates/cli/tests/migration.rs
      to: crates/cli/src/migration.rs
      via: "fixture-copy operations with pre/post source tree hashes and exact receipt-owned targets"
      pattern: "source_unchanged"
---

# Phase 12 Plan 02: Migration Evidence and Support Window

## Goal / Objective

<objective>
Harden the Rust migration safety suite and make fixture provenance plus the two-release support window machine-checkable.

Purpose: Complete RCMP-02, D-12-02, and D-12-04 without touching real user data.
Output: Fixture/support manifests, release validators, and expanded source-preserving migration tests.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-SPEC.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-CONTEXT.md
@crates/cli/src/migration.rs
@crates/cli/tests/migration.rs
@fixtures/compat/migration/typescript-v1
@crates/compat-harness/src/main.rs
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fingerprint migration fixtures and encode the two-release retention rule</name>
  <files>fixtures/compat/migration/typescript-v1/manifest.v1.json, fixtures/compat/migration/typescript-v1/support-window.v1.json, crates/compat-harness/src/migration_support.rs, crates/compat-harness/src/lib.rs, crates/compat-harness/src/main.rs, crates/compat-harness/tests/migration_support.rs</files>
  <read_first>
    - fixtures/compat/migration/typescript-v1 — immutable evidence tree inventoried by this task
    - fixtures/compat/migration/typescript-v1/manifest.v1.json — create/modify target
    - fixtures/compat/migration/typescript-v1/support-window.v1.json — create/modify target
    - crates/compat-harness/src/migration_support.rs — create/modify target
    - crates/compat-harness/src/lib.rs — exports modified by this task
    - crates/compat-harness/src/main.rs — verify wiring modified by this task
    - crates/compat-harness/tests/migration_support.rs — create/modify test target
    - .planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-CONTEXT.md — D-12-02/D-12-04
  </read_first>
  <behavior>
    - Fixture manifest lists every immutable evidence file, relative path, byte length, SHA-256, role, and expected inclusion/exclusion class.
    - Support metadata records cutover release `3.0.0`, `minimumSubsequentPublicReleases: 2`, observed public releases, and computed eligibility rather than a date guess.
    - Missing/added/edited fixture files or premature eligibility fail `verify` and `verify-candidate`.
  </behavior>
  <action>
Generate and check in strict fixture and support-window schemas. Exclude the two metadata files from their own recursive fingerprint but cover every source evidence file exactly once. Per D-12-02, make removal eligibility false until two distinct, ordered public releases after v3.0 are explicitly recorded; per EDGE-08, do not infer elapsed time or assume a release cadence. Implement validators, table-driven tamper/missing/extra/reordered/duplicate-release tests, export them, and wire them into repository verification before release evidence checks.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test migration_support --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - Every current fixture is hashed/classified once and any drift is review-visible.
    - The support-window gate is false at v3 cutover and cannot become true with duplicate/pre-v3/unordered releases.
    - No migration fixture is modified or deleted.
  </acceptance_criteria>
  <done>Static upgrade evidence and its minimum retention window are release-gated facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close migration bounds, recovery, secret, collision, and rollback cases</name>
  <files>crates/cli/src/migration.rs, crates/cli/tests/migration.rs, fixtures/compat/migration/typescript-v1/manifest.v1.json</files>
  <read_first>
    - crates/cli/src/migration.rs — `inventory_migration`, `build_migration_plan`, `apply_migration`, `verify_migration`, `rollback_migration`, and recovery helpers modified only if a test exposes a gap
    - crates/cli/tests/migration.rs — safety suite modified by this task
    - fixtures/compat/migration/typescript-v1/manifest.v1.json — expected inclusion/exclusion and hash source
    - fixtures/compat/migration/typescript-v1/secrets.local.json — hostile excluded fixture
    - .planning/SPEC.md — EDGE-03 and source-preserving upgrade contract
  </read_first>
  <behavior>
    - Inventory/dry-run write nothing and report bounds, exclusions, collisions, target schema, hashes, and confirmation.
    - Apply is idempotent, receipt-bound, secret-free, and recovers only operation-manifest targets after interruption.
    - Verify detects source or target drift; rollback removes only unchanged targets marked created and retains receipts/reused/modified files.
    - Malformed, oversized, symlinked, secret-bearing, colliding, forged, and unknown known-schema inputs fail before target mutation.
  </behavior>
  <action>
Extend `migration.rs` only where focused red tests show the locked behavior is missing; otherwise keep production code unchanged. In `migration.rs` tests, iterate the fixture manifest, hash the complete source tree before/after every lifecycle operation, and add explicit cases for aggregate/file/count bounds, symlinks, secret-looking known fields, private-reasoning fields, malformed JSON/JSONL, target collision, forged plan/receipt, interrupted operation cleanup, changed targets, repeated apply/rollback, and narrow rollback. Assert no network/provider/credential path and no write outside `.minimax` for every case, per D-12-04.
  </action>
  <verify>
    <automated>cargo test -p minimax-cli --test migration --locked &amp;&amp; cargo test -p minimax-compat-harness --test migration_support --locked</automated>
  </verify>
  <acceptance_criteria>
    - All required lifecycle and hostile cases pass from temp copies of immutable fixtures.
    - Every failure before apply leaves the target and source byte-identical; every later failure is recoverable from bounded receipts/manifests.
    - Source hashes are identical after apply, verify, rollback, and repeated operations.
  </acceptance_criteria>
  <done>Rust migration preserves upgrade safety without any executable legacy runtime.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-cli --test migration --locked`
- `cargo test -p minimax-compat-harness --test migration_support --test compat_report --locked`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- Existing Rust CLI and npm installed-command identity smoke remain green.

## Success Criteria

- RCMP-02 and D-12-02/D-12-04 are complete.
- EDGE-03 and EDGE-08 are deterministic backstops.
- No real user data, fixture source data, platform scope, Provider, or release authorization is touched.

## Artifacts this phase produces

- `fixtures/compat/migration/typescript-v1/manifest.v1.json`: new exact path/hash/role inventory.
- `fixtures/compat/migration/typescript-v1/support-window.v1.json`: new release-count retention record.
- `crates/compat-harness/src/migration_support.rs`: new fixture/support validators.
- `crates/compat-harness/tests/migration_support.rs`: new tamper/window negative test target.
- `crates/cli/src/migration.rs`: None new required; modify only if a red safety test exposes a contract gap.
- `crates/cli/tests/migration.rs`: modified safety coverage; no real-data migration command is run.
