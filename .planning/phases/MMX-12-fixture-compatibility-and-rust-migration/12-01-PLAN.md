---
phase: 12-fixture-compatibility-and-rust-migration
plan: "01"
type: execute
wave: 8
depends_on:
  - "11-04"
files_modified:
  - fixtures/compat/public-contract.v1.json
  - fixtures/compat/baseline-status.v1.json
  - fixtures/compat/report.expected.json
  - crates/compat-harness/src/manifest.rs
  - crates/compat-harness/src/report.rs
  - crates/compat-harness/src/baseline.rs
  - crates/compat-harness/tests/compat_report.rs
autonomous: true
requirements:
  - RCMP-01
must_haves:
  truths:
    - "Compatibility compares the current Rust product with an immutable versioned public-contract fixture and explicit approved differences per D-12-03."
    - "The report is deterministic, complete, machine-readable, and contains contract and Rust evidence rather than live TypeScript product rows."
    - "The compatibility command neither imports, builds, nor executes the transitional TypeScript implementation per D-12-01."
  prohibitions:
    - statement: "MUST NOT execute TypeScript or require generated legacy output for compatibility."
      status: resolved
      verification: "Compat tests run with the TS tree/generated output unavailable and reject process/import/build links to it."
    - statement: "MUST NOT lose a public command, provider profile, protocol, permission, migration, retrieval, Vault, tool, or release contract during identity renaming."
      status: resolved
      verification: "Contract/report completeness tests compare exact stable IDs and golden entries."
    - statement: "MUST NOT rewrite fixture intent or approve a difference implicitly."
      status: resolved
      verification: "Strict fixture fingerprints and approved-difference IDs make changes explicit and reviewable."
  artifacts:
    - path: fixtures/compat/public-contract.v1.json
      provides: "Immutable public-contract identities, provenance, required Rust evidence, and approved-difference links"
    - path: crates/compat-harness/src/manifest.rs
      provides: "Strict `PublicContractManifest` loading and validation"
      exports: ["PublicContractManifest", "load_compat_manifests"]
    - path: fixtures/compat/report.expected.json
      provides: "Golden contract-versus-Rust compatibility report without live TS rows"
  key_links:
    - from: crates/compat-harness/src/report.rs
      to: fixtures/compat/public-contract.v1.json
      via: "build_report expands every contract item and current Rust evidence exactly once"
      pattern: "public_contract"
    - from: crates/compat-harness/src/report.rs
      to: fixtures/compat/command-differences.v1.json
      via: "approved differences require explicit stable IDs and evidence"
      pattern: "approved_difference"
---

# Phase 12 Plan 01: Fixture-Owned Compatibility

## Goal / Objective

<objective>
Rebase compatibility on immutable public-contract fixtures and explicit differences so no running TypeScript product is needed.

Purpose: Implement RCMP-01, D-12-01, and D-12-03 while preserving deterministic completeness and architecture gates.
Output: A versioned public-contract manifest, refactored report model, and a new golden with no live TS product identity.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-SPEC.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-CONTEXT.md
@fixtures/compat/baseline-status.v1.json
@fixtures/compat/commands.v1.json
@fixtures/compat/providers.v1.json
@fixtures/compat/command-differences.v1.json
@crates/compat-harness/src/manifest.rs
@crates/compat-harness/src/report.rs
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace live baseline identity with an immutable public-contract manifest</name>
  <files>fixtures/compat/public-contract.v1.json, fixtures/compat/baseline-status.v1.json, crates/compat-harness/src/manifest.rs</files>
  <read_first>
    - fixtures/compat/baseline-status.v1.json — legacy identity model deleted only after full field migration
    - fixtures/compat/commands.v1.json — locked public command source of truth
    - fixtures/compat/providers.v1.json — profile/protocol source of truth
    - fixtures/compat/command-differences.v1.json — only approved-difference source
    - fixtures/compat/public-contract.v1.json — create/modify target
    - crates/compat-harness/src/manifest.rs — `BaselineStatus` and `CompatManifests` target modified by this task
    - .planning/SPEC.md — current Rust/documented contract parity baseline
  </read_first>
  <behavior>
    - `public-contract.v1.json` has a strict schema, contract version, provenance commit, content fingerprint, required item IDs, status, and evidence.
    - IDs describe public-contract and current Rust evidence, never a live language implementation.
    - Every command/profile/protocol and mandatory cross-cutting contract is complete and unique.
    - The former baseline file is removed only after all consumers use `PublicContractManifest`.
  </behavior>
  <action>
Create `PublicContractManifest` and replace `CompatManifests.baseline` with `public_contract`. Migrate public command/profile/protocol intent from the old live rows into versioned contract items, preserving provenance and every Rust evidence link. Per D-12-03, model historical TypeScript only as fixture provenance; per D-12-01, remove fields that imply an executable legacy product. Validate fingerprint, stable ID namespace, statuses, evidence existence, unique coverage, product entry, and strict unknown-field rejection. Delete `baseline-status.v1.json` only after repository searches show no consumer remains.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test compat_report --locked compat_report_contains_every_contract_item_exactly_once -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - The new manifest preserves every locked public-contract item from commands/providers/cross-cutting evidence.
    - No manifest field or ID represents a live TypeScript implementation.
    - Missing/edited fixture content, unknown fields, duplicates, or evidence drift fail closed.
  </acceptance_criteria>
  <done>Compatibility input is immutable data provenance rather than a second executable baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild deterministic reports and prove no TypeScript execution dependency</name>
  <files>fixtures/compat/report.expected.json, crates/compat-harness/src/report.rs, crates/compat-harness/src/baseline.rs, crates/compat-harness/tests/compat_report.rs</files>
  <read_first>
    - fixtures/compat/public-contract.v1.json — contract input created by Task 1
    - fixtures/compat/report.expected.json — golden modified by this task
    - crates/compat-harness/src/report.rs — `CompatReport`, `build_report`, `validate_report` targets
    - crates/compat-harness/src/baseline.rs — release/product validators migrated from baseline argument
    - crates/compat-harness/tests/compat_report.rs — completeness/golden/negative tests modified by this task
    - fixtures/compat/command-differences.v1.json — explicit difference evidence
    - .planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-SPEC.md — missing/edited fixture and lost-contract edges
  </read_first>
  <behavior>
    - Report fields are `schemaVersion`, `contractVersion`, `contractFingerprint`, and stable entries.
    - Each public contract has current Rust evidence or an explicit approved difference exactly once.
    - Repeated report runs are byte-identical after newline normalization.
    - Verification passes when `src/` and generated legacy output are hidden, and fails if any source/process dependency on them is introduced.
  </behavior>
  <action>
Refactor report composition/validation and release/product helper signatures to consume `PublicContractManifest`. Emit contract IDs plus current Rust status/evidence, merge only differences declared in the locked difference fixture, and regenerate the golden. Replace old completeness expectations with the exact public-contract set. Add a hermetic test repository that includes fixtures/Rust evidence but no TypeScript source or generated legacy output; run report/verify there and add mutation cases for a lost contract, edited fingerprint, unknown difference, live-product row, TS import, TS build command, and attempted legacy process execution.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test compat_report --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- report --format json</automated>
  </verify>
  <acceptance_criteria>
    - The JSON report matches the new golden and has no live TypeScript product row.
    - All contract items are present exactly once with evidence or an approved difference.
    - Compatibility succeeds without importing, building, or executing TS and still runs all Rust architecture/evaluation gates.
  </acceptance_criteria>
  <done>The compatibility report is fixture-owned, deterministic, complete, and Rust-only.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-compat-harness --test compat_report --locked`
- `cargo run -p minimax-compat-harness --locked -- report --format json`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- `cargo build -p minimax-cli --locked` and the existing npm installed-command smoke remain green.

## Success Criteria

- RCMP-01 is satisfied without a live TS baseline.
- D-12-01/D-12-03 and all Phase 12 compatibility edges/prohibitions are traceable.
- No TS product/test source or migration fixture is deleted by this plan.

## Artifacts this phase produces

- `fixtures/compat/public-contract.v1.json`: new strict public-contract/provenance manifest.
- `fixtures/compat/baseline-status.v1.json`: deleted after all consumers migrate; no replacement runtime is created.
- `fixtures/compat/report.expected.json`: modified contract-versus-Rust golden.
- `crates/compat-harness/src/manifest.rs`: modified `PublicContractManifest`/`CompatManifests` contract.
- `crates/compat-harness/src/report.rs`: modified report fields and completeness logic.
- `crates/compat-harness/src/baseline.rs` and `tests/compat_report.rs`: modified public-contract parameters/assertions; None new product symbols or commands.
