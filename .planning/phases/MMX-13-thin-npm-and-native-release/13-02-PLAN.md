---
phase: 13-thin-npm-and-native-release
plan: "02"
type: execute
wave: 11
depends_on:
  - "13-01"
files_modified:
  - fixtures/compat/release/targets.v1.json
  - scripts/release/package-contract.mjs
  - scripts/release/package-contract.test.mjs
  - scripts/release/package-rust.mjs
  - scripts/release/verify-rust-release.mjs
  - fixtures/compat/release/thresholds.v1.json
  - fixtures/compat/source-authority.v1.json
  - crates/compat-harness/tests/source_authority.rs
autonomous: true
requirements:
  - RNPM-01
  - RNPM-03
must_haves:
  truths:
    - "Native archives and npm artifacts are assembled deterministically from the same exact Rust binary, launcher, metadata, docs, and licenses per D-13-02."
    - "Windows x64 MSVC and Linux x64 GNU are the only hosted-release targets; GNU-LLVM is explicitly development-only per D-13-05."
    - "Release manifests bind target triple, filename/mode, binary/launcher/npm hashes, archive contents, support tier, and product fingerprint."
  prohibitions:
    - statement: "MUST NOT expand hosted support to macOS, ARM, or GNU-LLVM Windows."
      status: resolved
      verification: "Strict target fixture accepts exactly two hosted identities and one named development-only tier."
    - statement: "MUST NOT include generated legacy output, embedding weights, unsafe archive paths/types, or unlisted files."
      status: resolved
      verification: "Shared package-contract validation checks the exact canonical entry set before evidence output."
    - statement: "MUST NOT treat development-only GNU-LLVM evidence as hosted MSVC."
      status: resolved
      verification: "Target triple, rustc host, support tier, and hosted evidence class must all agree."
    - statement: "MUST NOT publish or perform a runtime download."
      status: resolved
      verification: "Scripts read local staged inputs and write only under target candidate directories."
  artifacts:
    - path: fixtures/compat/release/targets.v1.json
      provides: "Strict supported/development target identity and binary-mode contract"
    - path: scripts/release/package-contract.mjs
      provides: "Shared distribution-only target/manifest/archive contract helpers"
      exports: ["loadTargetContract", "validateReleaseManifest", "expectedArchiveEntries"]
    - path: scripts/release/package-contract.test.mjs
      provides: "Package-test-only table-driven schema and target rejection suite"
    - path: scripts/release/package-rust.mjs
      provides: "Deterministic native/npm candidate assembly"
  key_links:
    - from: scripts/release/package-rust.mjs
      to: scripts/release/package-contract.mjs
      via: "target lookup and canonical manifest/entry construction"
      pattern: "loadTargetContract"
    - from: scripts/release/verify-rust-release.mjs
      to: fixtures/compat/release/targets.v1.json
      via: "independent host/target/filename/mode/support-tier verification"
      pattern: "rustcHost"
---

# Phase 13 Plan 02: Checksummed Native and npm Artifacts

## Goal / Objective

<objective>
Assemble deterministic, checksummed native and npm candidates for the two supported targets from one exact Rust binary contract.

Purpose: Preserve D-13-02/D-13-05 and give RNPM-01/RNPM-03 a strict positive artifact path.
Output: Target contract, shared package contract, and deterministic native/npm manifest/checksum production and verification.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/MMX-13-thin-npm-and-native-release/13-SPEC.md
@.planning/phases/MMX-13-thin-npm-and-native-release/13-CONTEXT.md
@scripts/release/package-rust.mjs
@scripts/release/verify-rust-release.mjs
@fixtures/compat/release/thresholds.v1.json
@fixtures/compat/release/hosted-gates.v1.json
@package.json
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define one strict target and release-manifest contract</name>
  <files>fixtures/compat/release/targets.v1.json, scripts/release/package-contract.mjs, scripts/release/package-contract.test.mjs, fixtures/compat/release/thresholds.v1.json, fixtures/compat/source-authority.v1.json, crates/compat-harness/tests/source_authority.rs</files>
  <read_first>
    - fixtures/compat/release/targets.v1.json — create/modify target
    - scripts/release/package-contract.mjs — create/modify target
    - fixtures/compat/release/thresholds.v1.json — size/performance source modified only to link schema version
    - scripts/release/package-rust.mjs — current platform detection/manifest shape
    - scripts/release/verify-rust-release.mjs — current target/support-tier checks
    - fixtures/compat/release/hosted-gates.v1.json — hosted environment identity schema
    - fixtures/compat/source-authority.v1.json — release-script authority classes updated atomically with both new MJS files
    - crates/compat-harness/tests/source_authority.rs — exact package-test-only/distribution-only classification assertions updated by this task
    - .planning/phases/MMX-13-thin-npm-and-native-release/13-CONTEXT.md — D-13-02/D-13-05
  </read_first>
  <behavior>
    - Target fixture has exact platform ID, rustc host triple, OS/arch, binary name, executable mode policy, archive suffix, and support tier.
    - Two hosted targets and the existing Windows GNU-LLVM development tier are distinct; development evidence can never satisfy hosted identity.
    - Manifest schema requires version/platform/host/support tier, binary/launcher/npm/archive hashes, product fingerprint/count, embedding flag false, and canonical entries.
    - Table-driven negatives cover unknown fields, unsafe names/path traversal, duplicate targets/entries, hosted-vs-development tier confusion, malformed hashes, unsupported platform identities, and embedding inclusion.
    - `package-contract.mjs` is classified distribution-only and `package-contract.test.mjs` package-test-only; assertion/fixture strings in the test file are not scanned as executable fallback logic.
  </behavior>
  <action>
Create a strict target fixture and shared MJS contract helpers using only Node built-ins. Per D-13-05, enumerate Windows x64 MSVC and Linux x64 GNU as `hosted_release`; retain Windows x64 GNU-LLVM only as `development_only` and require its full identity on every check. Create `package-contract.test.mjs` now with a table-driven negative matrix for unknown fields, unsafe names/path traversal, duplicate targets/entries, hosted/development tier confusion, malformed binary/launcher/npm/archive hashes, unsupported platform/host pairs, and embedding inclusion; each row must assert the exact rejection category and the healthy control must pass. In the same atomic task, update `source-authority.v1.json` and its Rust tests so `package-contract.mjs` is `release_orchestration`/distribution-only and `package-contract.test.mjs` is `package_test_only`, neither is a product executable, and the forbidden-token scan analyzes executable production code rather than treating fixture or assertion string literals as fallback/download behavior. Actual imports/spawns/download/fallback constructs in production MJS must still fail. Link threshold schema/version without changing locked numeric budgets.
  </action>
  <verify>
    <automated>node --test scripts/release/package-contract.test.mjs &amp;&amp; cargo test -p minimax-compat-harness --test source_authority --locked</automated>
  </verify>
  <acceptance_criteria>
    - Exact supported/development identities are parseable and all malformed/tier-confused variants fail.
    - Every named unknown-field/unsafe-name/duplicate/tier/hash/platform/embedding negative row executes and passes.
    - Numeric performance/size thresholds remain unchanged.
    - Shared helper contains only package/target/archive validation logic, and source authority distinguishes package-test fixture/assertion strings from executable fallback logic.
  </acceptance_criteria>
  <done>Every artifact is governed by one explicit target and manifest schema before bytes are assembled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assemble and independently verify deterministic native/npm candidates</name>
  <files>scripts/release/package-rust.mjs, scripts/release/verify-rust-release.mjs, scripts/release/package-contract.mjs</files>
  <read_first>
    - scripts/release/package-contract.mjs — target/manifest/entry contract from Task 1
    - scripts/release/package-rust.mjs — deterministic tar/gzip and output target modified by this task
    - scripts/release/verify-rust-release.mjs — archive/npm verification modified by this task
    - package.json — thin npm metadata and file allowlist from 13-01
    - bin/minimax-codex.cjs — launcher included by exact hash
    - docs/release/install-upgrade-rollback.md — native distribution files included in artifacts
  </read_first>
  <behavior>
    - Native archive and npm package contain canonical entries only and share exact launcher/binary bytes.
    - Sidecars use canonical SHA-256 plus filename format; manifest hashes match source and contained bytes.
    - Linux binary entry is executable; Windows binary identity and target triple are MSVC for hosted candidates.
    - Two builds from identical inputs produce byte-identical archives/checksums/manifests.
  </behavior>
  <action>
Refactor `package-rust.mjs` to consume the target contract, emit the strict release manifest, and assemble canonical deterministic native/npm tarballs from the staged Rust binary. Keep all writes under an explicit `target/` candidate directory. Refactor positive verification to parse independently, validate gzip/tar checksums/types/modes/paths, compare every source and contained hash, enforce target identity/tier/fingerprint, and reject extra/missing entries before any installed smoke. Do not add publication or runtime retrieval.
  </action>
  <verify>
    <automated>npm run build:rust:release &amp;&amp; npm run package:rust &amp;&amp; npm run verify:rust-release</automated>
  </verify>
  <acceptance_criteria>
    - Current-host candidate passes exact target, archive, checksum, manifest, hash, size, license, and embedding-exclusion checks.
    - Repeating packaging with unchanged inputs yields identical bytes.
    - Hosted-target selection cannot accept the GNU-LLVM development tier as MSVC.
  </acceptance_criteria>
  <done>Both installation channels are checksummed views of one exact native Rust product.</done>
</task>

</tasks>

## Verification

- `npm run build:rust:release`
- Run `npm run package:rust` twice in separate target directories and compare archive/sidecar hashes.
- `npm run verify:rust-release`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- Rust CLI and staged npm launcher `--version` remain identity-equal.

## Success Criteria

- RNPM-01/RNPM-03 and D-13-02/D-13-05 are satisfied on the current candidate tier.
- EDGE-02 and checksum/fingerprint drift are blocked before release evidence.
- Only Windows x64 MSVC/Linux x64 GNU are hosted-release targets.

## Artifacts this phase produces

- `fixtures/compat/release/targets.v1.json`: new strict target/support-tier schema.
- `scripts/release/package-contract.mjs`: new distribution-only manifest/target/archive helper exports.
- `scripts/release/package-contract.test.mjs`: new package-test-only table-driven contract rejection suite created and run in Task 1.
- `scripts/release/package-rust.mjs`: modified deterministic native/npm assembly and manifest fields.
- `scripts/release/verify-rust-release.mjs`: modified independent positive verification.
- `fixtures/compat/release/thresholds.v1.json`: schema link only; locked numeric values remain unchanged.
- `fixtures/compat/source-authority.v1.json` and `crates/compat-harness/tests/source_authority.rs`: modified atomically to classify the new distribution/test MJS files and scan only executable fallback behavior.
