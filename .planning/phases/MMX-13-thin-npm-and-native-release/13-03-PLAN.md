---
phase: 13-thin-npm-and-native-release
plan: "03"
type: execute
wave: 12
depends_on:
  - "13-02"
files_modified:
  - scripts/release/package-contract.mjs
  - scripts/release/package-contract.test.mjs
  - scripts/release/package-rust.mjs
  - scripts/release/verify-rust-release.mjs
  - scripts/release/verify-milestone-flow.mjs
  - package.json
  - .github/workflows/ci.yml
  - fixtures/compat/source-authority.v1.json
  - crates/compat-harness/tests/source_authority.rs
autonomous: true
requirements:
  - RNPM-01
  - RNPM-03
must_haves:
  truths:
    - "Offline native and npm installed smokes execute the expected checksummed Rust binary with no runtime download."
    - "Missing, wrong-platform, renamed, non-executable, hash-mismatched, unsafe-type, or extra-entry artifacts fail with stable actionable non-zero outcomes per D-13-04."
    - "Package failure blocks release even when the Rust source binary itself is healthy."
  prohibitions:
    - statement: "MUST NOT fall back to another runtime or search/download a replacement binary."
      status: resolved
      verification: "Corruption cases run in isolated PATH/network-free fixtures and assert no alternate child marker."
    - statement: "MUST NOT let a passing source-tree command substitute for a failing packed artifact."
      status: resolved
      verification: "Milestone flow requires extracted native and npm smoke evidence bound to the package hashes."
    - statement: "MUST NOT publish, push, open a PR, or trigger hosted evidence in Phase 13."
      status: resolved
      verification: "CI/tests create local candidates only; hosted refresh remains Phase 14 manual work."
  artifacts:
    - path: scripts/release/package-contract.test.mjs
      provides: "Node built-in package-test-only positive/corruption tests with no product authority"
    - path: scripts/release/verify-rust-release.mjs
      provides: "Offline extracted native/npm identity smoke and actionable failures"
    - path: scripts/release/verify-milestone-flow.mjs
      provides: "Complete package evidence gate bound to current product fingerprint"
  key_links:
    - from: scripts/release/package-contract.test.mjs
      to: scripts/release/package-contract.mjs
      via: "synthetic archive/target/manifest corruption matrix"
      pattern: "validateReleaseManifest"
    - from: scripts/release/verify-milestone-flow.mjs
      to: target/release-evidence
      via: "requires both native and npm installed Rust identity/hash evidence"
      pattern: "installed"
---

# Phase 13 Plan 03: Offline Installed and Corruption Gates

## Goal / Objective

<objective>
Prove the packed native/npm paths offline and reject every locked corrupt or incompatible artifact before release.

Purpose: Finish RNPM-01/RNPM-03 and D-13-04 with package-level negative coverage and release-blocking installed evidence.
Output: Corruption matrix, extracted native/npm smoke, milestone evidence contract, and CI wiring.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/MMX-13-thin-npm-and-native-release/13-SPEC.md
@.planning/phases/MMX-13-thin-npm-and-native-release/13-CONTEXT.md
@scripts/release/package-contract.mjs
@scripts/release/verify-rust-release.mjs
@scripts/release/verify-milestone-flow.mjs
@fixtures/compat/release/targets.v1.json
@.github/workflows/ci.yml
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the fail-closed package corruption matrix</name>
  <files>scripts/release/package-contract.mjs, scripts/release/package-contract.test.mjs, package.json, fixtures/compat/source-authority.v1.json, crates/compat-harness/tests/source_authority.rs</files>
  <read_first>
    - scripts/release/package-contract.mjs — pure contract helpers modified for testability
    - scripts/release/package-contract.test.mjs — create/modify target
    - package.json — `test:package` script added/modified by this task
    - fixtures/compat/release/targets.v1.json — exact host/filename/mode source
    - bin/minimax-codex.cjs — launcher error categories mirrored by fixture expectations
    - .planning/SPEC.md — EDGE-01/EDGE-02 no-fallback and wrong-platform requirements
    - fixtures/compat/source-authority.v1.json — distribution-only/package-test-only MJS classifications modified atomically
    - crates/compat-harness/tests/source_authority.rs — executable-code versus assertion/fixture-string scan cases modified atomically
  </read_first>
  <behavior>
    - Tests cover absent binary, wrong target, renamed file, Linux lost execute bit, symlink/unsafe type, checksum mismatch, product fingerprint drift, launcher mismatch, extra executable, truncated/corrupt archive, and invalid sidecar.
    - Each case fails before installed smoke and returns a stable actionable category.
    - Healthy fixtures pass twice with identical results.
    - Source authority keeps `package-contract.mjs` distribution-only and `package-contract.test.mjs` package-test-only, and ignores assertion/fixture string literals while rejecting real executable fallback constructs.
  </behavior>
  <action>
Extend the table-driven `node:test` suite created in 13-02 using temporary directories, synthetic bytes, and exported distribution-only validation helpers to port the remaining package responsibilities formerly held by TS tests. Keep fixtures local and network-disabled; add an alternate-child marker that must remain absent in all failures. Add `test:package` as `node --test scripts/release/package-contract.test.mjs`. In the same atomic change, update `source-authority.v1.json` and its Rust tests so `package-contract.mjs` remains distribution-only and `package-contract.test.mjs` is package-test-only, neither is executable product authority, assertion/fixture strings containing words such as download/fallback/spawn do not create false positives, and actual executable imports/spawns/network/runtime retrieval still fail. Do not let these Node tests substitute for Rust behavior/evaluation gates; they validate only package bytes/metadata/launcher boundaries.
  </action>
  <verify>
    <automated>npm run test:package &amp;&amp; cargo test -p minimax-compat-harness --test source_authority --locked</automated>
  </verify>
  <acceptance_criteria>
    - Every RNPM-03 corruption class has a named passing negative test and stable failure category.
    - No case accesses network, credentials, user state, or an external runtime.
    - Test file is accepted only as package-test-only JavaScript, not as an executable or distribution helper.
  </acceptance_criteria>
  <done>Invalid package bytes cannot progress to installation or evidence generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run offline extracted native and npm installed Rust smokes</name>
  <files>scripts/release/package-rust.mjs, scripts/release/package-contract.test.mjs, scripts/release/verify-rust-release.mjs, scripts/release/verify-milestone-flow.mjs, fixtures/compat/source-authority.v1.json, crates/compat-harness/tests/source_authority.rs</files>
  <read_first>
    - scripts/release/package-rust.mjs — explicit fingerprint input argument and deterministic package assembly modified by this task
    - scripts/release/package-contract.test.mjs — argument-forwarding and missing/mismatched fingerprint cases modified by this task
    - fixtures/compat/source-authority.v1.json — package/release script classifications synchronized in this task
    - crates/compat-harness/tests/source_authority.rs — synchronized release-script authority assertions
    - scripts/release/verify-rust-release.mjs — extraction and installed smoke target modified by this task
    - scripts/release/verify-milestone-flow.mjs — final composition evidence target modified by this task
    - scripts/release/package-contract.mjs — canonical manifest/entry parser
    - fixtures/compat/release/targets.v1.json — expected current-host identity
    - package.json — packed one-bin metadata
    - crates/cli/src/app.rs — stable `--version` and read-only command surface
  </read_first>
  <behavior>
    - Native archive smoke executes its extracted binary; npm smoke installs/extracts locally and executes the package bin launcher.
    - Both return the same version/product identity and execute a read-only capability status command.
    - Evidence records artifact hashes, binary hash, identity output, offline/provider/credential/download counts, and current product fingerprint.
    - `package-rust.mjs` and `verify-milestone-flow.mjs` accept the same explicit `--fingerprint-file <path>` contract, reject missing/malformed/mismatched fingerprint inputs, and never fall back to a default stale fingerprint.
  </behavior>
  <action>
After archive validation, extract native and npm candidates into separate target-contained roots. Run the exact native binary and npm bin with a controlled empty environment, offline npm configuration, no lifecycle scripts, no network, and isolated project/state directories. Compare `--version`, binary SHA-256, and read-only capability status output to the manifest/source binary. Modify `package-rust.mjs` and `verify-milestone-flow.mjs` in this task to implement one explicit `--fingerprint-file <path>` argument, alongside explicit `--binary`, `--output`/`--artifacts`, and `--evidence-dir` paths, so Phase 14 can bind every check to one clean task-specific build and artifact directory instead of default or stale `target/` outputs. Add Node tests that spawn/parse both CLIs with a temporary fingerprint file and prove success for an exact fingerprint plus stable rejection for absent, malformed, stale, and artifact-mismatched fingerprint files. Update `source-authority.v1.json` and its Rust test in the same task so all touched package/release scripts retain their exact distribution-only/package-test-only classifications and executable fallback scanning. Update milestone-flow validation so both installed paths and their hashes are mandatory; a healthy source binary cannot compensate for a failing package.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command '$p="target/phase13-installed"; if (Test-Path -LiteralPath $p) { Remove-Item -LiteralPath $p -Recurse -Force }; New-Item -ItemType Directory -Force -Path "$p/artifacts","$p/evidence" | Out-Null; $env:CARGO_TARGET_DIR="$p/cargo"; npm run test:package; if ($LASTEXITCODE) { exit $LASTEXITCODE }; node scripts/release/product-fingerprint.mjs &gt; "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run build:rust:release; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli.exe"; if (-not (Test-Path -LiteralPath $b)) { $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli" }; npm run package:rust -- --binary $b --output "$p/artifacts" --fingerprint-file "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:rust-release -- --binary $b --artifacts "$p/artifacts" --evidence-dir "$p/evidence"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:milestone-flow -- --artifacts "$p/artifacts" --evidence-dir "$p/evidence" --fingerprint-file "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo test -p minimax-compat-harness --test source_authority --locked; exit $LASTEXITCODE'</automated>
  </verify>
  <acceptance_criteria>
    - Both extracted installation paths run the expected Rust binary entirely offline.
    - Evidence is bound to exact archive/npm/binary hashes and the current product fingerprint.
    - Missing/wrong/corrupt/non-executable candidates never reach a different runtime.
    - Both package assembly and milestone verification consume the explicit tested fingerprint path used by 14-01; no default-target fingerprint fallback exists.
  </acceptance_criteria>
  <done>The installed product, not merely the source binary, is release-gated.</done>
</task>

<task type="auto">
  <name>Task 3: Make package corruption and installed smoke mandatory in CI</name>
  <files>.github/workflows/ci.yml, package.json, scripts/release/verify-milestone-flow.mjs</files>
  <read_first>
    - .github/workflows/ci.yml — release command order modified by this task
    - package.json — exact test/package/verify aliases modified by this task
    - scripts/release/verify-milestone-flow.mjs — CI evidence assertions modified by this task
    - .planning/phases/MMX-13-thin-npm-and-native-release/13-SPEC.md — offline/no-publish scope
  </read_first>
  <action>
Order CI so Rust format/Clippy/tests, coverage, Provider/retrieval evaluators, compatibility/migration support, package corruption tests, build, package, installed verification, and milestone flow all block in that order. Preserve candidate-versus-strict hosted freshness branching, read-only permissions, Linux sandbox canary, and the two-host matrix. Add no registry publication, tag, push, PR, or runtime download step.
  </action>
  <verify>
    <automated>npm run test:package &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - CI cannot reach evidence upload after a Rust evaluator, compatibility, migration, package corruption, or installed smoke failure.
    - The workflow has no publication/write authority and no unsupported platform.
    - Current Rust CLI and npm-installed command pass at plan end.
  </acceptance_criteria>
  <done>Phase 13 leaves one offline-verifiable native/npm Rust release path ready for final cleanup.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test --workspace --locked`
- `npm run eval:provider && npm run eval:retrieval`
- `npm run test:package`
- Generate `target/phase13-installed/fingerprint.json` first, then use `CARGO_TARGET_DIR=target/phase13-installed/cargo` and pass the resolved release binary plus `--output target/phase13-installed/artifacts --fingerprint-file target/phase13-installed/fingerprint.json` to `package:rust`; pass the same explicit binary/artifact/evidence paths to `verify:rust-release` and `--artifacts target/phase13-installed/artifacts --evidence-dir target/phase13-installed/evidence --fingerprint-file target/phase13-installed/fingerprint.json` to `verify:milestone-flow`.
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`

## Success Criteria

- RNPM-01/RNPM-03 and D-13-04 are complete.
- Every package edge fails closed before release with no fallback/download.
- No hosted trigger, push, PR, publication, API call, or model download occurs.

## Artifacts this phase produces

- `scripts/release/package-contract.test.mjs`: extended package-test-only corruption test suite first created by 13-02.
- `scripts/release/package-contract.mjs`: modified testable validation exports.
- `scripts/release/package-rust.mjs`: modified and tested explicit `--fingerprint-file`/binary/output argument ownership used by Phase 14 clean-root runs.
- `scripts/release/verify-rust-release.mjs`: modified native/npm installed smoke/evidence fields.
- `scripts/release/verify-milestone-flow.mjs`: modified mandatory installed hash/identity composition contract plus the same tested explicit `--fingerprint-file` input.
- `package.json`: new/modified `test:package` and aggregate local release aliases.
- `.github/workflows/ci.yml`: modified strict order only; no new authority or platform.
- `fixtures/compat/source-authority.v1.json` and `crates/compat-harness/tests/source_authority.rs`: modified with the new MJS files so package-test literals are separated from executable fallback logic.
