---
phase: 13-thin-npm-and-native-release
plan: "01"
type: execute
wave: 10
depends_on:
  - "12-02"
files_modified:
  - package.json
  - package-lock.json
  - bin/minimax-codex.cjs
  - fixtures/compat/source-authority.v1.json
  - crates/compat-harness/src/baseline.rs
  - crates/compat-harness/tests/compat_report.rs
  - crates/cli/tests/product_identity.rs
autonomous: true
requirements:
  - RNPM-01
  - RNPM-02
must_haves:
  truths:
    - "Global npm install and npx expose one `minimax-codex` command that starts the packaged Rust binary per D-13-01 and D-13-03."
    - "Package metadata has no legacy bin/path, TS compiler/runtime, React/Ink runtime, or TypeScript-only dependency/script."
    - "Launcher errors are stable, actionable, and never trigger fallback/runtime download per D-13-04."
  prohibitions:
    - statement: "MUST NOT include a legacy command, generated legacy output, or TypeScript/React/Ink dependency in the packed npm package."
      status: resolved
      verification: "Package metadata/lockfile and tar manifest assertions require the exact thin file/dependency set."
    - statement: "MUST NOT implement Provider, retrieval, session, Vault, tool, migration, or other product behavior in JavaScript."
      status: resolved
      verification: "Phase 10 source-authority validation remains mandatory after the package prune."
    - statement: "MUST NOT publish to npm or download a runtime."
      status: resolved
      verification: "Only local `npm pack`/offline install paths run; launcher and scripts use local files."
  artifacts:
    - path: package.json
      provides: "Thin dependency-free npm distribution metadata and Rust/release scripts"
    - path: package-lock.json
      provides: "Lockfile with no legacy application dependencies"
    - path: bin/minimax-codex.cjs
      provides: "Fixed supported-host Rust locator/launcher with stable fail-closed error codes/messages"
  key_links:
    - from: package.json
      to: bin/minimax-codex.cjs
      via: "the sole npm bin mapping and exact files allowlist"
      pattern: "bin/minimax-codex.cjs"
    - from: bin/minimax-codex.cjs
      to: minimax-codex.exe|minimax-codex
      via: "platform/arch map plus safe regular-file/executable checks"
      pattern: "win32:x64|linux:x64"
---

# Phase 13 Plan 01: Thin npm Metadata and Launcher

## Goal / Objective

<objective>
Reduce npm to a dependency-free distribution shell with one Rust command and explicit no-fallback failures.

Purpose: Implement RNPM-01/RNPM-02 and D-13-01/D-13-03/D-13-04 before archive assembly.
Output: Minimal package metadata/lockfile, hardened thin launcher, and Rust-owned metadata/identity assertions.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-13-thin-npm-and-native-release/13-SPEC.md
@.planning/phases/MMX-13-thin-npm-and-native-release/13-CONTEXT.md
@package.json
@package-lock.json
@bin/minimax-codex.cjs
@fixtures/compat/source-authority.v1.json
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prune package metadata, scripts, files, and lockfile to the Rust distribution shell</name>
  <files>package.json, package-lock.json, fixtures/compat/source-authority.v1.json, crates/compat-harness/src/baseline.rs, crates/compat-harness/tests/compat_report.rs</files>
  <read_first>
    - package.json — bins/files/scripts/dependencies target modified by this task
    - package-lock.json — root and transitive package graph target modified by this task
    - fixtures/compat/source-authority.v1.json — exact JavaScript/package-entry allowlist modified by this task
    - crates/compat-harness/src/baseline.rs — package contract validator modified by this task
    - crates/compat-harness/tests/compat_report.rs — package metadata negative assertions modified by this task
    - .planning/phases/MMX-13-thin-npm-and-native-release/13-SPEC.md — exact thin package acceptance contract
  </read_first>
  <behavior>
    - `bin` has only `minimax-codex`; `files` lists only launcher, release docs/licenses/readme, and platform native filenames.
    - Scripts retained are Rust checks/tests/evaluators and local release/package orchestration; legacy dev/build/test/eval/live-smoke scripts are absent.
    - Dependencies/devDependencies/optionalDependencies have no TS compiler/runtime, React/Ink UI, Node keyring, or legacy-only package graph.
  </behavior>
  <action>
Per D-13-01, preserve npm global/npx ergonomics; per D-13-03, keep Node only for the launcher and release orchestration. Remove legacy source/build/test/eval/smoke scripts and all application dependencies that existed only for TypeScript, React/Ink, or the Node credential path. Regenerate the lockfile mechanically from the pruned package manifest without installing/publishing arbitrary new packages. Update source-authority/package contract tests to require the exact allowed keys/files and reject reintroduced legacy bins, product source, runtime dependencies, install hooks, or download URLs.
  </action>
  <verify>
    <automated>npm install --package-lock-only --ignore-scripts --offline &amp;&amp; cargo test -p minimax-compat-harness --test compat_report --locked rust_command_permission_provider_and_product_baselines_are_executable -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - Package/lock root metadata contains no legacy application dependencies or scripts.
    - `npm pack --ignore-scripts --dry-run` shows only the thin allowed files plus a present platform binary when staged.
    - Rust evaluator/check/release aliases remain functional and no publish/install hook is added.
  </acceptance_criteria>
  <done>The npm manifest describes distribution only, not a second application runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize launcher selection and actionable fail-closed outcomes</name>
  <files>bin/minimax-codex.cjs, crates/cli/tests/product_identity.rs, crates/compat-harness/tests/compat_report.rs</files>
  <read_first>
    - bin/minimax-codex.cjs — platform map/file safety/spawn/error target modified by this task
    - crates/cli/tests/product_identity.rs — identity/error integration target modified by this task
    - crates/compat-harness/tests/compat_report.rs — source/package contract negative tests modified by this task
    - package.json — sole entry and supported file names from Task 1
    - .planning/SPEC.md — EDGE-01 and no runtime download/fallback contract
  </read_first>
  <behavior>
    - Windows x64 maps only to `minimax-codex.exe`; Linux x64 maps only to `minimax-codex`.
    - Unsupported host, missing, symlink/non-file, non-executable, spawn error, and signal termination produce stable categorized non-zero errors with reinstall/expected-path guidance.
    - Arguments are forwarded without shell/path search/environment override and Rust exit codes are preserved.
  </behavior>
  <action>
Refine the existing thin CJS launcher without adding dependencies or product logic. Define stable error prefixes/codes for unsupported host, missing file, unsafe type, missing executable bit, start failure, and signal termination; include expected platform/path and reinstall guidance. Per D-13-04, no error may mention or start an alternate runtime or runtime download. Extend Rust-owned fixture tests to cover every branch, Unicode/metacharacter argv forwarding, sibling-hash identity, shell false, and exit-code preservation.
  </action>
  <verify>
    <automated>cargo test -p minimax-cli --test product_identity --locked &amp;&amp; cargo test -p minimax-compat-harness --test compat_report --locked</automated>
  </verify>
  <acceptance_criteria>
    - Every locked failure is actionable, stable, non-zero, and occurs before any alternate child can start.
    - Supported-host success reaches the exact sibling Rust binary with unchanged argv.
    - Launcher remains distribution-only under the Phase 10 source gate.
  </acceptance_criteria>
  <done>npm/npx have one predictable local Rust path and explicit no-fallback failures.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-cli --test product_identity --locked`
- `cargo test -p minimax-compat-harness --test compat_report --test source_authority --locked`
- `npm pack --ignore-scripts --dry-run`
- Build/stage the Rust binary and run the launcher `--version` plus one read-only capability status command.

## Success Criteria

- RNPM-01/RNPM-02 and D-13-01/D-13-03/D-13-04 are enforced.
- EDGE-01 is covered with no fallback/download.
- Rust CLI and npm installed path remain usable after dependency pruning.

## Artifacts this phase produces

- None new.
- `package.json`: modified single-bin, thin-files, Rust/release-script, zero-legacy-dependency contract.
- `package-lock.json`: mechanically modified to match the pruned dependency graph.
- `bin/minimax-codex.cjs`: modified stable error taxonomy while retaining fixed sibling launch.
- Source/package/identity Rust tests: modified assertions; no JavaScript business implementation.
