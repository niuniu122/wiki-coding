---
phase: MMX-09-capability-workspace-and-non-programmer-harness
plan: "03"
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - README.md
  - docs/capability-workspace.md
  - fixtures/compat/retrieval/capability-workspace.v1.json
  - .planning/phases/MMX-09-capability-workspace-and-non-programmer-harness/09-VERIFICATION.md
autonomous: true
requirements: [CAPW-08]
must_haves:
  truths:
    - "Mixed-language intent and readiness fixtures cover all kinds and safe failure paths."
    - "Documentation explains the source/runtime boundary without promising an installer."
    - "Existing retrieval, tool, sandbox, Vault, Wiki, and release gates remain green."
  artifacts:
    - path: docs/capability-workspace.md
      provides: Non-programmer architecture and authority guide
    - path: fixtures/compat/retrieval/capability-workspace.v1.json
      provides: Deterministic intent/readiness cases
    - path: .planning/phases/MMX-09-capability-workspace-and-non-programmer-harness/09-VERIFICATION.md
      provides: Exact phase evidence
  key_links:
    - from: fixtures/compat/retrieval/capability-workspace.v1.json
      to: crates/retrieval/tests/capability_workspace.rs
      via: offline labeled evaluation
      pattern: "capability-workspace.v1.json"
---

# Plan 09-03: Evaluation, Documentation, and Regression Gates

## Goal

Make the workspace understandable to non-programmers and prove it does not weaken existing retrieval, tool, sandbox, or release behavior.

## Tasks

1. Add mixed-language intent/readiness fixtures and compatibility assertions.
2. Document source catalogs, runtime separation, readiness, safe next actions, and deferred installer/runtime work.
3. Run formatting, Clippy, Rust workspace tests, TypeScript checks/tests/build, retrieval eval, and architecture/release guards.
4. Record exact verification evidence and close Phase 9 requirements only after all mandatory gates pass.

<tasks>

<task type="auto">
  <name>Task 1: Publish fixtures and user-facing boundary documentation</name>
  <files>README.md, docs/capability-workspace.md, fixtures/compat/retrieval/capability-workspace.v1.json</files>
  <read_first>capabilities/README.md, README.md, .planning/phases/MMX-09-capability-workspace-and-non-programmer-harness/09-SPEC.md</read_first>
  <action>Document the three catalogs, readiness labels, BM25/embedding order, safe next actions, and deferred installer/runtime. Add deterministic Chinese/English cases.</action>
  <verify>cargo test -p minimax-retrieval -p minimax-cli --locked</verify>
  <acceptance_criteria>Docs match executable routes and fixtures cover each kind/readiness state and no-match behavior.</acceptance_criteria>
  <done>The feature can be understood and audited without programming knowledge.</done>
</task>

<task type="auto">
  <name>Task 2: Run full regression gates and record evidence</name>
  <files>.planning/phases/MMX-09-capability-workspace-and-non-programmer-harness/09-VERIFICATION.md</files>
  <read_first>package.json, Cargo.toml, .github/workflows/ci.yml</read_first>
  <action>Run formatting, strict Rust tests and Clippy, TypeScript checks/tests/build, retrieval evaluation, and architecture/release guards; record only observed evidence.</action>
  <verify>git diff --check</verify>
  <acceptance_criteria>All mandatory local gates pass or an external hosted-only limitation is explicitly separated from product failures.</acceptance_criteria>
  <done>Phase 9 has reproducible evidence and no hidden regression.</done>
</task>

</tasks>

## Verification

`cargo fmt --all -- --check`, strict Clippy/tests, `npm run check`, `npm test`, `npm run build`, and `npm run eval:retrieval`.
