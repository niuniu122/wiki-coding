---
phase: MMX-04-vault-and-main-model-wiki
plan: "03"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - crates/protocol/src/vault.rs
  - crates/protocol/tests/vault_roundtrip.rs
  - crates/vault/src/lib.rs
  - crates/vault/src/lint.rs
  - crates/vault/src/rebuild.rs
  - crates/vault/src/gc.rs
  - crates/vault/src/forget.rs
  - crates/vault/tests/maintenance.rs
  - crates/vault/tests/retention.rs
  - crates/cli/src/app.rs
  - crates/cli/src/main.rs
  - crates/cli/src/headless.rs
  - crates/cli/tests/vault_commands.rs
  - crates/tui/src/command.rs
  - crates/tui/src/render.rs
  - crates/tui/tests/command_render.rs
  - crates/compat-harness/src/architecture.rs
  - crates/compat-harness/src/baseline.rs
  - crates/compat-harness/tests/architecture.rs
  - crates/compat-harness/tests/compat_report.rs
  - fixtures/compat/vault/maintenance.v1.json
autonomous: true
requirements: [VAULT-02, VAULT-04, VAULT-06, WIKI-04]
must_haves:
  truths:
    - "D-419: vault lint is read-only with stable issue codes/order, while repair performs only deterministic journal and prepared-transaction recovery."
    - "D-420: Explicit rebuild replaces compiled Wiki/index outputs through a recoverable transaction and leaves every raw byte/hash unchanged; it never runs automatically."
    - "D-421: GC classifies reference first, age second, size last; preview never deletes and apply recomputes all reachability before moving eligible objects to a plan-scoped trash directory."
    - "D-422: User input/final answers/commands/key tool facts/migration receipts, referenced or supersession evidence, pending/recovery/transaction data, and pins are never ordinary GC candidates."
    - "D-423: Trash remains undoable until its exact recorded seven-day expiry; purge is separate and needs a second exact plan-bound confirmation; gc exposes no force switch."
    - "D-424: vault forget inventories affected claims, commits removal or re-crystallization first, then deletes sensitive raw and writes a non-secret tombstone outside ordinary GC authority."
  artifacts:
    - path: crates/vault/src/lint.rs
      provides: Read-only deterministic integrity diagnostics and narrow deterministic repair
    - path: crates/vault/src/rebuild.rs
      provides: Explicit raw-preserving compiled Wiki/index rebuild transaction
    - path: crates/vault/src/gc.rs
      provides: Reference-safe report/apply/trash/undo/reconfirmed-purge lifecycle
    - path: crates/vault/src/forget.rs
      provides: Wiki-first privacy deletion with non-secret tombstone
    - path: crates/cli/tests/vault_commands.rs
      provides: Text/JSONL maintenance command and confirmation evidence
  key_links:
    - from: crates/vault/src/lint.rs
      to: crates/vault/src/transaction.rs
      via: report incomplete/tampered transactions and invoke only valid deterministic roll-forward during repair
      pattern: "LintIssue|repair|roll_forward"
    - from: crates/vault/src/gc.rs
      to: crates/vault/src/workflow.rs
      via: include pending jobs/receipts and source references in reachability before apply
      pattern: "Gc(Plan|Class|Receipt)|pending|source"
    - from: crates/vault/src/forget.rs
      to: crates/vault/src/transaction.rs
      via: commit claim removal/re-crystallization before sensitive raw deletion
      pattern: "Forget(Plan|Receipt)|KnowledgePatch|transaction"
  prohibitions:
    - statement: "MUST NOT persist credentials, environment secrets, request headers, or private raw reasoning in diagnostics, trash manifests, or tombstones."
      status: resolved
      verification: test
    - statement: "MUST NOT expose a GC force path that removes protected evidence or bypasses Wiki-first forget."
      status: resolved
      verification: test
    - statement: "MUST NOT automatically rebuild, apply GC, purge, forget, migrate, cut over, open a PR, or merge."
      status: resolved
      verification: judgment
---

<objective>
Complete the Vault lifecycle with deterministic diagnostics, raw-preserving rebuild, reference-safe reversible cleanup, privacy-aware forget, and honest CLI/compatibility evidence.

Purpose: A mature local knowledge tool must explain damage and disk usage, recover safely, and distinguish ordinary cleanup from intentional privacy deletion.
Output: Lint/repair/rebuild, GC report/apply/trash/undo/purge, forget, maintenance commands, and Phase 4 architecture/compatibility gates.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/MMX-04-vault-and-main-model-wiki/04-SPEC.md
@.planning/phases/MMX-04-vault-and-main-model-wiki/04-CONTEXT.md
@.planning/phases/MMX-04-vault-and-main-model-wiki/04-02-SUMMARY.md
@crates/vault/src/transaction.rs
@crates/vault/src/workflow.rs
@crates/cli/src/app.rs
@crates/tui/src/command.rs
@crates/compat-harness/src/architecture.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic lint, narrow repair, and raw-preserving rebuild</name>
  <files>crates/protocol/src/vault.rs, crates/protocol/tests/vault_roundtrip.rs, crates/vault/src/lib.rs, crates/vault/src/lint.rs, crates/vault/src/rebuild.rs, crates/vault/tests/maintenance.rs</files>
  <read_first>.planning/phases/MMX-04-vault-and-main-model-wiki/04-SPEC.md, crates/vault/src/bootstrap.rs, crates/vault/src/raw.rs, crates/vault/src/page.rs, crates/vault/src/transaction.rs, crates/vault/src/workflow.rs</read_first>
  <action>Define stable bounded maintenance issue/report/receipt records. Implement read-only lint over manifest/schema/project binding, owned paths and expected hashes, raw sequence/finalization/source reachability, strict page/frontmatter IDs/status, exactly one current page/topic, pending jobs, and transaction state; sort by issue code then normalized path/ID. Repair may only quarantine an incomplete final journal fragment or roll forward an already prepared validated transaction and reports all other damage unchanged. Implement explicit rebuild that snapshots raw hashes, derives compiled page/index targets from valid evidence and durable receipts without Provider calls, commits them through the transaction engine, re-lints, and proves all raw hashes unchanged. Never trigger rebuild at startup.</action>
  <verify>cargo test -p minimax-vault --locked maintenance</verify>
  <acceptance_criteria>Empty/clean/damaged/externally edited fixtures have stable issue codes/order; lint changes no bytes; repair touches only the two authorized recoverable cases and is idempotent; rebuild converges to one current truth, preserves provenance, is byte-stable on repeat, and leaves every raw hash unchanged; corrupt/unresolvable evidence fails without replacing compiled targets.</acceptance_criteria>
  <done>The user can diagnose and deterministically reconstruct compiled knowledge without weakening raw truth.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reference-safe reversible GC and Wiki-first forget</name>
  <files>crates/protocol/src/vault.rs, crates/vault/src/gc.rs, crates/vault/src/forget.rs, crates/vault/src/lib.rs, crates/vault/tests/retention.rs, fixtures/compat/vault/maintenance.v1.json</files>
  <read_first>.planning/phases/MMX-04-vault-and-main-model-wiki/04-CONTEXT.md, crates/protocol/src/vault.rs, crates/vault/src/raw.rs, crates/vault/src/workflow.rs, crates/vault/src/transaction.rs, crates/vault/src/rebuild.rs</read_first>
  <action>Build a deterministic reachability inventory from current/supersession sources, pending jobs, transactions/recovery, migration receipts, and explicit pins. Classify permanent/referenced/rebuildable/collectable in reference-age-size order. Report is the default and never mutates. Apply binds to the exact plan hash, reacquires the lease, recomputes inventory, rejects drift, and atomically moves only completed staging, derived caches, exact duplicate attachments, or unreferenced transient attachments older than 30 days into .minimax/trash/&lt;gc-id&gt; with hashes and exact seven-day expiry. Undo verifies the manifest and restores before expiry; at the expiry instant it remains undoable until later than expiry. Purge is a separate exact-plan confirmation token and removes only matching expired trash. Expose no force. Implement forget as a separate plan that inventories claims, requires a validated Wiki removal/re-crystallization transaction, then removes the selected sensitive raw object and writes a bounded non-secret tombstone; any earlier failure keeps raw.</action>
  <verify>cargo test -p minimax-vault --locked retention</verify>
  <acceptance_criteria>Mixed-graph fixtures never plan protected/permanent/pinned/pending/referenced evidence; preview changes no bytes; apply rejects any reachability/age/hash drift; one eligible object moves to trash and undo restores exact bytes through expiry; purge needs a second matching confirmation and cannot touch unexpired/mismatched trash; command/schema has no force; forget commits Wiki changes before raw removal and leaks no sensitive marker in its tombstone.</acceptance_criteria>
  <done>Ordinary disk cleanup is conservative, inspectable, and reversible, while privacy deletion follows its own claim-aware authority.</done>
</task>

<task type="auto">
  <name>Task 3: Expose maintenance commands and close Phase 4 evidence</name>
  <files>crates/cli/src/app.rs, crates/cli/src/main.rs, crates/cli/src/headless.rs, crates/cli/tests/vault_commands.rs, crates/tui/src/command.rs, crates/tui/src/render.rs, crates/tui/tests/command_render.rs, crates/compat-harness/src/architecture.rs, crates/compat-harness/src/baseline.rs, crates/compat-harness/tests/architecture.rs, crates/compat-harness/tests/compat_report.rs, fixtures/compat/vault/maintenance.v1.json, package.json</files>
  <read_first>.planning/phases/MMX-04-vault-and-main-model-wiki/04-02-SUMMARY.md, crates/cli/src/app.rs, crates/cli/src/headless.rs, crates/tui/src/command.rs, crates/tui/src/render.rs, crates/compat-harness/src/architecture.rs, crates/compat-harness/src/baseline.rs, package.json</read_first>
  <action>Route explicit vault bootstrap/status/lint/repair/rebuild/import/gc report/gc apply/gc undo/gc purge/forget commands through CLI-owned composition and typed text/JSONL rendering. Destructive actions display normalized plan ID, affected relative paths/classes/bytes, expiry or claim consequences, then require the exact action-specific token; EOF/non-interactive confirm rejects. Keep GC default report-only and make purge/forget impossible through a generic yes/force flag. Sanitize controls/secrets and expose stable codes. Add architecture gates that core has no filesystem/HTTP/Provider concrete dependency, Vault has no Provider/database dependency/call path, CLI/TUI do not parse Markdown, and package.json still points to dist/cli.js. Promote only executable VAULT/WIKI evidence from fixtures. Run all local Rust, TypeScript, eval, compatibility, architecture, forbidden-dependency, and diff gates; use no live Provider/model/download and do not change the npm entry or open/merge a PR.</action>
  <verify>cargo test -p minimax-cli --locked vault_commands; cargo test -p minimax-tui --locked command_render; cargo test -p minimax-compat-harness --locked architecture; cargo test -p minimax-compat-harness --locked compat_report; npm run verify:rust-contracts</verify>
  <acceptance_criteria>Text and JSONL expose the same stable maintenance facts; report-only commands need no confirmation and mutate nothing; apply/purge/forget bind exact visible plans and EOF rejects; no force parses; architecture scans prove no Vault-to-Provider/database or core concrete-I/O edge; all Phase 4 acceptance fixtures and complete local gates pass; package.json remains dist/cli.js.</acceptance_criteria>
  <done>Users can inspect, repair, rebuild, clean, undo, purge, or forget through an auditable mature CLI without hidden deletion or architecture drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault files -> lint/rebuild | Externally edited, corrupt, or secret-bearing data must be diagnosed without accidental mutation or disclosure. |
| Reachability plan -> GC apply/purge | State can drift between preview and action; protected evidence must remain outside cleanup authority. |
| Forget plan -> Wiki/raw mutation | Referenced sensitive evidence may be deleted only after affected claims are safely resolved. |
| Typed maintenance data -> terminal/JSONL | Paths, excerpts, confirmations, and tombstones must be bounded, stable, and non-secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Tampering | lint/repair/rebuild | critical | mitigate | read-only lint, narrow repair allowlist, expected-hash rebuild transaction |
| T-04-14 | Elevation of Privilege | GC authority | critical | mitigate | protected classes, no force, exact plan hash, revalidation under lease |
| T-04-15 | Tampering | trash/undo/purge | critical | mitigate | content hashes, exact expiry semantics, second plan-bound purge confirmation |
| T-04-16 | Information Disclosure | reports/tombstones | high | mitigate | no content excerpts, bounded relative IDs, sensitive marker scans |
| T-04-17 | Repudiation | destructive confirmation | high | mitigate | action-specific visible plan and durable receipt |
| T-04-18 | Tampering | Wiki-first forget | critical | mitigate | affected-claim inventory and committed transaction before raw removal |
</threat_model>

<verification>
Run focused maintenance/retention/CLI/TUI/compatibility tests; formatting; workspace Clippy with warnings denied; workspace Rust tests; npm check/tests/build and Rust-contract verification; retrieval/provider eval suites; architecture, forbidden-dependency, package-entry, secret-marker, and git diff checks. All Provider behavior remains scripted and all deletion fixtures remain inside temporary test Vaults.
</verification>

<success_criteria>
VAULT-06 and WIKI-04 maintenance behavior are executable and Phase 4 is fully closed: diagnostics are deterministic, rebuild preserves raw evidence, ordinary GC is report-first/protected/reversible, purge is separately reconfirmed, forget is Wiki-first, and architecture plus product-entry boundaries remain true.
</success_criteria>

<output>
Create `.planning/phases/MMX-04-vault-and-main-model-wiki/04-03-SUMMARY.md` and `04-VERIFICATION.md` when done.
</output>
