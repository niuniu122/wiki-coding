---
phase: MMX-06-migration-release-and-cutover
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
autonomous: true
requirements: [REL-01, REL-02, REL-03]
files_modified:
  - scripts/release/**
  - crates/compat-harness/src/release.rs
  - crates/compat-harness/src/main.rs
  - crates/compat-harness/tests/release_gate.rs
  - fixtures/compat/release/**
  - .github/workflows/ci.yml
  - package.json
  - docs/release/**
must_haves:
  truths:
    - "Versioned Windows/Linux base artifacts are checksum-covered, inspectable, and embedding-free."
    - "Security, license, packaging, and all four performance limits are enforced by one offline release gate."
    - "Hosted Windows MSVC and Linux run the same complete release verification."
    - "D-610 and D-613 through D-616 are executable."
  prohibitions:
    - "MUST NOT bundle weights, use credentials/network Providers, publish artifacts, or claim unsupported local MSVC proof."
---

<objective>Create reproducible base packages and the complete offline/cross-platform release gate.</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic versioned release archives and checksums</name>
  <action>Add a cross-platform packaging command that accepts a compiled binary, rejects unexpected files/symlinks/embedding content, writes a versioned platform archive plus manifest and SHA-256 file, and verifies archive contents/checksum. Add install, upgrade, rollback, and separate-embedding documentation.</action>
  <verify>npm run package:rust -- --fixture; npm run verify:rust-release -- --artifacts target/release-fixtures</verify>
</task>

<task type="auto">
  <name>Task 2: Enforce security, licenses, and footprint budgets</name>
  <action>Extend the compatibility harness with unsafe/database/network/model-bundle boundary checks, Cargo license allowlist evidence, package-size inspection, release-binary cold-start sampling, cross-platform idle-RSS sampling, and the existing 10k Wiki p95 evidence. Record environment and samples; fail any limit.</action>
  <verify>npm run verify:rust-release</verify>
</task>

<task type="auto">
  <name>Task 3: Make the complete release gate mandatory on Windows and Linux CI</name>
  <action>Extend CI to build release binaries and execute migration, compatibility, security, license, packaging, checksum, and performance gates on hosted Windows MSVC and Linux without credentials, downloads, or live calls. Upload is not required and publishing is forbidden.</action>
  <verify>npm run verify:release</verify>
</task>

</tasks>

<success_criteria>REL-01 through REL-03 have local fixture evidence and mandatory hosted Windows/Linux jobs.</success_criteria>
