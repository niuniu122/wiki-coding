---
phase: MMX-06-migration-release-and-cutover
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [MIGR-01, MIGR-02, MIGR-03]
files_modified:
  - crates/cli/src/migration.rs
  - crates/cli/src/app.rs
  - crates/cli/src/lib.rs
  - crates/cli/src/main.rs
  - crates/cli/tests/migration.rs
  - fixtures/compat/migration/typescript-v1/**
must_haves:
  truths:
    - "Inventory and dry-run are deterministic, explicit, and write nothing."
    - "Only allowlisted, non-secret durable data enters replay-valid Rust records."
    - "Apply is plan-bound, source-preserving, idempotent, collision-safe, receipt-backed, and narrowly reversible."
    - "D-601 through D-609 are executable and covered by offline fixtures."
  prohibitions:
    - "MUST NOT mutate source, import traces/secrets/caches, overwrite targets, or expose a force flag."
---

<objective>Build the complete explicit TypeScript-to-Rust migration transaction.</objective>

<tasks>

<task type="auto">
  <name>Task 1: Inventory and normalize safe TypeScript inputs</name>
  <action>Add strict bounded readers for the `.mini-codex` schema, deterministic source hashing, exclusions/collisions, safe config normalization, session/message/tool/capability normalization, and Rust journal replay validation. Reject symlinks, escapes, malformed/oversized input, secret keys/patterns, private reasoning, traces, and derived files.</action>
  <verify>cargo test -p minimax-cli --test migration --locked inventory</verify>
</task>

<task type="auto">
  <name>Task 2: Add plan-bound apply, receipts, verify, and rollback</name>
  <action>Stage exact target bytes, bind apply to a deterministic plan hash and confirmation, reacquire/recompute inventory, reject drift/collisions, recover interrupted staging, publish with create-new semantics, verify hashes, and write an immutable receipt. Repeated apply must reuse exact targets. Verify and rollback require receipt integrity; rollback removes only unchanged created targets and writes a sidecar rollback receipt.</action>
  <verify>cargo test -p minimax-cli --test migration --locked transaction</verify>
</task>

<task type="auto">
  <name>Task 3: Expose truthful migration commands and adversarial fixtures</name>
  <action>Replace the unavailable migrate route with `inventory`, `dry-run`, `apply`, `verify`, and `rollback` JSON/text commands. Add a complete fixture plus secret/private/derived/collision/drift/crash/idempotency/rollback cases and prove the source hash never changes.</action>
  <verify>cargo clippy -p minimax-cli --all-targets --locked -- -D warnings; cargo test -p minimax-cli --locked migration</verify>
</task>

</tasks>

<success_criteria>MIGR-01 through MIGR-03 have deterministic offline evidence and no real user source is touched.</success_criteria>
