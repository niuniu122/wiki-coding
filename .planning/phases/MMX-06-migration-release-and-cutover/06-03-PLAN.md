---
phase: MMX-06-migration-release-and-cutover
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
autonomous: true
requirements: [REL-04]
files_modified:
  - bin/minimax-codex.cjs
  - package.json
  - crates/compat-harness/src/baseline.rs
  - crates/compat-harness/src/manifest.rs
  - crates/compat-harness/src/main.rs
  - fixtures/compat/baseline-status.v1.json
  - docs/release/cutover.md
  - README.md
must_haves:
  truths:
    - "The default command is a fixed shell-free Rust launcher only after every mandatory gate is green."
    - "The TypeScript entry remains explicitly runnable as minimax-codex-legacy during the support window."
    - "Compatibility evidence, install/upgrade/rollback, source preservation, and support boundaries are truthful and complete."
    - "D-611, D-612, D-617, and D-618 are honored."
  prohibitions:
    - "MUST NOT silently fall back, download, publish, tag, merge, delete TypeScript, or claim unexecuted hosted evidence."
---

<objective>Prove parity, switch the default npm command to Rust, and preserve a clear legacy/rollback path.</objective>

<tasks>

<task type="auto">
  <name>Task 1: Close executable parity and cutover evidence</name>
  <action>Update compatibility validators and fixtures only after migration/release/provider/command evidence exists. Reject pending mandatory items and require the release-gate record before accepting a Rust product entry.</action>
  <verify>cargo run -p minimax-compat-harness --locked -- verify; cargo run -p minimax-compat-harness --locked -- report --format json</verify>
</task>

<task type="auto">
  <name>Task 2: Add the fixed Rust launcher and explicit TypeScript fallback</name>
  <action>Add a minimal CommonJS launcher that maps only supported platform/architecture pairs to fixed packaged binary names and uses spawnSync without shell. Change `minimax-codex` to the launcher and add `minimax-codex-legacy` to `dist/cli.js`; unsupported/missing artifacts fail clearly and never auto-download or silently run TypeScript.</action>
  <verify>npm test; npm run test:launcher</verify>
</task>

<task type="auto">
  <name>Task 3: Run the final cutover and rollback matrix</name>
  <action>Document fresh install, migration, verify, upgrade, rollback, legacy support window, separate embedding, and source retention. Run the full Rust/TypeScript/offline eval/migration/security/license/package/performance/diff matrix and then hosted Windows/Linux CI on the branch. Do not publish, tag, PR, merge, or delete source.</action>
  <verify>npm run verify:release; git diff --check; hosted Windows/Linux CI green</verify>
</task>

</tasks>

<success_criteria>REL-04 is complete: Rust is the evidence-gated default, TypeScript remains explicitly usable, and rollback/source-preservation are proven.</success_criteria>
