---
phase: MMX-01-contract-foundation
plan: "04"
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - crates/compat-harness/src/lib.rs
  - crates/compat-harness/src/manifest.rs
  - crates/compat-harness/src/architecture.rs
  - crates/compat-harness/src/report.rs
  - crates/compat-harness/src/main.rs
  - crates/compat-harness/tests/compat_report.rs
  - fixtures/compat/report.expected.json
  - fixtures/compat/baseline-status.v1.json
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [ARCH-02, ARCH-04, COMP-04]
must_haves:
  truths:
    - "A deterministic report labels each compatibility item matched, pending, or approved_difference with evidence."
    - "Cargo metadata verification blocks core-to-adapter, production-to-harness, cycles, and database dependencies."
    - "Two identical harness runs produce byte-identical normalized JSON."
    - "Windows/Linux CI runs npm and locked Rust checks without live credentials."
  artifacts:
    - path: crates/compat-harness/src/report.rs
      provides: Deterministic compatibility report generation
    - path: crates/compat-harness/src/architecture.rs
      provides: Cargo metadata dependency-policy validation
    - path: fixtures/compat/report.expected.json
      provides: Golden initial phase report
    - path: .github/workflows/ci.yml
      provides: Windows/Linux TypeScript plus Rust verification
  key_links:
    - from: crates/compat-harness/src/report.rs
      to: fixtures/compat/baseline-status.v1.json
      via: status/evidence manifest load
      pattern: "baseline-status\\.v1\\.json"
    - from: crates/compat-harness/src/architecture.rs
      to: Cargo.toml
      via: cargo metadata dependency graph
      pattern: "cargo.*metadata"
    - from: package.json
      to: crates/compat-harness/src/main.rs
      via: verify:rust-contracts script invokes compat harness verify
      pattern: "minimax-compat-harness"
  prohibitions:
    - statement: "MUST NOT place runtime state in the compatibility harness."
      status: resolved
      verification: "harness owns only manifests, reports, and checks; production crates never depend on it and no session or Vault state types are defined there"
    - statement: "MUST NOT add core-to-adapter dependencies."
      status: resolved
      verification: "architecture verifier fails on any forbidden edge"
    - statement: "MUST NOT add SQLite/database dependencies."
      status: resolved
      verification: "architecture verifier rejects normalized database crate denylist"
    - statement: "MUST NOT mark pending behavior matched."
      status: resolved
      verification: "matched requires nonempty evidence whose referenced check exists"
    - statement: "MUST NOT change the npm product entry."
      status: resolved
      verification: "package.json bin snapshot remains dist/cli.js"
---

<objective>
Turn Phase 1 contracts into a deterministic compatibility/architecture gate and add it to Windows/Linux CI (D-02, D-03, D-04).

Purpose: The workspace skeleton becomes enforceable evidence, not an architectural promise that can drift unnoticed.
Output: Compat harness library/binary, architecture verifier, golden report, npm verification scripts, and cross-platform CI steps.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/MMX-01-contract-foundation/01-SPEC.md
@.planning/phases/MMX-01-contract-foundation/01-CONTEXT.md
@.planning/phases/MMX-01-contract-foundation/01-03-SUMMARY.md
@Cargo.toml
@fixtures/compat/baseline-status.v1.json
@package.json
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build manifest loading and deterministic parity reporting</name>
  <files>crates/compat-harness/src/lib.rs, crates/compat-harness/src/manifest.rs, crates/compat-harness/src/report.rs, crates/compat-harness/tests/compat_report.rs, fixtures/compat/report.expected.json, fixtures/compat/baseline-status.v1.json</files>
  <read_first>fixtures/compat/commands.v1.json, fixtures/compat/providers.v1.json, fixtures/compat/baseline-status.v1.json, fixtures/compat/provider-streams/invalid-cases.v1.json, crates/protocol/src/event.rs, crates/provider/src/fixture_protocol.rs, .planning/phases/MMX-01-contract-foundation/01-SPEC.md</read_first>
  <action>Implement strict Serde manifest structs and repository-root discovery without current-directory assumptions. Generate a sorted `CompatReport` with schemaVersion 1, baseline commit `84784f5`, and entries containing `id`, `status`, `evidence`, and optional `difference`. Matched entries require nonempty evidence tied to a test/fixture; pending needs no false success; approved_difference requires rationale. Promote only the protocol/contracts proven by Plans 01-01/03 from pending to matched. Add a golden report test and a second-run byte-equality test.</action>
  <verify>cargo test -p minimax-compat-harness --locked compat_report</verify>
  <acceptance_criteria>Golden report is stable across two runs, every known command/provider item appears once, only implemented protocol contracts are matched, and removing matched evidence makes the test fail.</acceptance_criteria>
  <done>The report communicates honest parity and carries executable evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the workspace dependency and denylist architecture</name>
  <files>crates/compat-harness/src/architecture.rs, crates/compat-harness/tests/compat_report.rs</files>
  <read_first>Cargo.toml, Cargo.lock, crates/core/Cargo.toml, crates/cli/Cargo.toml, crates/compat-harness/Cargo.toml, .planning/SPEC.md</read_first>
  <action>Run `cargo metadata --locked --format-version 1` through `std::process::Command` and validate the local package graph. Enforce: core local deps subset {minimax-protocol}; protocol has no local deps; provider/tools/retrieval/vault/tui point only inward as designed; cli is the only production composition root; no production package depends on compat-harness; no local dependency cycle; normalized package names do not contain sqlite, sqlx, diesel, rusqlite, orm, or libsqlite. Extract pure `validate_architecture` so tests inject a synthetic forbidden core->vault edge and database package and assert exact failures.</action>
  <verify>cargo test -p minimax-compat-harness --locked architecture</verify>
  <acceptance_criteria>Real metadata passes; synthetic core-to-vault, production-to-harness, cycle, and database cases each fail with an actionable package/edge message.</acceptance_criteria>
  <done>ARCH-02 is mechanically enforced rather than reviewed by convention.</done>
</task>

<task type="auto">
  <name>Task 3: Expose one verification command and run it on Windows/Linux CI</name>
  <files>crates/compat-harness/src/main.rs, package.json, .github/workflows/ci.yml</files>
  <read_first>crates/compat-harness/src/lib.rs, crates/compat-harness/src/report.rs, crates/compat-harness/src/architecture.rs, package.json, .github/workflows/ci.yml, rust-toolchain.toml</read_first>
  <action>Add a dependency-free CLI argument parser supporting `verify` and `report --format json`. `verify` runs manifest, provider fixture, architecture, and deterministic report checks and exits nonzero with safe diagnostics. Add npm scripts `check:rust`, `test:rust`, and `verify:rust-contracts` without changing `bin`. Extend the existing Ubuntu/Windows matrix to install the pinned toolchain through existing rustup, cache nothing secret, then run fmt, clippy with `-D warnings`, workspace tests, and compat verify before build/evals. Do not add a third-party Rust setup action.</action>
  <verify>npm run check; npm test; npm run build; npm run verify:rust-contracts</verify>
  <acceptance_criteria>Local aggregate command exits 0; package.json bin is unchanged; CI contains locked Rust checks on both matrix OS values; no step references credentials or real Provider smoke tests.</acceptance_criteria>
  <done>One command and CI make regression visible on both target platforms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository manifests/Cargo metadata -> verifier | Untrusted structured inputs drive pass/fail claims |
| Compat report -> maintainer decision | False matched state could authorize premature cutover |
| CI runner -> dependency acquisition | External packages/toolchain execute in build environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering | compatibility report | high | mitigate | Golden deterministic report and evidence-required matched invariant |
| T-01-11 | Elevation of Privilege | dependency graph | high | mitigate | Local graph allowlist, cycle detection, database denylist |
| T-01-12 | Information Disclosure | CI logs | high | mitigate | Fixture-only tests, safe errors, no credentials or smoke command |
| T-01-SC | Tampering | Cargo installs | high | mitigate | Locked audited dependencies and official rustup toolchain |
</threat_model>

## Artifacts this phase produces

- `CompatManifest` and `CompatReport` structs.
- `load_compat_manifests`, `build_report`, and `validate_report`.
- `validate_architecture` and cargo-metadata adapter.
- `minimax-compat-harness verify` and `report --format json` commands.
- `fixtures/compat/report.expected.json` golden report.
- npm scripts `check:rust`, `test:rust`, and `verify:rust-contracts`.
- Windows/Linux CI Rust verification steps.

<verification>
Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked`, `cargo run -p minimax-compat-harness --locked -- verify` twice, compare JSON reports byte-for-byte, then run `npm run check`, `npm test`, `npm run build`, and `git diff --check`.
</verification>

<success_criteria>
ARCH-02/04 and COMP-04 have deterministic enforcement; all eight Phase 1 requirements have plan coverage; the TypeScript entry remains usable; CI verifies both target OS families without live API use.
</success_criteria>

<output>
Create `.planning/phases/MMX-01-contract-foundation/01-04-SUMMARY.md` when done.
</output>
