---
phase: MMX-01-contract-foundation
plan: "03"
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - crates/protocol/src/lib.rs
  - crates/protocol/src/event.rs
  - crates/protocol/tests/roundtrip.rs
  - crates/core/src/lib.rs
  - crates/core/src/ports.rs
  - crates/core/src/sequence.rs
  - crates/core/tests/sequence.rs
  - crates/provider/src/lib.rs
  - crates/provider/src/fixture_protocol.rs
  - crates/provider/tests/provider_fixtures.rs
autonomous: true
requirements: [ARCH-03, ARCH-04, COMP-02]
must_haves:
  truths:
    - "Provider-neutral commands/events round-trip through a versioned, deny-unknown-fields protocol."
    - "A stream accepts exactly one terminal outcome and rejects premature EOF, duplicate terminal, or data after terminal."
    - "Clock and ID generation are injected so identical fixture replay is byte-identical."
    - "Responses and Chat Completions fixtures normalize to the same visible protocol contract without raw private reasoning."
  artifacts:
    - path: crates/protocol/src/event.rs
      provides: Versioned command/event/terminal/usage/tool-call wire types
    - path: crates/core/src/sequence.rs
      provides: Provider-neutral stream sequence state machine
    - path: crates/core/src/ports.rs
      provides: Deterministic Clock and IdGenerator ports
    - path: crates/provider/src/fixture_protocol.rs
      provides: Responses/Chat Completions fixture normalizer
  key_links:
    - from: crates/provider/src/fixture_protocol.rs
      to: crates/protocol/src/event.rs
      via: provider-specific JSON normalized into StreamEvent
      pattern: "StreamEvent"
    - from: crates/core/src/sequence.rs
      to: crates/protocol/src/event.rs
      via: terminal sequence validation
      pattern: "TerminalOutcome"
  prohibitions:
    - statement: "MUST NOT add core-to-adapter dependencies."
      status: resolved
      verification: "minimax-core imports minimax-protocol only"
    - statement: "MUST NOT write credentials or private raw reasoning."
      status: resolved
      verification: "fixture output excludes reasoning content and secret markers"
    - statement: "MUST NOT use live Provider credentials or network in tests."
      status: resolved
      verification: "tests read only fixtures/compat"
---

<objective>
Implement the smallest typed protocol, deterministic core sequence validator, and fixture-only provider normalizer needed to prove the contracts.

Purpose: Later runtime code can build on tested state transitions instead of importing TypeScript/wire/UI types.
Output: Rust protocol types, core ports/state machine, provider fixture normalization, and negative tests.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/MMX-01-contract-foundation/01-SPEC.md
@.planning/phases/MMX-01-contract-foundation/01-CONTEXT.md
@.planning/phases/MMX-01-contract-foundation/01-01-SUMMARY.md
@.planning/phases/MMX-01-contract-foundation/01-02-SUMMARY.md
@src/protocol.ts
@src/providers/provider-protocol.ts
@fixtures/compat/provider-streams/invalid-cases.v1.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the versioned provider-neutral protocol</name>
  <files>crates/protocol/src/lib.rs, crates/protocol/src/event.rs, crates/protocol/tests/roundtrip.rs</files>
  <read_first>src/protocol.ts, src/providers/provider-protocol.ts, src/agent/model-action.ts, fixtures/compat/commands.v1.json, fixtures/compat/provider-streams/invalid-cases.v1.json, crates/protocol/Cargo.toml</read_first>
  <action>Add schema version constant 1; validated string newtypes `SessionId`, `TurnId`, and `ToolCallId`; `ProviderProtocolKind`; `Usage`; `ToolCallFragment`; `StreamEvent` variants `visible_text_delta`, `tool_call_fragments`, `usage`, and `terminal`; and `TerminalOutcome` variants `completed`, `failed`, `interrupted`, and `stopped`. Use Serde snake_case tagging and `deny_unknown_fields` on v1 records. Add a typed `ProtocolErrorCode` covering malformed_json, missing_tool_call_id, premature_eof, duplicate_terminal, event_after_terminal, and unknown_event. Tests must serialize/deserialize every variant and reject unknown fields/types.</action>
  <verify>cargo test -p minimax-protocol --locked</verify>
  <acceptance_criteria>Every public variant round-trips with schemaVersion 1; invalid/empty IDs are rejected; unknown fields and unknown event types return typed errors; no provider-specific JSON shape appears in minimax-protocol.</acceptance_criteria>
  <done>The protocol crate owns stable provider-neutral wire/domain boundary types.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce one terminal outcome with deterministic ports</name>
  <files>crates/core/src/lib.rs, crates/core/src/ports.rs, crates/core/src/sequence.rs, crates/core/tests/sequence.rs</files>
  <read_first>crates/protocol/src/event.rs, src/runtime/turn-engine.ts, src/utils/id.ts, test/provider-conformance.test.ts, fixtures/compat/provider-streams/invalid-cases.v1.json</read_first>
  <action>Define synchronous Phase-1 `Clock` and `IdGenerator` traits plus fixed test implementations. Implement `StreamSequence` with `accept(StreamEvent)` and `finish_eof()`: nonterminal events are accepted only before terminal; first terminal is stored; second terminal returns duplicate_terminal; later data returns event_after_terminal; EOF without terminal returns premature_eof. Normalized replay records use injected time/ID and deterministic event order. Add table-driven tests for all valid and negative sequences, including two identical replays producing equal serialized output.</action>
  <verify>cargo test -p minimax-core --locked</verify>
  <acceptance_criteria>Valid streams end once; all three illegal terminal orderings map to the exact ProtocolErrorCode; fixed clock/ID replays are byte-identical; core Cargo manifest has no provider/tools/vault/tui/cli dependency.</acceptance_criteria>
  <done>Core owns a deterministic, adapter-free sequence contract.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize both provider fixture protocols into the core contract</name>
  <files>crates/provider/src/lib.rs, crates/provider/src/fixture_protocol.rs, crates/provider/tests/provider_fixtures.rs</files>
  <read_first>src/providers/provider-protocol.ts, test/support/provider-conformance-suite.ts, fixtures/compat/provider-streams/responses.valid.jsonl, fixtures/compat/provider-streams/chat-completions.valid.jsonl, fixtures/compat/provider-streams/invalid-cases.v1.json, crates/protocol/src/event.rs, crates/core/src/sequence.rs</read_first>
  <action>Implement fixture-only parsers `parse_responses_event` and `parse_chat_completions_event` that deserialize the shared raw strings, emit provider-neutral StreamEvent values, preserve tool call IDs, normalize usage, drop raw reasoning text, and map malformed/missing identity cases to ProtocolErrorCode. Add a replay helper that feeds normalized events into StreamSequence and validates EOF. Tests load every JSON/JSONL case from the repository root and compare exact expected events/errors.</action>
  <verify>cargo test -p minimax-provider --locked</verify>
  <acceptance_criteria>Both valid protocol files produce expected visible text/usage/tool IDs/one terminal; every invalid case returns its declared code; serialized output contains neither PRIVATE_REASONING nor SECRET_PROVIDER_DETAIL.</acceptance_criteria>
  <done>Provider wire differences terminate at the provider crate and share one tested core sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider JSON fixture -> provider parser | Malformed/untrusted remote-shaped data crosses into typed protocol |
| Provider parser -> core state machine | Adapter events may attempt illegal terminal ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | stream sequence | high | mitigate | Exactly-one-terminal reducer and negative fixtures |
| T-01-07 | Spoofing | tool call identity | high | mitigate | Validated nonempty ToolCallId and missing-ID failure |
| T-01-08 | Information Disclosure | reasoning/error text | high | mitigate | Normalize categories and assert forbidden markers absent |
| T-01-09 | Denial of Service | oversized/unknown events | medium | mitigate | Phase-1 strict schema and bounded fixture records |
</threat_model>

## Artifacts this phase produces

- `ProtocolErrorCode`, `StreamEvent`, `TerminalOutcome`, `Usage`, `ToolCallFragment`, `SessionId`, `TurnId`, and `ToolCallId`.
- `Clock` and `IdGenerator` ports plus deterministic test implementations.
- `StreamSequence::accept` and `StreamSequence::finish_eof`.
- `parse_responses_event`, `parse_chat_completions_event`, and fixture replay helper.

<verification>
Run `cargo test -p minimax-protocol --locked`, `cargo test -p minimax-core --locked`, `cargo test -p minimax-provider --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, and `git diff --check`.
</verification>

<success_criteria>
ARCH-03, ARCH-04, and COMP-02 are represented by executable Rust types and deterministic positive/negative fixture tests with no adapter leakage into core.
</success_criteria>

<output>
Create `.planning/phases/MMX-01-contract-foundation/01-03-SUMMARY.md` when done.
</output>
