---
phase: MMX-01-contract-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - rust-toolchain.toml
  - Cargo.toml
  - Cargo.lock
  - .gitignore
  - crates/protocol/Cargo.toml
  - crates/protocol/src/lib.rs
  - crates/core/Cargo.toml
  - crates/core/src/lib.rs
  - crates/provider/Cargo.toml
  - crates/provider/src/lib.rs
  - crates/tools/Cargo.toml
  - crates/tools/src/lib.rs
  - crates/retrieval/Cargo.toml
  - crates/retrieval/src/lib.rs
  - crates/vault/Cargo.toml
  - crates/vault/src/lib.rs
  - crates/tui/Cargo.toml
  - crates/tui/src/lib.rs
  - crates/cli/Cargo.toml
  - crates/cli/src/main.rs
  - crates/compat-harness/Cargo.toml
  - crates/compat-harness/src/lib.rs
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "The repository builds as one Rust 1.97.0 edition-2024 workspace beside the unchanged TypeScript product."
    - "All eight production crates and the dev-only compat harness have explicit one-way dependency declarations."
    - "Only audited Serde dependencies enter Phase 1 and the lockfile makes resolution reproducible."
  artifacts:
    - path: rust-toolchain.toml
      provides: Pinned Rust 1.97.0 toolchain with rustfmt and clippy
    - path: Cargo.toml
      provides: Root workspace membership and shared package/dependency policy
    - path: Cargo.lock
      provides: Locked dependency resolution
    - path: crates/cli/src/main.rs
      provides: Non-default placeholder Rust binary target
  key_links:
    - from: Cargo.toml
      to: crates/core/Cargo.toml
      via: workspace member and shared package fields
      pattern: "crates/core"
    - from: crates/core/Cargo.toml
      to: crates/protocol/Cargo.toml
      via: sole inward production dependency
      pattern: "minimax-protocol"
  prohibitions:
    - statement: "MUST NOT change the npm product entry."
      status: resolved
      verification: "package.json is not modified and npm bin stays dist/cli.js"
    - statement: "MUST NOT add core-to-adapter dependencies."
      status: resolved
      verification: "crates/core/Cargo.toml depends only on minimax-protocol and audited general-purpose crates"
    - statement: "MUST NOT add SQLite/database dependencies."
      status: resolved
      verification: "Cargo.lock and cargo metadata contain no sqlite/sqlx/diesel/orm crate"
---

<objective>
Create the pinned root Cargo workspace and one-way crate skeleton (D-01, D-02, D-03).

Purpose: Every later slice needs a compilable architecture boundary before behavior is ported.
Output: Official Rust toolchain, root workspace, lockfile, and minimal crate entry points that do not replace the npm CLI.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/MMX-01-contract-foundation/01-SPEC.md
@.planning/phases/MMX-01-contract-foundation/01-CONTEXT.md
@.planning/phases/MMX-01-contract-foundation/01-RESEARCH.md
@package.json
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and pin the audited Rust toolchain</name>
  <files>rust-toolchain.toml</files>
  <read_first>.planning/phases/MMX-01-contract-foundation/01-RESEARCH.md, .planning/phases/MMX-01-contract-foundation/01-SPEC.md</read_first>
  <action>Preflight `rustup`, `rustc`, Cargo, and an MSVC linker. Because they are absent, acquire only the official x86_64 rustup installer from rust-lang.org/static.rust-lang.org, verify its Authenticode signature or published transport/source, install the minimal 1.97.0 stable toolchain plus `rustfmt` and `clippy`, and avoid changing the default toolchain for unrelated repositories where a pinned file suffices. Add `rust-toolchain.toml` with channel `1.97.0`, profile `minimal`, components `rustfmt` and `clippy`.</action>
  <verify>rustc --version; cargo --version; rustup show active-toolchain</verify>
  <acceptance_criteria>`rustc --version` reports 1.97.0, Cargo is available, rustfmt/clippy components are installed, and `rust-toolchain.toml` contains channel 1.97.0 with minimal profile.</acceptance_criteria>
  <done>The supported compiler/tooling is reproducible and came from the official audited distribution path.</done>
</task>

<task type="auto">
  <name>Task 2: Define the root workspace and dependency policy</name>
  <files>Cargo.toml, Cargo.lock, .gitignore</files>
  <read_first>package.json, .gitignore, .planning/phases/MMX-01-contract-foundation/01-CONTEXT.md, .planning/phases/MMX-01-contract-foundation/01-RESEARCH.md</read_first>
  <action>Create a resolver-3 edition-2024 root workspace with members for `protocol`, `core`, `provider`, `tools`, `retrieval`, `vault`, `tui`, `cli`, and `compat-harness`. Set shared version `0.1.0`, license, rust-version `1.97`, and workspace lints forbidding unsafe code and warning on common correctness issues. Add workspace dependencies exactly `serde = 1.0.228` with derive and `serde_json = 1.0.150`. Generate `Cargo.lock`; add only `/target/` to `.gitignore` if absent. Do not modify package.json or the npm bin.</action>
  <verify>cargo metadata --locked --format-version 1; cargo tree --workspace --locked</verify>
  <acceptance_criteria>Cargo metadata lists nine intended members, resolver is 3, every package uses edition 2024/rust-version 1.97, Cargo.lock exists, and dependency output contains no database crate.</acceptance_criteria>
  <done>The workspace root owns versions/lints and TypeScript remains independently runnable.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold explicit inward-pointing crate entry points</name>
  <files>crates/*/Cargo.toml, crates/*/src/lib.rs, crates/cli/src/main.rs</files>
  <read_first>Cargo.toml, .planning/SPEC.md, docs/superpowers/specs/2026-07-15-rust-vault-rewrite-design.md</read_first>
  <action>Create the nine packages with names fixed in CONTEXT.md. `minimax-protocol` uses Serde; `minimax-core` depends only on protocol; provider/tools/retrieval/vault depend inward only on protocol/core as required; tui depends on protocol/core; cli is the composition root and may depend on all production crates; compat-harness is non-publishable and may depend on all crates plus serde_json. Each library exports a short crate-level boundary document and a `CRATE_ROLE` constant; `minimax-cli` prints an explicit development-only message and is not wired into package.json.</action>
  <verify>cargo fmt --all -- --check; cargo check --workspace --all-targets --locked</verify>
  <acceptance_criteria>All nine packages compile, production crates do not depend on compat-harness, core has no adapter dependency, each crate documents its ownership boundary, and running the npm bin still uses dist/cli.js.</acceptance_criteria>
  <done>A compilable skeleton expresses the target dependency direction without pretending later features exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolchain/package registry -> local build | Downloaded compiler and crates execute during build |
| Root workspace -> existing npm product | New build metadata must not hijack the current user entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | rustup/Cargo installs | high | mitigate | Official endpoints, package legitimacy audit, lockfile, no assumed packages |
| T-01-04 | Elevation of Privilege | crate dependency direction | high | mitigate | Core manifest allowlist and later metadata gate |
| T-01-05 | Denial of Service | toolchain/linker absence | medium | mitigate | Explicit preflight and actionable failure before code work |
</threat_model>

## Artifacts this phase produces

- `rust-toolchain.toml`: pinned channel/profile/components.
- Root `Cargo.toml`: workspace members, package policy, lints, and audited dependencies.
- `Cargo.lock`: reproducible resolved graph.
- Nine Cargo packages and their `CRATE_ROLE` constants.
- Development-only `minimax-cli` binary target.

<verification>
Run Rust version checks, `cargo metadata --locked`, `cargo tree --workspace --locked`, `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --locked`, `npm run check`, and `npm test`.
</verification>

<success_criteria>
ARCH-01 workspace compiles on the pinned toolchain, the manifest structure embodies ARCH-02, audited packages are locked, and the existing TypeScript product still passes.
</success_criteria>

<output>
Create `.planning/phases/MMX-01-contract-foundation/01-02-SUMMARY.md` when done.
</output>
