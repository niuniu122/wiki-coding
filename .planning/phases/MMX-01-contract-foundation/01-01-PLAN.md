---
phase: MMX-01-contract-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/compat/commands.v1.json
  - fixtures/compat/providers.v1.json
  - fixtures/compat/provider-streams/responses.valid.jsonl
  - fixtures/compat/provider-streams/chat-completions.valid.jsonl
  - fixtures/compat/provider-streams/invalid-cases.v1.json
  - fixtures/compat/baseline-status.v1.json
  - test/rust-rewrite-compat-manifest.test.ts
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
must_haves:
  truths:
    - "All locked slash commands and aliases have one machine-readable compatibility entry."
    - "Responses and Chat Completions valid and invalid cases are language-neutral fixtures."
    - "Built-in MiniMax official/Hashsight and custom OpenAI-compatible profile classes are inventoried without secret values."
    - "Unimplemented Rust behavior is explicitly pending, never falsely matched."
  artifacts:
    - path: fixtures/compat/commands.v1.json
      provides: Versioned public command inventory
    - path: fixtures/compat/providers.v1.json
      provides: Versioned provider/profile inventory
    - path: fixtures/compat/provider-streams/invalid-cases.v1.json
      provides: Negative protocol cases and expected error codes
    - path: test/rust-rewrite-compat-manifest.test.ts
      provides: TypeScript-side manifest and uniqueness verification
  key_links:
    - from: test/rust-rewrite-compat-manifest.test.ts
      to: fixtures/compat/commands.v1.json
      via: repository fixture load and locked command assertions
      pattern: "commands\\.v1\\.json"
    - from: fixtures/compat/provider-streams/invalid-cases.v1.json
      to: src/providers/provider-protocol.ts
      via: cases derived from current protocol error behavior
      pattern: "premature_eof|duplicate_terminal|malformed_json"
  prohibitions:
    - statement: "MUST NOT change the npm product entry."
      status: resolved
      verification: "package.json bin remains dist/cli.js and npm tests pass"
    - statement: "MUST NOT use live Provider credentials or network in tests."
      status: resolved
      verification: "all fixtures use provider.test and literal non-secret markers"
    - statement: "MUST NOT mark pending behavior matched."
      status: resolved
      verification: "manifest validator rejects matched entries without evidence"
---

<objective>
Freeze the TypeScript baseline into language-neutral command/provider/protocol fixtures (D-03, D-04).

Purpose: Rust can only claim compatibility against an explicit, executable reference rather than prose or file-by-file imitation.
Output: Versioned compatibility manifests, JSONL protocol cases, initial pending/matched status, and TypeScript validation.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/MMX-01-contract-foundation/01-SPEC.md
@.planning/phases/MMX-01-contract-foundation/01-CONTEXT.md
@src/protocol.ts
@src/ui/App.tsx
@src/providers/provider-protocol.ts
@src/providers/builtin-provider-adapter.ts
@test/support/provider-conformance-suite.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze public command and provider manifests</name>
  <files>fixtures/compat/commands.v1.json, fixtures/compat/providers.v1.json, fixtures/compat/baseline-status.v1.json</files>
  <read_first>src/protocol.ts, src/ui/App.tsx, src/providers/provider-profile.ts, src/providers/model-profile.ts, src/providers/builtin-provider-adapter.ts, .planning/SPEC.md</read_first>
  <action>Create schemaVersion 1 JSON manifests. `commands.v1.json` must contain `/interrupt`, `/new`, `/threads`, `/resume`, `/compact`, `/api`, `/provider`, `/continue`, `/agent`, `/chat`, `/models`, `/model`, `/capabilities`, `/permissions`, `/trace`, `/retry`, `/exit`, and `/quit`, including aliases and whether an argument is none/optional/required. `providers.v1.json` must contain profile classes `minimax_official`, `minimax_hashsight`, and `custom_openai_compatible`, supported protocols `responses` and `chat_completions`, secret binding names only, and the current feature matrix. `baseline-status.v1.json` must use only `matched`, `pending`, or `approved_difference`; mark only fixture-validated TypeScript contracts matched and every Rust product behavior pending. Do not add `workspace_read` as a target Rust permission mode.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>All three JSON files parse; command names are unique; the exact locked command list is present; provider entries contain no API key/token value; every status has evidence or is pending.</acceptance_criteria>
  <done>The baseline inventory is complete, secret-free, versioned, and does not imply unimplemented Rust parity.</done>
</task>

<task type="auto">
  <name>Task 2: Convert provider streams into shared valid and invalid fixtures</name>
  <files>fixtures/compat/provider-streams/responses.valid.jsonl, fixtures/compat/provider-streams/chat-completions.valid.jsonl, fixtures/compat/provider-streams/invalid-cases.v1.json</files>
  <read_first>src/providers/provider-protocol.ts, test/support/provider-conformance-suite.ts, test/fixtures/providers/conformance/responses.json, test/fixtures/providers/conformance/chat_completions.json, fixtures/compat/providers.v1.json</read_first>
  <action>Write JSONL records with fields `case_id`, `protocol`, `raw`, and `expected_events` for visible text, reasoning filtering, usage, tool-call identity, and terminal completion. Add negative cases with expected error codes for malformed JSON, missing call ID, premature EOF, duplicate terminal, and event after terminal. Use only deterministic IDs and `provider.test`; include a `SECRET_PROVIDER_DETAIL` marker solely to prove normalized errors omit it.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>Both protocol fixture files contain visible delta, usage, tool identity, and one terminal outcome; invalid cases include all five required failures; files contain no real endpoint or credential.</acceptance_criteria>
  <done>One fixture set can be read unchanged by TypeScript and Rust.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce the compatibility manifests from the TypeScript suite</name>
  <files>test/rust-rewrite-compat-manifest.test.ts</files>
  <read_first>test/run-tests.ts, test/test-discovery.test.ts, src/protocol.ts, fixtures/compat/commands.v1.json, fixtures/compat/providers.v1.json, fixtures/compat/provider-streams/invalid-cases.v1.json, fixtures/compat/baseline-status.v1.json</read_first>
  <action>Add Node test coverage that loads each repository fixture using URL-relative paths, checks schemaVersion 1, exact command set/alias uniqueness, exact provider class/protocol set, permitted status values, evidence required for `matched`, and absence of common secret field names/values. The test must be auto-discovered by the existing runner and must not call network APIs.</action>
  <verify>npm test</verify>
  <acceptance_criteria>`npm test` discovers `rust-rewrite-compat-manifest.test.ts`; it exits 0 for valid fixtures and its helper rejects a duplicate command, a secret value, and a matched entry without evidence.</acceptance_criteria>
  <done>The existing TypeScript suite guards the cross-language baseline before Rust code consumes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture files -> test/runtime parser | Repository text is untrusted structured input and may contain instruction-like or secret-like strings |
| TypeScript source -> compatibility manifest | Extraction can omit or falsely promote behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | compatibility status | high | mitigate | Require evidence for matched and exact allowed status enum |
| T-01-02 | Information Disclosure | provider fixtures | high | mitigate | Secret-key scan and provider.test-only endpoints |
| T-01-03 | Spoofing | command aliases | medium | mitigate | Exact list and uniqueness assertions |
</threat_model>

## Artifacts this phase produces

- `fixtures/compat/commands.v1.json`: command records with `name`, `aliases`, `argument`, and `outcome`.
- `fixtures/compat/providers.v1.json`: profile class/protocol/feature records.
- `fixtures/compat/provider-streams/*.jsonl`: cross-language stream cases.
- `fixtures/compat/provider-streams/invalid-cases.v1.json`: typed failure expectations.
- `fixtures/compat/baseline-status.v1.json`: compatibility item status and evidence.
- `test/rust-rewrite-compat-manifest.test.ts`: manifest/secret/uniqueness tests.

<verification>
Run `npm run check`, `npm test`, and `git diff --check`. Inspect the status file to confirm no Rust runtime behavior is marked matched before its Rust verifier exists.
</verification>

<success_criteria>
All COMP-01..03 contracts exist as deterministic, secret-free data that both languages can consume, while the TypeScript product entry remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/MMX-01-contract-foundation/01-01-SUMMARY.md` when done.
</output>
