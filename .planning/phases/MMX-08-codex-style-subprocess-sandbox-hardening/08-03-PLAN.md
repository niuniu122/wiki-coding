# Plan 08-03: Truthful Diagnostics, Documentation, and Release Gates

## Objective

Make the new trust boundary understandable and release-gated without regressing Provider, retrieval, Vault/Wiki, or package behavior.

## Tasks

1. Add failing doctor, CLI/TUI permission, and snapshot tests for enforced, missing, unsupported, disabled-by-full-access, empty, and Unicode-path cases.
2. Wire sandbox capability reporting and update permission/help text without implying that approval itself isolates a process.
3. Update README/security/release/install documentation and CI platform setup, including Linux Bubblewrap prerequisites and honest Windows/macOS fail-closed limitations.
4. Run formatting, Clippy, all Rust/TypeScript tests, architecture/parity/security/retrieval/Provider/milestone-flow gates, release packaging, and a final Phase 8 threat audit; update planning evidence only after fresh green results.

## Verification

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test --workspace --locked`
- `npm run verify:release`

## Requirements

SBOX-01, SBOX-05, SBOX-06, SBOX-07, REL-01, REL-03, REL-04
