---
phase: 14-typescript-removal-and-hosted-closure
plan: "02"
type: execute
wave: 14
depends_on:
  - "14-01"
files_modified:
  - fixtures/compat/source-authority.v1.json
  - crates/compat-harness/src/source_authority.rs
  - crates/compat-harness/tests/source_authority.rs
  - .github/workflows/ci.yml
  - package.json
  - scripts/release/product-fingerprint.mjs
  - scripts/release/verify-milestone-flow.mjs
  - crates/compat-harness/src/baseline.rs
  - crates/compat-harness/tests/compat_report.rs
autonomous: true
requirements:
  - RCUT-01
  - RCUT-03
must_haves:
  truths:
    - "A permanent zero-TS source/dependency/no-fallback gate blocks reintroduction outside immutable migration/compatibility data."
    - "CI and release composition are Rust-authoritative and run evaluations, compatibility, migration, package corruption, installed smoke, security, license, and performance gates before evidence."
    - "The final v3 product fingerprint covers every tracked/untracked product input except planning and its own hosted evidence record."
    - "Task 2 defines and tests the v3 fingerprint algorithm provisionally; 14-03 must complete documentation inputs before freezing the final hosted-evidence fingerprint and intake."
  prohibitions:
    - statement: "MUST NOT allow TS/TSX product/test source, compiler/runtime/UI dependencies, a legacy bin, or a fallback path to re-enter the repository/package."
      status: resolved
      verification: "Zero-source, package-schema, launcher, and CI structural tests run before every release build."
    - statement: "MUST NOT forbid static TypeScript-era JSON/JSONL fixture provenance merely because its data names TypeScript."
      status: resolved
      verification: "The gate distinguishes executable extensions/dependencies from exact retained fixture allowlists per D-14-02."
    - statement: "MUST NOT add macOS/ARM or treat local GNU-LLVM evidence as hosted MSVC."
      status: resolved
      verification: "CI/target/fingerprint validation retains only Windows x64 MSVC and Linux x64 GNU hosted identities."
    - statement: "MUST NOT trigger hosted runs, push, publish, or open a PR in this plan."
      status: resolved
      verification: "14-02 prepares only the final local tree; fresh authorization is isolated in 14-03."
  artifacts:
    - path: crates/compat-harness/src/source_authority.rs
      provides: "Permanent zero-TS/source/dependency/no-fallback gate"
      exports: ["validate_source_authority"]
    - path: .github/workflows/ci.yml
      provides: "Final two-host Rust-only verification/release workflow"
    - path: scripts/release/product-fingerprint.mjs
      provides: "v3 final-tree product fingerprint"
      exports: ["computeProductFingerprint"]
  key_links:
    - from: .github/workflows/ci.yml
      to: crates/compat-harness/src/source_authority.rs
      via: "source gate runs before Rust release build/package/evidence steps"
      pattern: "verify:rust-contracts"
    - from: scripts/release/verify-milestone-flow.mjs
      to: scripts/release/product-fingerprint.mjs
      via: "release/install evidence must match the current v3 fingerprint and exact hashes"
      pattern: "productFingerprint"
---

# Phase 14 Plan 02: Rust-Only Gates and Release Flow

## Goal / Objective

<objective>
Converge permanent source gates, CI/release composition, and v3 fingerprinting on the post-deletion Rust-only tree.

Purpose: Make RCUT-01/RCUT-03 durable and prepare one stable product fingerprint for the manual hosted closure.
Output: Zero-TS enforcement, final Rust-only CI/release flow, and the provisional v3 fingerprint contract consumed after 14-03 documentation finalization.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-SPEC.md
@.planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-CONTEXT.md
@fixtures/compat/source-authority.v1.json
@crates/compat-harness/src/source_authority.rs
@.github/workflows/ci.yml
@scripts/release/product-fingerprint.mjs
@scripts/release/verify-milestone-flow.mjs
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make zero TS, thin JS, one state writer, and no fallback permanent CI gates</name>
  <files>fixtures/compat/source-authority.v1.json, crates/compat-harness/src/source_authority.rs, crates/compat-harness/tests/source_authority.rs, .github/workflows/ci.yml, package.json</files>
  <read_first>
    - fixtures/compat/source-authority.v1.json — zero transitional baseline and exact retained JS/data allowlists modified by this task
    - crates/compat-harness/src/source_authority.rs — final validator modified by this task
    - crates/compat-harness/tests/source_authority.rs — reintroduction negative suite modified by this task
    - .github/workflows/ci.yml — final command order/job matrix modified by this task
    - package.json — exact Rust/package command surface modified by this task
    - fixtures/compat/verification/typescript-responsibilities.v1.json — historical deletion evidence retained as data
    - .planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-CONTEXT.md — D-14-02
  </read_first>
  <behavior>
    - Any new TS/TSX source, compiler config, legacy/generated entry, TS/React/Ink dependency, legacy bin/script, JS domain behavior, fallback, or second writable state root fails.
    - Any non-empty `transitionalTypeScript` or `transitionalLegacyTestFixtures` class fails; the three removed `diag-*.js` fixtures cannot re-enter another JS authority class.
    - Exact CJS/MJS launcher/release orchestration and static JSON/JSONL fixture provenance remain allowed.
    - CI runs source/package gates before build and Rust evaluations/compatibility/migration before packaging.
  </behavior>
  <action>
Convert the shrinking Phase 10 baselines into permanent zero-source rules for both `transitionalTypeScript` and `transitionalLegacyTestFixtures`. Validate tracked and present files, package/lock metadata, JS allowlist content/imports/process targets, supported entries, and state roots without flagging allowed fixture text. Add mutation tests for each forbidden reintroduction, including any of the removed `diag-*.js` fixtures or attempts to reclassify them as executable/allowed JavaScript, and for accidental deletion of retained migration/support/evaluation fixtures. Rewrite CI/package aggregate commands to remove TS steps and make Rust format, Clippy, workspace/doc tests, coverage, Provider/retrieval evaluation, compatibility, migration support, package corruption, build, installed smoke, security/license/performance, and milestone flow mandatory in order. Preserve read-only permissions, Linux sandbox canary, and exactly the Windows/Linux x64 hosted matrix.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test source_authority --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - Every forbidden source/dependency/bin/fallback/state mutation is rejected by a named Rust test.
    - Static TypeScript-era migration data remains accepted only at exact manifest paths/hashes.
    - CI contains no TS build/test/eval step, no write permission, and no unsupported target.
  </acceptance_criteria>
  <done>The repository cannot silently regain duplicate executable, writable, or verification authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind final release composition to a v3 product fingerprint and exact target identity</name>
  <files>scripts/release/product-fingerprint.mjs, scripts/release/verify-milestone-flow.mjs, crates/compat-harness/src/baseline.rs, crates/compat-harness/tests/compat_report.rs, .github/workflows/ci.yml</files>
  <read_first>
    - crates/compat-harness/tests/compat_report.rs — dedicated passing stale-evidence rejection test added by this task
    - scripts/release/product-fingerprint.mjs — fingerprint domain/version/exclusion target modified by this task
    - scripts/release/verify-milestone-flow.mjs — final local/hosted composition validator modified by this task
    - crates/compat-harness/src/baseline.rs — Rust fingerprint/hosted evidence parity implementation modified by this task
    - .github/workflows/ci.yml — candidate evidence upload inputs modified by this task
    - fixtures/compat/release/targets.v1.json — exact hosted/development target source
    - fixtures/compat/release/hosted-gates.v1.json — stale record intentionally not refreshed until 14-03
    - .planning/SPEC.md — EDGE-07 and stale-evidence rejection contract
  </read_first>
  <behavior>
    - Rust and MJS compute the same versioned v3 fingerprint/file count over the same canonical product inputs.
    - Planning files and the hosted evidence record itself are excluded; product source/config/fixtures/launcher/release scripts/docs are included.
    - Candidate flow rejects stale hosted evidence only at the freshness comparison; strict flow requires exact final fingerprint, file count, target, artifact hashes, and successful jobs.
    - Local GNU-LLVM remains development-only and cannot satisfy Windows MSVC.
    - A dedicated negative test supplies stale freshness/fingerprint evidence, asserts the exact rejection result, and itself exits zero; positive local candidate gates run separately.
  </behavior>
  <action>
Version the fingerprint domain for v3 and keep the Rust/MJS canonical path, byte/hash, tracked/untracked, and exclusion algorithms identical. Update milestone-flow and CI evidence fields to require native/npm installed identity, archive/npm/binary hashes, Rust evaluator/compatibility/migration/package/security/license/performance results, and exact target facts. Retain candidate mode solely for refreshing stale evidence and make strict mode reject the current stale record. In `crates/compat-harness/tests/compat_report.rs`, add `stale_hosted_evidence_is_rejected_for_freshness_or_fingerprint`: construct otherwise-valid hosted evidence with an old freshness timestamp and/or product fingerprint, call the validator directly, assert the stable stale-freshness/fingerprint error, and return success so the negative test exits zero. Add mutation tests showing any final product input changes the fingerprint while planning/evidence-record edits do not, and that GNU-LLVM development evidence cannot satisfy hosted MSVC. Treat the fingerprint printed here as provisional because Task 3 docs are included product inputs.
  </action>
  <verify>
    <automated>node scripts/release/product-fingerprint.mjs &amp;&amp; cargo test -p minimax-compat-harness --test compat_report --locked stale_hosted_evidence_is_rejected_for_freshness_or_fingerprint -- --exact &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
    <source_assertions>The dedicated stale-evidence negative test passes by asserting the rejection; the separate local candidate command is positive and must also exit zero.</source_assertions>
  </verify>
  <acceptance_criteria>
    - Rust and MJS fingerprint/file count match exactly on the final local tree.
    - Candidate release evidence carries every required gate and exact target/hash field.
    - Stale v2/Phase 9 evidence and local GNU-LLVM cannot pass strict closure.
  </acceptance_criteria>
  <done>The v3 fingerprint algorithm and stale-evidence rejection are proven; the provisional value is ready for the 14-03 post-doc final freeze.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test --workspace --locked` plus doc tests
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- `npm run eval:provider && npm run eval:retrieval && npm run test:package`
- `npm run build:rust:release && npm run package:rust && npm run verify:rust-release && npm run verify:milestone-flow`
- Run the passing stale-evidence rejection test and keep positive candidate gates separate; final fingerprint freeze waits for 14-03 Task 1 docs.

## Success Criteria

- RCUT-01/RCUT-03 and D-14-02 are durable after deletion.
- EDGE-06/EDGE-07 and final source/package/fingerprint prohibitions are gated.
- The product fingerprint is stable; no hosted action, push, PR, or publication occurs yet.

## Artifacts this phase produces

- None new.
- `source-authority.v1.json`, `source_authority.rs`, and its test: modified permanent zero-TS/dependency/fallback/state gate.
- `.github/workflows/ci.yml` and `package.json`: modified Rust-only ordered verification/release commands.
- `product-fingerprint.mjs`, `verify-milestone-flow.mjs`, and Rust release validator: modified v3 fingerprint/evidence schema.
- `crates/compat-harness/tests/compat_report.rs`: modified with a dedicated stale freshness/fingerprint rejection test that exits zero on the expected rejection.
