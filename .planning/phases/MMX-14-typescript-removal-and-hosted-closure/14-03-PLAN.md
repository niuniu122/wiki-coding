---
phase: 14-typescript-removal-and-hosted-closure
plan: "03"
type: execute
wave: 15
depends_on:
  - "14-02"
files_modified:
  - fixtures/compat/release/hosted-gates.v1.json
  - README.md
  - docs/release/install-upgrade-rollback.md
  - docs/release/cutover.md
  - target/release-evidence/final-v3/
  - target/release-evidence/hosted-candidate/
  - target/release-evidence/hosted-strict/
autonomous: false
requirements:
  - RCUT-02
  - RCUT-03
must_haves:
  truths:
    - "Hosted Windows x64 MSVC and Linux x64 GNU candidate and strict jobs pass against the exact final v3 fingerprint and artifact hashes per D-14-03."
    - "Local Windows GNU-LLVM evidence is recorded only as development evidence and never substitutes for hosted MSVC per D-14-04."
    - "Push, workflow dispatch, publication, and PR actions remain blocked until the user gives fresh, explicit authorization per D-14-05."
    - "The final evidence record is accepted only after both native host jobs prove Rust tests/evaluations, compatibility, migration, package/install, checksum, upgrade/rollback, security, license, and performance gates."
    - "Hosted closure follows one auditable order: candidate run, candidate artifact validation, interim candidate-only evidence record commit/push, strict run against that commit, then final combined candidate+strict evidence record."
    - "Rust-only user/maintainer docs are completed locally first; only then is the final fingerprint frozen and the authorization intake generated, before any external action."
  prohibitions:
    - statement: "MUST NOT close v3 using stale v2/Phase 9 evidence, a pre-deletion fingerprint, or local development evidence."
      status: resolved
      verification: "Strict evidence validation requires the final head/tree SHA, v3 fingerprint/file count, exact host triples, job conclusions, and artifact hashes."
    - statement: "MUST NOT treat local GNU-LLVM as Windows MSVC."
      status: resolved
      verification: "The hosted Windows job must report `x86_64-pc-windows-msvc`; development-tier evidence is rejected."
    - statement: "MUST NOT push, dispatch hosted CI, publish, tag, or open a PR before a fresh human authorization checkpoint."
      status: resolved
      verification: "Task 2 is a blocking manual checkpoint; no external mutation is permitted before the user's explicit response."
    - statement: "MUST NOT publish npm or migrate real user data as part of milestone closure."
      status: resolved
      verification: "Authorized scope is limited to hosted evidence collection and the evidence record/strict verification."
  artifacts:
    - path: target/release-evidence/final-v3/
      provides: "Independent clean final Cargo root, binary, artifacts, evidence, fingerprint, and authorization intake; not tracked"
    - path: README.md
      provides: "Rust-only architecture/install/migration contract completed before evidence intake"
    - path: target/release-evidence/hosted-candidate/
      provides: "Downloaded manual candidate Windows/Linux job artifacts; not tracked"
    - path: target/release-evidence/hosted-strict/
      provides: "Downloaded strict Windows/Linux run results/artifacts; not tracked"
    - path: fixtures/compat/release/hosted-gates.v1.json
      provides: "Task 2 interim candidate-only record, then Task 3 final combined candidate+strict run/job/target/hash evidence bound to the v3 tree"
  key_links:
    - from: fixtures/compat/release/hosted-gates.v1.json
      to: scripts/release/product-fingerprint.mjs
      via: "recorded fingerprint/file count must equal the final local tree"
      pattern: "productFingerprint"
    - from: fixtures/compat/release/hosted-gates.v1.json
      to: .github/workflows/ci.yml
      via: "candidate and strict run/job IDs prove exact Windows MSVC/Linux GNU workflow results"
      pattern: "windows-x86_64-msvc|linux-x86_64-gnu"
    - from: docs/release/cutover.md
      to: fixtures/compat/migration/typescript-v1/support-window.v1.json
      via: "documents the machine-checkable two-subsequent-public-release retention rule before fingerprint freeze"
      pattern: "two"
---

# Phase 14 Plan 03: Manual Hosted Evidence and v3 Closure

## Goal / Objective

<objective>
Collect fresh hosted Windows MSVC/Linux GNU evidence for the final post-deletion tree and close v3 only after an explicit human authorization checkpoint and strict evidence validation.

Purpose: Complete RCUT-02/RCUT-03 and D-14-03/D-14-04/D-14-05 without fabricating platform proof or broadening authorization.
Output: Final Rust-only docs, frozen post-doc fingerprint/intake, hosted evidence record, and strict local validation of the exact hosted artifacts/fingerprint.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-SPEC.md
@.planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-CONTEXT.md
@.planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-02-SUMMARY.md
@.github/workflows/ci.yml
@scripts/release/product-fingerprint.mjs
@scripts/release/verify-rust-release.mjs
@scripts/release/verify-milestone-flow.mjs
@fixtures/compat/release/targets.v1.json
@fixtures/compat/release/hosted-gates.v1.json
</context>

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Finalize Rust-only docs, freeze the fingerprint, and prepare evidence intake locally</name>
  <files>README.md, docs/release/install-upgrade-rollback.md, docs/release/cutover.md, target/release-evidence/final-v3/</files>
  <read_first>
    - README.md — architecture/install/development sections modified before fingerprint freeze
    - docs/release/install-upgrade-rollback.md — native/npm/install/upgrade/rollback target modified before fingerprint freeze
    - docs/release/cutover.md — Rust-only cutover/evidence/support target modified before fingerprint freeze
    - package.json — current commands and npm surface
    - bin/minimax-codex.cjs — exact supported targets/failure wording
    - fixtures/compat/migration/typescript-v1/support-window.v1.json — two-release retention source
    - docs/release/subprocess-sandbox.md — unchanged platform sandbox limitations to link accurately
    - .planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-02-SUMMARY.md — final local gate and documentation evidence
    - .github/workflows/ci.yml — exact candidate/strict hosted job contract
    - scripts/release/product-fingerprint.mjs — final v3 fingerprint source
    - scripts/release/verify-rust-release.mjs — release evidence schema
    - fixtures/compat/release/targets.v1.json — exact host/tier facts
    - fixtures/compat/release/hosted-gates.v1.json — stale record to replace only after hosted proof
    - target/release-evidence/final-v3/ — independent clean final Cargo/artifact/evidence/fingerprint/intake root created by this task
  </read_first>
  <action>
First rewrite README and both release docs to describe Rust as sole product/test/evaluation authority; document global npm, npx, and native archive installation; list only Windows x64 MSVC and Linux x64 GNU as supported release targets; preserve actionable failure, migration inventory/dry-run/apply/verify/narrow rollback, source immutability, truthful sandbox limits, and the two-subsequent-public-release fixture support window; remove legacy command/TS/dual-runtime/90-day/stale-hosted claims. Verify docs against package/launcher/target/support fixtures. Only after the last tracked documentation edit, resolve and validate the independent generated root `target/release-evidence/final-v3/`, remove/recreate only that root, create its `artifacts/` and `evidence/` children, and set `CARGO_TARGET_DIR=target/release-evidence/final-v3/cargo`. Before any build or milestone check, compute Rust/MJS parity and write the final product fingerprint/file count to `target/release-evidence/final-v3/fingerprint.json`; no tracked product edit is permitted afterward. Then run the fresh local chain from that root: format/Clippy/workspace and doc tests, Provider/retrieval/package tests, `build:rust:release`, resolve the exact release binary under the final Cargo root, `package:rust` with explicit `--binary`, `--output`, and `--fingerprint-file`, `verify:rust-release` with explicit binary/artifact/evidence paths, positive candidate compatibility, and finally `verify:milestone-flow` with the same explicit artifact/evidence/fingerprint paths. Only after every gate succeeds, generate `target/release-evidence/final-v3/intake.json` with current branch/head/tree SHA, clean/dirty diff summary, frozen fingerprint/file count, exact binary/archive/npm hashes, final-root paths, local environment/target tier, gate results, and required hosted fields. Mark GNU-LLVM `development_only`; do not commit, push, dispatch, publish, tag, open a PR, or edit hosted-gates in this task.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command '$p="target/release-evidence/final-v3"; if (Test-Path -LiteralPath $p) { Remove-Item -LiteralPath $p -Recurse -Force }; New-Item -ItemType Directory -Force -Path "$p/artifacts","$p/evidence" | Out-Null; $env:CARGO_TARGET_DIR="$p/cargo"; node scripts/release/product-fingerprint.mjs &gt; "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo fmt --all -- --check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo clippy --workspace --all-targets --locked -- -D warnings; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo test --workspace --locked; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run eval:provider; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run eval:retrieval; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run test:package; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run build:rust:release; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli.exe"; if (-not (Test-Path -LiteralPath $b)) { $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli" }; npm run package:rust -- --binary $b --output "$p/artifacts" --fingerprint-file "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:rust-release -- --binary $b --artifacts "$p/artifacts" --evidence-dir "$p/evidence"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo run -p minimax-compat-harness --locked -- verify-candidate; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:milestone-flow -- --artifacts "$p/artifacts" --evidence-dir "$p/evidence" --fingerprint-file "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $fingerprint=Get-Content -Raw -LiteralPath "$p/fingerprint.json"; $artifacts=@(Get-ChildItem -LiteralPath "$p/artifacts" -File -Recurse | Sort-Object FullName | ForEach-Object { [ordered]@{ path=$_.FullName; sha256=(Get-FileHash -Algorithm SHA256 -LiteralPath $_.FullName).Hash.ToLowerInvariant(); size=$_.Length } }); $evidence=@(Get-ChildItem -LiteralPath "$p/evidence" -File -Recurse | Sort-Object FullName | ForEach-Object { [ordered]@{ path=$_.FullName; sha256=(Get-FileHash -Algorithm SHA256 -LiteralPath $_.FullName).Hash.ToLowerInvariant(); size=$_.Length } }); $intake=[ordered]@{ schemaVersion=1; generatedAt=(Get-Date).ToUniversalTime().ToString("o"); branch=(git branch --show-current); headSha=(git rev-parse HEAD); treeSha=(git rev-parse "HEAD^{tree}"); dirtyDiff=@(git status --porcelain); fingerprint=$fingerprint; cargoTargetDir=$env:CARGO_TARGET_DIR; binary=[ordered]@{ path=$b; sha256=(Get-FileHash -Algorithm SHA256 -LiteralPath $b).Hash.ToLowerInvariant(); size=(Get-Item -LiteralPath $b).Length }; artifacts=$artifacts; evidence=$evidence; gates=[ordered]@{ format="passed"; clippy="passed"; workspaceTests="passed"; providerEval="passed"; retrievalEval="passed"; packageTests="passed"; releaseBuild="passed"; package="passed"; releaseVerify="passed"; candidateVerify="passed"; milestoneFlow="passed" }; requiredHostedTargets=@("x86_64-pc-windows-msvc","x86_64-unknown-linux-gnu") }; $intake | ConvertTo-Json -Depth 12 | Set-Content -LiteralPath "$p/intake.json" -Encoding utf8; if (-not (Test-Path -LiteralPath "$p/intake.json") -or (Get-Item -LiteralPath "$p/intake.json").Length -eq 0) { throw "Failed to generate final intake after milestone verification" }; Get-Content -Raw -LiteralPath "$p/intake.json" | ConvertFrom-Json | Out-Null'</automated>
    <source_assertions>README and both release docs match package/target/support/launcher facts; fingerprint precedes fresh build/package/milestone, and intake is generated last from the same final-root binary/artifacts/evidence/hashes.</source_assertions>
  </verify>
  <acceptance_criteria>
    - Final local tree and candidate artifact identities are stable and fully recorded.
    - Docs cover every RCUT-03 topic without legacy/dual-runtime claims and are included in the final frozen fingerprint.
    - Intake lists both required hosted targets and every required job/evidence field.
    - After documentation and final fingerprint freeze, no tracked product edit occurs; Task 1 performs no external mutation.
  </acceptance_criteria>
  <done>The user can review one exact final diff/fingerprint/gate package before granting any hosted authority.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Obtain fresh authorization and genuine hosted Windows/Linux evidence</name>
  <files>target/release-evidence/final-v3/intake.json, target/release-evidence/hosted-candidate/, fixtures/compat/release/hosted-gates.v1.json, target/release-evidence/hosted-strict/</files>
  <read_first>
    - target/release-evidence/final-v3/intake.json — exact final tree, hashes, local results, and requested scope
    - .planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-CONTEXT.md — D-14-03/D-14-04/D-14-05
    - .github/workflows/ci.yml — manual candidate versus ordinary strict run behavior
    - fixtures/compat/release/targets.v1.json — Windows MSVC/Linux GNU identity requirements
    - fixtures/compat/release/hosted-gates.v1.json — Task 2 writes only the validated interim candidate-only record before the strict run
  </read_first>
  <action>
STOP and show the user the complete final diff summary, fingerprint/file count, local gate results, candidate artifact hashes, remote/branch proposal, and requested external actions. Ask for fresh explicit authorization limited to: creating the necessary evidence commit/branch, pushing it to the user-approved remote, manually dispatching one hosted candidate workflow, downloading and validating its Windows/Linux artifacts, writing and committing an interim candidate-only `hosted-gates.v1.json`, pushing that exact interim record, allowing the ordinary strict workflow to run against it, and downloading its strict artifacts. Offer the alternative that the user performs both hosted runs and supplies artifacts/URLs. Do not infer approval from prior conversation, and do not include npm publication, tagging, PR creation, merge, or real-data migration. If authorization is withheld or narrower, stop and report the exact remaining evidence gap. After approval, execute the order exactly: (1) run candidate; (2) validate successful `x86_64-pc-windows-msvc` and `x86_64-unknown-linux-gnu` candidate artifacts against the frozen fingerprint/hashes; (3) update `hosted-gates.v1.json` with candidate evidence only and explicit `strictStatus: pending`; (4) commit and push that exact interim record; (5) launch/observe the ordinary strict run against the interim commit; (6) download strict artifacts. Local GNU-LLVM is never acceptable, and strict may not start before the interim record commit is remotely visible.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); for (const d of ['target/release-evidence/hosted-candidate','target/release-evidence/hosted-strict']) { if (!fs.existsSync(d)) process.exit(1); } const r=JSON.parse(fs.readFileSync('fixtures/compat/release/hosted-gates.v1.json','utf8')); if (!r.candidate || r.strictStatus !== 'pending') process.exit(1);"</automated>
    <human-check>User explicitly authorizes the stated hosted scope or provides genuine candidate/strict run URLs and artifacts for both exact targets.</human-check>
  </verify>
  <acceptance_criteria>
    - Fresh authorization is recorded before any push/dispatch/external mutation.
    - Candidate evidence comes from exact hosted Windows MSVC and Linux GNU jobs against the final fingerprint.
    - Strict evidence is collected after the evidence record update and proves the committed closure tree.
    - The interim candidate-only record is committed/pushed before strict starts; Task 2 does not write final strict fields.
    - No publication, tag, PR, merge, or scope expansion occurs.
  </acceptance_criteria>
  <done>Genuine target-specific candidate and strict artifacts are available under an explicit, bounded human authorization.</done>
</task>

<task type="auto">
  <name>Task 3: Validate hosted artifacts, update the evidence record, and pass strict closure</name>
  <files>fixtures/compat/release/hosted-gates.v1.json</files>
  <read_first>
    - target/release-evidence/final-v3/intake.json — final local fingerprint/hash baseline
    - target/release-evidence/final-v3/fingerprint.json — frozen post-doc fingerprint reused without recomputation
    - target/release-evidence/final-v3/cargo/ — exact Cargo root containing the packaged release binary
    - target/release-evidence/final-v3/artifacts/ — exact local package artifacts reused by strict verification
    - target/release-evidence/final-v3/evidence/ — exact local release/milestone evidence reused by strict verification
    - target/release-evidence/hosted-candidate/ — authorized candidate Windows/Linux artifacts
    - target/release-evidence/hosted-strict/ — authorized strict Windows/Linux results/artifacts
    - fixtures/compat/release/hosted-gates.v1.json — sole tracked evidence record modified by this task
    - fixtures/compat/release/targets.v1.json — exact supported targets/tier validation
    - scripts/release/verify-rust-release.mjs — per-job evidence schema and hash checks
    - scripts/release/verify-milestone-flow.mjs — final installed-flow composition gate
    - .github/workflows/ci.yml — run/job semantics and candidate/strict distinction
  </read_first>
  <action>
After Task 2 authorization/evidence only, revalidate the candidate section already owned by Task 2 and validate the downloaded strict artifact JSON schemas, run/job URLs and conclusions, head/tree SHA, v3 fingerprint/file count, rustc host triples, archive/npm/binary hashes, installed identities, offline/provider/credential/download counters, security/license/performance results, and Linux sandbox canary. Reject stale, mixed-run, development-tier, wrong-target, missing, or hash-mismatched evidence. Upgrade the interim `hosted-gates.v1.json` to the final combined candidate+strict record by adding only the validated strict run identities/per-target evidence and changing `strictStatus` from `pending` to `passed`; Task 3 owns this final local record change. For strict local closure, reuse without cleaning/rebuilding/repackaging the exact `target/release-evidence/final-v3/` root frozen by Task 1: set the same `CARGO_TARGET_DIR`, resolve the same release binary, and pass the same explicit binary/artifact/evidence paths to release verification plus `--artifacts`, `--evidence-dir`, and required `--fingerprint-file target/release-evidence/final-v3/fingerprint.json` to milestone verification. Also require the intake hashes and hosted record fingerprint to match that file. If any path/hash/fingerprint differs or strict verification fails, do not close; return to the checkpoint with the new evidence gap. Never substitute local GNU-LLVM for MSVC.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command '$p="target/release-evidence/final-v3"; $env:CARGO_TARGET_DIR="$p/cargo"; $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli.exe"; if (-not (Test-Path -LiteralPath $b)) { $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli" }; foreach ($f in @($b,"$p/fingerprint.json","$p/intake.json","$p/artifacts","$p/evidence")) { if (-not (Test-Path -LiteralPath $f)) { throw "Missing frozen final-root input: $f" } }; cargo test --workspace --locked; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo run -p minimax-compat-harness --locked -- verify; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:rust-release -- --binary $b --artifacts "$p/artifacts" --evidence-dir "$p/evidence"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:milestone-flow -- --artifacts "$p/artifacts" --evidence-dir "$p/evidence" --fingerprint-file "$p/fingerprint.json"; exit $LASTEXITCODE'</automated>
    <source_assertions>`hosted-gates.v1.json` names successful fresh candidate and strict runs, exact Windows MSVC/Linux GNU jobs, the final v3 fingerprint/file count, and exact artifact hashes.</source_assertions>
  </verify>
  <acceptance_criteria>
    - Both exact hosted targets pass every RCUT-02 gate against one final fingerprint.
    - Strict post-record workflow succeeds; stale/pre-deletion/development-only evidence is rejected.
    - Strict closure reuses the exact Task 1 final root, binary, artifacts, evidence, intake, and fingerprint file with no default target path or repackaging.
    - User/maintainer documentation remains accurate and no unsupported publication/PR action occurred.
  </acceptance_criteria>
  <done>v3.0 has genuine hosted target evidence and all strict local/release gates accept the final Rust-only tree.</done>
</task>

</tasks>

## Verification

- Before checkpoint: complete docs, clean `target/release-evidence/final-v3/`, freeze `fingerprint.json` before fresh release build, then package/release/milestone checks with explicit same-root paths, and generate `intake.json` last.
- At checkpoint: explicit fresh user authorization, candidate run/artifact validation, interim candidate-only record commit/push, then genuine strict Windows MSVC and Linux GNU run artifacts in that order.
- After checkpoint: Task 3 upgrades the interim record to the final combined candidate+strict record, validates exact run/job/target/SHA/fingerprint/hash fields, then reuses the frozen final root/binary/artifacts/evidence/intake and passes its required `--fingerprint-file` to strict milestone verification.
- Do not accept a local GNU-LLVM build, stale hosted record, or pre-deletion fingerprint as equivalent evidence.

## Success Criteria

- RCUT-02/RCUT-03 and D-14-03/D-14-04/D-14-05 are satisfied.
- Hosted Windows x64 MSVC and Linux x64 GNU both pass the final complete gate matrix.
- Fresh authorization precedes every permitted push/dispatch; no npm publication, tag, PR, merge, or real user migration occurs.

## Artifacts this phase produces

- `target/release-evidence/final-v3/`: new generated, untracked clean final Cargo/binary/artifact/evidence root containing `fingerprint.json` then `intake.json`; any later tracked product edit invalidates both.
- `README.md`, `docs/release/install-upgrade-rollback.md`, and `docs/release/cutover.md`: modified Rust-only user/maintainer contract completed before intake and authorization.
- `target/release-evidence/hosted-candidate/` and `hosted-strict/`: new downloaded, untracked genuine hosted artifacts.
- `fixtures/compat/release/hosted-gates.v1.json`: Task 2 modifies it to an interim candidate-only/pending-strict record before the strict run; Task 3 modifies that record to the final combined candidate+strict run, job, target, fingerprint, file-count, artifact-hash, environment, package, performance, license, security, and offline counters.
- None new product code, command, platform, publication, PR, tag, or user-data migration.
