---
phase: 14-typescript-removal-and-hosted-closure
plan: "01"
type: execute
wave: 13
depends_on:
  - "13-03"
files_modified:
  - src/
  - test/
  - dist/
  - tsconfig.json
  - package.json
  - package-lock.json
  - fixtures/compat/source-authority.v1.json
  - target/phase14-delete-preflight/
  - target/phase14-post-delete/
autonomous: true
requirements:
  - RCUT-01
must_haves:
  truths:
    - "TypeScript/TSX product and test source is deleted only after the Phase 10 authority, Phase 11 Rust evaluation, Phase 12 compatibility/migration, and Phase 13 installed-package gates pass."
    - "The final package metadata contains no TypeScript compiler/runtime, React/Ink runtime, legacy build/test/eval command, legacy bin, or generated legacy path."
    - "Static compatibility and TypeScript-era migration data under `fixtures/compat/` remains intact per D-14-02."
    - "The Rust CLI and extracted npm-installed command remain usable immediately after deletion."
    - "Deletion preflight is rebuilt from a clean task-specific Cargo target and artifact/evidence directory, and every artifact is bound to the current product fingerprint and binary/package hashes."
  prohibitions:
    - statement: "MUST NOT delete TypeScript source/tests before every replacement Rust/package gate passes."
      status: resolved
      verification: "Task 1 is a fail-closed cutover preflight; Task 2 cannot start on any failed or missing predecessor evidence."
    - statement: "MUST NOT delete migration fixtures, fixture manifests, support-window metadata, or immutable JSON/JSONL compatibility evidence."
      status: resolved
      verification: "Retained-file assertions fingerprint `fixtures/compat/migration/typescript-v1/` and all Phase 11/12 evaluation/contract fixtures before and after cleanup."
    - statement: "MUST NOT recreate dormant or undocumented TypeScript-only behavior."
      status: resolved
      verification: "Deletion eligibility comes from the reviewed responsibility matrix and current Rust/public contract per D-14-01."
    - statement: "MUST NOT publish, push, open a PR, call a real Provider, or download model weights."
      status: resolved
      verification: "This plan performs only local deletion and offline candidate verification; hosted actions are reserved for 14-03."
  artifacts:
    - path: fixtures/compat/source-authority.v1.json
      provides: "Final zero-transitional-TS and zero-transitional-legacy-test-fixture authority manifest with retained static-data allowlist"
    - path: target/phase14-delete-preflight/
      provides: "Generated clean Cargo build, fingerprint, package artifacts, installed-smoke evidence, and deletion/retention hash snapshot"
    - path: package.json
      provides: "Rust/release-only npm distribution metadata after dependency/script cleanup"
    - path: src/
      provides: "None new — replaced TypeScript product tree is deleted"
    - path: test/
      provides: "None new — replaced TypeScript test tree is deleted after responsibility/fixture migration"
  key_links:
    - from: fixtures/compat/verification/typescript-responsibilities.v1.json
      to: src/|test/
      via: "every deleted responsibility has Rust/package evidence or an explicit retirement disposition"
      pattern: "disposition"
    - from: fixtures/compat/source-authority.v1.json
      to: crates/compat-harness/src/source_authority.rs
      via: "the shrinking transitional baseline becomes empty and retained fixture roots stay allowed"
      pattern: "transitionalTypeScript|transitionalLegacyTestFixtures"
---

# Phase 14 Plan 01: Verified TypeScript Removal

## Goal / Objective

<objective>
Delete the replaced TypeScript product, tests, generated output, compiler configuration, and residual dependencies only after all Rust and package replacements are proven.

Purpose: Implement RCUT-01 and D-14-01/D-14-02 without weakening migration evidence or leaving the product unusable between plans.
Output: A zero-TS product/test tree, pruned package metadata, and a zero-transitional-source authority manifest.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-SPEC.md
@.planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-CONTEXT.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-03-SUMMARY.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-03-SUMMARY.md
@.planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-02-SUMMARY.md
@.planning/phases/MMX-13-thin-npm-and-native-release/13-03-SUMMARY.md
@fixtures/compat/verification/typescript-responsibilities.v1.json
@fixtures/compat/source-authority.v1.json
@package.json
@tsconfig.json
</context>

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Fail closed unless every deletion target has proven replacement evidence</name>
  <files>target/phase14-delete-preflight/</files>
  <read_first>
    - .planning/phases/MMX-10-rust-authority-and-source-boundaries/10-03-SUMMARY.md — sole execution/state authority evidence
    - .planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-03-SUMMARY.md — coverage/Provider/retrieval Rust authority evidence
    - .planning/phases/MMX-12-fixture-compatibility-and-rust-migration/12-02-SUMMARY.md — fixture compatibility and migration retention evidence
    - .planning/phases/MMX-13-thin-npm-and-native-release/13-03-SUMMARY.md — thin package/corruption/installed smoke evidence
    - fixtures/compat/verification/typescript-responsibilities.v1.json — exact disposition for every TS test/eval responsibility
    - fixtures/compat/source-authority.v1.json — hashed transitional deletion inventory modified in Task 2
    - package.json and package-lock.json — residual script/dependency targets modified in Task 2
    - src/, test/, tsconfig.json — exact deletion targets inventoried before mutation
    - fixtures/compat/migration/typescript-v1/manifest.v1.json — retained migration-data hash source
    - test/fixtures/executors/diag-ok.js, diag-slow.js, and diag-large.js — Phase 10 hash-pinned legacy test fixtures requiring Phase 11 disposition before deletion
  </read_first>
  <action>
Resolve the exact repository-relative preflight root `target/phase14-delete-preflight/`, remove/recreate only that validated generated subtree, and set `CARGO_TARGET_DIR=target/phase14-delete-preflight/cargo`; never read a binary or package from the default/stale `target/release` or `target/release-evidence`. Run the complete fresh chain from Phases 10–13: `cargo fmt`, Clippy, locked workspace/doc tests, coverage, Provider/retrieval reports, fixture compatibility, migration support, package corruption tests, release build, product fingerprint, deterministic package assembly into `target/phase14-delete-preflight/artifacts`, extracted native/npm installed smoke, candidate source/compat gates, and candidate milestone flow using `target/phase14-delete-preflight/evidence`. Pass the explicit binary/artifact/evidence paths added in 13-03, record the fingerprint plus binary/archive/npm hashes in the preflight snapshot, and verify every downstream report cites those exact values. Confirm the responsibility matrix covers every tracked TS/TSX test/evaluator path plus the three Phase 10 `transitionalLegacyTestFixtures`, and that each non-TS data file currently under `test/` is either fingerprint-equivalent under `fixtures/compat/` or explicitly retired. Capture hashes for all retained migration/compatibility fixtures. If any predecessor summary, report, fixture copy, disposition, fingerprint/hash binding, or command is missing/failing, stop without deleting or editing a tracked target.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command '$p="target/phase14-delete-preflight"; if (Test-Path -LiteralPath $p) { Remove-Item -LiteralPath $p -Recurse -Force }; New-Item -ItemType Directory -Force -Path "$p/artifacts","$p/evidence" | Out-Null; $env:CARGO_TARGET_DIR="$p/cargo"; cargo fmt --all -- --check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo clippy --workspace --all-targets --locked -- -D warnings; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo test --workspace --locked; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run eval:provider; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run eval:retrieval; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run test:package; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run build:rust:release; if ($LASTEXITCODE) { exit $LASTEXITCODE }; node scripts/release/product-fingerprint.mjs &gt; "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli.exe"; if (-not (Test-Path -LiteralPath $b)) { $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli" }; npm run package:rust -- --binary $b --output "$p/artifacts" --fingerprint-file "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:rust-release -- --binary $b --artifacts "$p/artifacts" --evidence-dir "$p/evidence"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo run -p minimax-compat-harness --locked -- verify-candidate; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:milestone-flow -- --artifacts "$p/artifacts" --evidence-dir "$p/evidence" --fingerprint-file "$p/fingerprint.json"; exit $LASTEXITCODE'</automated>
    <source_assertions>Every planned deletion path is present in the Phase 10 source baseline, every required TS or legacy-JS test responsibility/fixture has a non-TS replacement owner, and no preflight evidence points outside the task-specific clean root.</source_assertions>
  </verify>
  <acceptance_criteria>
    - All replacement gates pass on the current branch before the first deletion.
    - The preflight produces exact deletion and retained-fixture path/hash sets.
    - The preflight fingerprint, binary, native archive, npm package, and installed-smoke evidence all come from the same clean task-specific directories and current hash set.
    - Failure leaves `src/`, `test/`, `tsconfig.json`, package metadata, and fixtures unchanged.
  </acceptance_criteria>
  <done>The cleanup set is complete, reviewable, and authorized by already-passing replacement evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Delete replaced TS source/config/dependencies and drive the transitional allowance to zero</name>
  <files>src/, test/, dist/, tsconfig.json, package.json, package-lock.json, fixtures/compat/source-authority.v1.json, target/phase14-post-delete/</files>
  <read_first>
    - Task 1 deletion/retained hash sets — exact mutation boundary
    - src/ and test/ — tracked product/test trees deleted by this task
    - dist/ — generated legacy output removed if present and never regenerated
    - tsconfig.json — compiler configuration deleted by this task
    - package.json and package-lock.json — residual compiler/runtime/UI dependencies and legacy scripts removed by this task
    - fixtures/compat/source-authority.v1.json — transitional TS list changed to empty; static fixture/JS allowlists retained
    - fixtures/compat/migration/typescript-v1/manifest.v1.json — retained paths that must still hash identically
    - .planning/phases/MMX-14-typescript-removal-and-hosted-closure/14-CONTEXT.md — D-14-01/D-14-02
  </read_first>
  <action>
Per D-14-01, delete all tracked `.ts/.tsx` product and test source, the three dispositioned `test/fixtures/executors/diag-{ok,slow,large}.js` legacy test fixtures, and `tsconfig.json`; remove generated `dist/` if present. Remove any residual TypeScript/tsx/tsc/React/Ink/Node-legacy dependencies, scripts, file globs, and lock entries, then regenerate only the dependency-free npm lock metadata with scripts disabled. Per D-14-02, do not touch CJS/MJS launcher/release orchestration or any static JSON/JSONL fixture/support metadata. Update `source-authority.v1.json` so both `transitionalTypeScript` and `transitionalLegacyTestFixtures` lists/counts/hashes are zero and the retained JS/static-data allowlists remain exact. Resolve and validate the independent generated root `target/phase14-post-delete/`, remove/recreate only that subtree, set `CARGO_TARGET_DIR=target/phase14-post-delete/cargo`, and rerun the same complete fresh format/Clippy/workspace-tests/evaluators/package-test/release-build/fingerprint/package/installed-smoke/candidate/milestone chain with explicit `--binary`, `--output`/`--artifacts`, `--evidence-dir`, and `--fingerprint-file` arguments. No post-deletion command may read default `target/release` or preflight artifacts.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command '$p="target/phase14-post-delete"; if (Test-Path -LiteralPath $p) { Remove-Item -LiteralPath $p -Recurse -Force }; New-Item -ItemType Directory -Force -Path "$p/artifacts","$p/evidence" | Out-Null; $env:CARGO_TARGET_DIR="$p/cargo"; cargo fmt --all -- --check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo clippy --workspace --all-targets --locked -- -D warnings; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo test --workspace --locked; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run eval:provider; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run eval:retrieval; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run test:package; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run build:rust:release; if ($LASTEXITCODE) { exit $LASTEXITCODE }; node scripts/release/product-fingerprint.mjs &gt; "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli.exe"; if (-not (Test-Path -LiteralPath $b)) { $b=Join-Path $env:CARGO_TARGET_DIR "release/minimax-cli" }; npm run package:rust -- --binary $b --output "$p/artifacts" --fingerprint-file "$p/fingerprint.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:rust-release -- --binary $b --artifacts "$p/artifacts" --evidence-dir "$p/evidence"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; cargo run -p minimax-compat-harness --locked -- verify-candidate; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify:milestone-flow -- --artifacts "$p/artifacts" --evidence-dir "$p/evidence" --fingerprint-file "$p/fingerprint.json"; exit $LASTEXITCODE'</automated>
    <source_assertions>The source-authority report states zero TS/TSX product/test paths and zero transitional legacy test fixtures, the retained fixture hashes equal Task 1, and package metadata has no legacy compiler/runtime/UI dependency or script.</source_assertions>
  </verify>
  <acceptance_criteria>
    - Repository product/test source contains zero TS/TSX files and no compiler config/generated legacy output.
    - `test/fixtures/executors/diag-{ok,slow,large}.js` are deleted after their Phase 11 disposition and `transitionalLegacyTestFixtures` is exactly empty.
    - Package and lock metadata contain no TypeScript compiler/runtime, React/Ink runtime, legacy bin, build, test, eval, or fallback path.
    - Immutable compatibility/migration/evaluation fixtures and two-release support metadata are byte-identical to the preflight.
    - Direct Rust and extracted npm commands still report the same identity and pass a read-only capability status smoke.
    - Every post-deletion fingerprint/hash/evidence value is regenerated under `target/phase14-post-delete/` and cannot alias Task 1 or default target outputs.
  </acceptance_criteria>
  <done>The replaced implementation is gone while Rust, migration, and both installation paths remain green.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test --workspace --locked` plus doc tests
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- `npm run eval:provider && npm run eval:retrieval && npm run test:package`
- `npm run build:rust:release && npm run package:rust && npm run verify:rust-release && npm run verify:milestone-flow`
- Compare all retained fixture hashes with the Task 1 snapshot.

## Success Criteria

- RCUT-01 and D-14-01/D-14-02 pass after replacement-first deletion.
- EDGE-06 is exercised from the post-deletion tree.
- No migration fixture, real user data, platform scope, Provider/API, publication, push, or PR is touched.

## Artifacts this phase produces

- None new.
- `src/`, `test/`, `dist/`, `tsconfig.json`: deleted under the reviewed replacement contract.
- `package.json` and `package-lock.json`: modified to remove any residual TypeScript/compiler/UI/legacy dependency or script.
- `fixtures/compat/source-authority.v1.json`: modified `transitionalTypeScript` and `transitionalLegacyTestFixtures` counts/fingerprint to the zero-source final state while retaining static data and thin JS allowlists.
- `target/phase14-delete-preflight/`: generated clean preflight build, package, fingerprint, installed-smoke, and exact deletion/retention hash evidence; no stale default target is accepted.
- `target/phase14-post-delete/`: independently generated clean post-deletion build, package, fingerprint, installed-smoke, and evidence root; all commands use explicit paths.
