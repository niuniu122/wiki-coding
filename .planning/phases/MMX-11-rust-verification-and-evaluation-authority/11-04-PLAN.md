---
phase: 11-rust-verification-and-evaluation-authority
plan: "04"
type: execute
wave: 7
depends_on:
  - "11-03"
files_modified:
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - RVE-03
must_haves:
  truths:
    - "`npm run eval:provider` and `npm run eval:retrieval` execute the Rust compat-harness reports, and no package/CI command executes a TypeScript evaluator."
    - "Coverage, Provider evaluation, and retrieval evaluation are blocking predecessors of build/package/evidence in CI per D-11-05."
    - "A failing Rust report blocks verification and release even when transitional Node/package smoke succeeds."
    - "TypeScript evaluator source may remain inert and hash-pinned until 14-01, but it has no package, CI, parity, or release authority."
  prohibitions:
    - statement: "MUST NOT delete transitional TypeScript evaluator source in Phase 11."
      status: resolved
      verification: "This plan modifies only package.json and CI; source deletion remains 14-01."
    - statement: "MUST NOT execute `tsx`, `ts-node`, or a TypeScript evaluator from package or CI verification paths."
      status: resolved
      verification: "Structural scans and runnable aliases require only Rust compat-harness evaluator commands."
    - statement: "MUST NOT let package smoke compensate for a failed Rust Provider or retrieval report."
      status: resolved
      verification: "CI dependency/order and aggregate commands stop before build/package/evidence on either report failure."
  artifacts:
    - path: package.json
      provides: "Rust-only Provider/retrieval evaluation aliases and aggregate verification order"
    - path: .github/workflows/ci.yml
      provides: "Release-blocking Rust coverage/evaluation ordering before package/evidence"
  key_links:
    - from: package.json
      to: crates/compat-harness/src/main.rs
      via: "Provider and retrieval aliases invoke Rust machine-report subcommands"
      pattern: "provider-eval|retrieval-eval"
    - from: .github/workflows/ci.yml
      to: package.json
      via: "coverage and both Rust evaluator aliases block build/package/evidence"
      pattern: "eval:provider|eval:retrieval"
---

# Phase 11 Plan 04: Rust Evaluation Release Authority

## Goal / Objective

<objective>
Cut package and CI verification authority over to the Rust Provider/retrieval reports without deleting the still-inert TypeScript evaluator sources.

Purpose: Complete RVE-03 and D-11-05 after 11-03 proves the retrieval evaluator itself.
Output: Rust-only evaluation aliases and fail-closed CI/release ordering in `package.json` and `.github/workflows/ci.yml`.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-CONTEXT.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-03-SUMMARY.md
@package.json
@.github/workflows/ci.yml
@src/eval/provider-conformance.ts
@src/eval/capability-retrieval-report.ts
@crates/compat-harness/src/main.rs
</context>

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Make Rust evaluation reports the blocking package and CI authority</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-03-SUMMARY.md — proven retrieval report command, golden, and failure semantics
    - package.json — evaluation and aggregate verification aliases modified by this task
    - .github/workflows/ci.yml — evaluation/build/package/evidence order modified by this task
    - crates/compat-harness/src/main.rs — exact `provider-eval`, `retrieval-eval`, and verification commands
    - src/eval/provider-conformance.ts — transitional source that remains present but must not execute
    - src/eval/capability-retrieval-report.ts — transitional source that remains present but must not execute
    - .planning/SPEC.md — D-11-05 Rust-failure-over-package-smoke authority contract
  </read_first>
  <behavior>
    - Package aliases invoke `cargo run -p minimax-compat-harness --locked -- provider-eval --format json` and the equivalent `retrieval-eval` command.
    - Aggregate verification runs coverage and both Rust reports before build, package, smoke, or evidence commands.
    - CI uses the same Rust commands/order and a non-zero report exit prevents every downstream release step.
    - Transitional TS evaluator files remain tracked but are unreachable from scripts and workflow steps.
  </behavior>
  <action>
Point `eval:provider` and `eval:retrieval` at their exact Rust compat-harness subcommands and make aggregate verification run coverage, Provider, then retrieval before any build/package/evidence command. Update CI to use those same commands as blocking steps, preserving offline/read-only permissions and the existing supported target matrix. Remove every package/CI invocation of `tsx`, `ts-node`, `src/eval/provider-conformance.ts`, and `src/eval/capability-retrieval-report.ts`, but do not delete or edit those transitional sources. Use the 11-03 failure-path test plus CI dependency/order assertions to prove a Rust report failure blocks downstream work even if package smoke would pass.
  </action>
  <verify>
    <automated>npm run eval:provider &amp;&amp; npm run eval:retrieval &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
    <source_assertions>`rg "tsx|ts-node|src/eval/(provider-conformance|capability-retrieval-report)" package.json .github/workflows/ci.yml` returns no executable reference, while both TS source files remain present.</source_assertions>
  </verify>
  <acceptance_criteria>
    - Both package aliases execute deterministic Rust JSON reports and propagate non-zero failures.
    - CI cannot reach build/package/evidence after coverage, Provider, or retrieval failure.
    - No package/CI path executes TS evaluation; transitional evaluator source remains untouched for 14-01.
    - Hosted matrix, permissions, offline behavior, and product functionality are unchanged.
  </acceptance_criteria>
  <done>Rust reports are the sole release-blocking evaluation authority before Phase 12 begins.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-compat-harness --test provider_eval --test retrieval_eval --test coverage --locked`
- `npm run eval:provider && npm run eval:retrieval`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- Structural scan confirms package/CI contain no executable TS evaluator reference and the TS sources remain present.

## Success Criteria

- RVE-03 and D-11-05 are complete.
- Rust Provider/retrieval failures block parity/release regardless of package smoke.
- No TypeScript evaluator executes, while source deletion remains deferred to 14-01.

## Artifacts this phase produces

- `package.json`: modified Rust-only evaluation aliases and aggregate blocking order.
- `.github/workflows/ci.yml`: modified coverage/Provider/retrieval-before-build/package/evidence order; no new permissions, platform, or functionality.
