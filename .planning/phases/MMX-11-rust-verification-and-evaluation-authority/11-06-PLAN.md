---
phase: 11-rust-verification-and-evaluation-authority
plan: "06"
type: execute
gap_closure: true
wave: 9
depends_on:
  - "11-05"
files_modified:
  - crates/compat-harness/src/source_authority.rs
  - crates/compat-harness/tests/source_authority.rs
  - test/run-tests.ts
  - test/test-discovery.ts
  - test/test-discovery.test.ts
  - test/provider-conformance.test.ts
  - test/capability-retrieval-report.test.ts
  - fixtures/compat/source-authority.v1.json
  - fixtures/compat/verification/typescript-responsibilities.v1.json
autonomous: true
requirements:
  - RVE-02
  - RVE-03
must_haves:
  truths:
    - "`npm test` discovers transitional tests but cannot directly or transitively import, re-export, or dynamically import either `src/eval/**` module."
    - "The two former evaluator-driving `.test.ts` files become static inert-source/authority tests and never execute evaluator code."
    - "Rust source authority and the TypeScript discovery preflight share fail-closed normalized dependency semantics, including `.js`-to-TS resolution and indirect paths."
    - "Package/CI Provider and retrieval aliases remain the existing Rust commands; other transitional TypeScript tests/static checks stay available."
  prohibitions:
    - statement: "MUST NOT delete or execute the hash-pinned TypeScript evaluator sources before Phase 14."
      status: resolved
      verification: "Static tests prove both source files remain hash-pinned while dependency reachability is empty."
    - statement: "MUST NOT rely on direct string scans that miss re-export, indirect, dynamic-import, extension, separator, or path-normalization variants."
      status: resolved
      verification: "Synthetic Rust/TS graphs cover direct, multi-hop, export-from, dynamic import, `.js` mapping, Windows separators, and normalized relative paths."
    - statement: "MUST NOT remove unrelated transitional tests/static checks or change Rust evaluator report authority."
      status: resolved
      verification: "Only two discovered evaluator tests change purpose; package/CI aliases and evaluator implementation/goldens remain untouched."
    - statement: "MUST NOT use network, credentials, Provider calls, model downloads, publication, push, or hosted evidence."
      status: resolved
      verification: "All checks are static dependency/source/hash analysis plus existing offline Rust evaluators."
  artifacts:
    - path: crates/compat-harness/src/source_authority.rs
      provides: "Rust-owned discovered-test transitive dependency authority"
    - path: test/test-discovery.ts
      provides: "Pre-import TypeScript dependency-graph guard using actual discovered tests"
    - path: test/run-tests.ts
      provides: "Fail-closed graph preflight before importing any transitional test module"
    - path: test/provider-conformance.test.ts
      provides: "Static inert Provider evaluator source/hash authority test"
    - path: test/capability-retrieval-report.test.ts
      provides: "Static inert retrieval evaluator source/hash authority test"
  key_links:
    - from: test/run-tests.ts
      to: test/test-discovery.ts
      via: "validate discovered dependency graph before `importTestFiles`"
      pattern: "discoverTestFiles|importTestFiles"
    - from: crates/compat-harness/src/source_authority.rs
      to: test/test-discovery.ts
      via: "independent Rust validation of the same discovered-test reachability boundary"
      pattern: "src/eval/"
    - from: fixtures/compat/source-authority.v1.json
      to: fixtures/compat/verification/typescript-responsibilities.v1.json
      via: "exact hashes for every edited transitional TS file remain synchronized"
      pattern: "sourceSha256"
    - from: package.json
      to: crates/compat-harness/src/main.rs
      via: "unchanged Rust-only Provider and retrieval eval aliases"
      pattern: "provider-eval|retrieval-eval"
---

# Phase 11 Plan 06: Make TypeScript Evaluators Transitively Inert

## Goal / Objective

<objective>
Remove the final transitive Node-test execution path to the TypeScript evaluators while retaining their source as hash-pinned inert evidence.

Purpose: Make the already-implemented Rust Provider and retrieval reports the sole evaluation authority for RVE-02/RVE-03.
Output: Dual Rust/TypeScript dependency-graph gates, static evaluator-source tests, and exact hash/disposition synchronization.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-CONTEXT.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-VERIFICATION.md
@package.json
@.github/workflows/ci.yml
@test/run-tests.ts
@test/test-discovery.ts
@test/provider-conformance.test.ts
@test/capability-retrieval-report.test.ts
@crates/compat-harness/src/source_authority.rs
@fixtures/compat/source-authority.v1.json
@fixtures/compat/verification/typescript-responsibilities.v1.json
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Prove discovered tests can reach TypeScript evaluators transitively</name>
  <files>crates/compat-harness/tests/source_authority.rs, test/test-discovery.test.ts</files>
  <read_first>
    - .planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-VERIFICATION.md — forbidden npm-test-to-evaluator data flow
    - test/run-tests.ts and test/test-discovery.ts — actual recursive discovery/import path
    - test/provider-conformance.test.ts and test/capability-retrieval-report.test.ts — current direct evaluator imports
    - crates/compat-harness/src/source_authority.rs — current direct-command-only authority weakness
    - crates/compat-harness/tests/source_authority.rs — synthetic repository/CI mutation test target
    - test/test-discovery.test.ts — transitional graph regression target
  </read_first>
  <behavior>
    - The committed graph is rejected because both discovered test modules directly reach `src/eval/**`.
    - Synthetic discovered tests are rejected for indirect imports, `export ... from`, string-literal dynamic imports, `.js` specifiers resolving to `.ts/.tsx`, normalized `.`/`..`, and Windows/Posix separators.
    - Package/CI direct aliases remain Rust-only but cannot make a transitive npm-test evaluator route legal.
  </behavior>
  <action>
Add Rust authority tests that ask the existing repository validator to reject the committed discovered-test graph and table-driven synthetic direct/indirect/re-export/dynamic-import variants. Add a TypeScript test-local graph audit over `discoverTestFiles` that exposes the two current direct evaluator paths plus normalized multi-hop fixtures. Run both focused suites before changing discovery, evaluator tests, or the validator and require RED failures tied to evaluator reachability rather than missing imports or malformed setup.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const a=spawnSync('cargo',['test','-p','minimax-compat-harness','--test','source_authority','--locked','discovered_test_graph_rejects_transitive_typescript_evaluators','--','--exact'],{stdio:'inherit'});const b=spawnSync('npx',['tsx','--test','test/test-discovery.test.ts'],{stdio:'inherit',shell:process.platform==='win32'});process.exit(a.status!==0&amp;&amp;b.status!==0?0:1)"</automated>
    <source_assertions>RED names both evaluator modules and at least one indirect/re-export/dynamic path; package and CI alias tests remain green.</source_assertions>
  </verify>
  <acceptance_criteria>
    - Both authority layers reproduce the forbidden evaluator reachability before GREEN.
    - The negative matrix includes path normalization and indirect dependency forms, not only literal direct imports.
    - No evaluator source, package alias, CI workflow, or manifest changes in this task.
  </acceptance_criteria>
  <done>The transitive evaluator escape is captured as deterministic Rust and TypeScript RED evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Preflight the dependency graph and convert evaluator tests to static evidence</name>
  <files>crates/compat-harness/src/source_authority.rs, crates/compat-harness/tests/source_authority.rs, test/run-tests.ts, test/test-discovery.ts, test/test-discovery.test.ts, test/provider-conformance.test.ts, test/capability-retrieval-report.test.ts, fixtures/compat/source-authority.v1.json, fixtures/compat/verification/typescript-responsibilities.v1.json</files>
  <read_first>
    - crates/compat-harness/tests/source_authority.rs and test/test-discovery.test.ts — RED cases that must turn green without weakening
    - test/test-discovery.ts — discovery and dependency resolution modified by this task
    - test/run-tests.ts — graph preflight placement before imports
    - test/provider-conformance.test.ts and test/capability-retrieval-report.test.ts — rewrite targets; evaluator imports/calls must disappear
    - src/eval/provider-conformance.ts and src/eval/capability-retrieval-report.ts — read-only inert source that must remain present
    - crates/compat-harness/src/source_authority.rs — Rust dependency scanner and shared verify-mode preflight
    - fixtures/compat/source-authority.v1.json — exact hashes for edited transitional files
    - fixtures/compat/verification/typescript-responsibilities.v1.json — matching source hashes and revised inert-test dispositions
    - package.json and .github/workflows/ci.yml — read-only confirmation that direct aliases already invoke Rust
  </read_first>
  <behavior>
    - `run-tests.ts` computes and validates the complete local dependency graph before importing the first discovered test.
    - Both Rust and TS scanners resolve static imports, side-effect imports, re-exports, and literal dynamic imports conservatively and fail closed on evaluator reachability or unsafe/unresolved local paths.
    - Former evaluator tests inspect source presence/hash and Rust authority statically without importing or calling evaluator modules.
    - Other discovered tests and transitional static checks continue unchanged.
  </behavior>
  <action>
Implement a deterministic dependency graph rooted at the exact recursive `*.test.ts`/`*.test.tsx` discovery set. Extract static imports, side-effect imports, `export ... from`, and string-literal dynamic imports; normalize separators and relative components; map emitted `.js` specifiers back to tracked `.ts/.tsx`; reject unsafe, ambiguous, symlinked, or unresolved local dependencies; and walk transitively with cycle handling. Invoke the TypeScript guard in `run-tests.ts` before `importTestFiles`, and independently run the Rust graph validator from `validate_source_authority` so both `verify` and `verify-candidate` fail before Node execution. Rewrite `provider-conformance.test.ts` and `capability-retrieval-report.test.ts` as static inert-source/authority tests that verify the evaluator files remain exact hash-pinned Phase 14 inputs and that Rust package aliases/report evidence own evaluation; remove all evaluator imports and calls. Preserve every other transitional test/static check. After contents settle, update only the exact `source-authority.v1.json` hashes for the five edited TS files (`run-tests.ts`, `test-discovery.ts`, `test-discovery.test.ts`, and the two evaluator tests), synchronize their matrix `sourceSha256` values and semantic dispositions, and change no evaluator source, report golden, package alias, CI workflow, or hosted evidence.
  </action>
  <verify>
    <automated>npx tsx --test test/test-discovery.test.ts test/provider-conformance.test.ts test/capability-retrieval-report.test.ts &amp;&amp; cargo test -p minimax-compat-harness --test source_authority --test coverage --locked &amp;&amp; npm run eval:provider &amp;&amp; npm run eval:retrieval &amp;&amp; cargo test -p minimax-provider --locked &amp;&amp; cargo test -p minimax-retrieval --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - Direct, indirect, re-export, dynamic-import, and normalized path mutations fail in both graph layers.
    - `npm test` cannot import either evaluator and both evaluator source files remain present/hash-pinned.
    - Exact source-authority and matrix hashes agree for all five edited TS files with no unrelated hash refresh.
    - Rust coverage/Provider/retrieval gates and both npm eval aliases remain authoritative and green.
  </acceptance_criteria>
  <done>TypeScript evaluators are transitively inert while Rust reports remain the sole evaluation authority.</done>
</task>

</tasks>

## Verification

- Focused graph/static tests and Rust authority/coverage suites from Task 2.
- Run full `npm test` exactly once after focused checks are green; confirm neither evaluator export is imported or called.
- Run `npm run eval:provider` and `npm run eval:retrieval`, focused Provider/retrieval Rust suites, and `cargo run -p minimax-compat-harness --locked -- verify-candidate`.
- `cargo fmt --all -- --check` and strict `cargo clippy -p minimax-compat-harness -p minimax-provider -p minimax-retrieval --all-targets --locked -- -D warnings`.
- If required locally, use installed `+1.97.0-x86_64-pc-windows-gnullvm`, bundled `rust-lld`, and `CARGO_NET_OFFLINE=true` as development-only evidence. Do not run or refresh hosted evidence.
- No network, Provider/model call, credential read, publication, push, PR, or source deletion.

## Success Criteria

- RVE-02/RVE-03 Rust evaluators are the only executable evaluation authority in package, CI, test discovery, and repository verification.
- TypeScript evaluator sources remain inert and hash-pinned until Phase 14.
- Other transitional TypeScript verification remains available for later phased retirement.

## Artifacts this phase produces

- `crates/compat-harness/src/source_authority.rs` and tests: modified transitive discovered-test dependency authority.
- `test/run-tests.ts`, `test/test-discovery.ts`, and discovery tests: modified pre-import graph gate and normalized dependency cases.
- Two evaluator-driving `.test.ts` files: modified into static inert-source/authority tests.
- Source-authority and responsibility matrices: modified exact hashes/dispositions for edited TS files only.
