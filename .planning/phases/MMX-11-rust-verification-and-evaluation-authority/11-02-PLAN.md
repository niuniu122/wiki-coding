---
phase: 11-rust-verification-and-evaluation-authority
plan: "02"
type: execute
wave: 5
depends_on:
  - "11-01"
files_modified:
  - fixtures/compat/evaluations/provider.v1.json
  - fixtures/compat/evaluations/provider-report.expected.json
  - crates/compat-harness/src/provider_eval.rs
  - crates/compat-harness/src/lib.rs
  - crates/compat-harness/src/main.rs
  - crates/compat-harness/tests/provider_eval.rs
  - fixtures/compat/verification/typescript-responsibilities.v1.json
  - package.json
  - fixtures/compat/retrieval/capability-cases-expanded.v1.json
autonomous: true
requirements:
  - RVE-02
must_haves:
  truths:
    - "Rust runs Responses and Chat Completions conformance entirely from immutable fixtures and emits deterministic machine-readable pass/fail output per D-11-03."
    - "The evaluator uses production normalization/replay code and does not create a second Provider implementation."
    - "Malformed/premature streams, tool calls, terminal ordering, safe error codes, and redaction are release-blocking checks."
    - "The expanded retrieval corpus is transferred to immutable compatibility ownership and its source responsibility is dispositioned here before the Phase 11 retrieval evaluator consumes it."
  prohibitions:
    - statement: "MUST NOT call a real Provider or read credentials."
      status: resolved
      verification: "Evaluator inputs are fixture bytes and environment-negative tests fail if Provider credential variables influence output."
    - statement: "MUST NOT lower a fixture expectation or threshold silently to make Rust pass."
      status: resolved
      verification: "Fixture metadata and golden report changes are explicit, fingerprinted review inputs."
    - statement: "MUST NOT treat Node/package smoke as Provider behavioral authority."
      status: resolved
      verification: "The package eval command invokes the Rust compat-harness subcommand and verify blocks on its status."
  artifacts:
    - path: crates/compat-harness/src/provider_eval.rs
      provides: "Rust Provider evaluation runner/report serializer"
      exports: ["run_provider_evaluation", "provider_report_json"]
    - path: fixtures/compat/evaluations/provider.v1.json
      provides: "Strict protocol/check/fixture declaration"
    - path: fixtures/compat/evaluations/provider-report.expected.json
      provides: "Deterministic golden machine report"
  key_links:
    - from: crates/compat-harness/src/provider_eval.rs
      to: crates/provider/src/fixture_protocol.rs
      via: "production `replay_fixture` normalization"
      pattern: "replay_fixture"
    - from: package.json
      to: crates/compat-harness/src/main.rs
      via: "`eval:provider` runs the Rust `provider-eval --format json` command"
      pattern: "provider-eval"
---

# Phase 11 Plan 02: Rust Provider Evaluation

## Goal / Objective

<objective>
Replace the TypeScript Provider evaluator with a fixture-only Rust evaluator and a stable machine-readable report.

Purpose: Make RVE-02/D-11-03 authoritative without credentials, network, or duplicated protocol parsing.
Output: Provider evaluation schema/golden, Rust runner/subcommand, tests, and package command cutover.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-CONTEXT.md
@src/eval/provider-conformance.ts
@test/support/provider-conformance-suite.ts
@fixtures/compat/provider-streams
@crates/provider/src/fixture_protocol.rs
@crates/provider/tests/provider_fixtures.rs
@crates/compat-harness/src/main.rs
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the strict Provider evaluation contract and golden</name>
  <files>fixtures/compat/evaluations/provider.v1.json, fixtures/compat/evaluations/provider-report.expected.json, crates/compat-harness/src/provider_eval.rs, crates/compat-harness/tests/provider_eval.rs</files>
  <read_first>
    - src/eval/provider-conformance.ts — transitional check inventory only
    - test/support/provider-conformance-suite.ts — cancellation/redaction/feature responsibility inventory only
    - fixtures/compat/provider-streams/responses.valid.jsonl — Responses source fixture
    - fixtures/compat/provider-streams/chat-completions.valid.jsonl — Chat Completions source fixture
    - fixtures/compat/provider-streams/invalid-cases.v1.json — malformed/premature expected codes
    - crates/provider/src/fixture_protocol.rs — production replay API
    - crates/provider/tests/provider_fixtures.rs — current Rust normalization assertions
    - crates/compat-harness/src/provider_eval.rs — create/modify target
    - crates/compat-harness/tests/provider_eval.rs — create/modify target
  </read_first>
  <behavior>
    - Strict schema lists both supported protocols, fixture paths/fingerprints, and required check IDs.
    - Report contains schema version, fixture fingerprint, per-protocol checks, totals, and top-level pass.
    - Unknown fixture fields/check IDs, missing terminal events, unsafe errors, or golden drift fail.
  </behavior>
  <action>
Create a strict evaluation manifest that references the immutable compatibility streams rather than duplicating raw frames. Implement typed report construction in `provider_eval.rs` using production replay/config/profile APIs. Required checks cover valid text/usage/completion ordering, native tool call identity, malformed and premature rejection codes, failure redaction, unsupported feature rejection, and deterministic repeat output for both protocols. Add golden and mutation tests, including secret-bearing environment variables that must have no effect.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test provider_eval --locked</automated>
  </verify>
  <acceptance_criteria>
    - Both protocols execute every declared check and the committed report matches byte-for-byte after newline normalization.
    - Fixture and report parsing reject unknown fields and duplicate IDs.
    - No HTTP client, API key, keyring, or real endpoint is used.
  </acceptance_criteria>
  <done>The Provider evaluation result is deterministic Rust evidence with no live dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose Provider evaluation and make its failure authoritative</name>
  <files>crates/compat-harness/src/provider_eval.rs, crates/compat-harness/src/lib.rs, crates/compat-harness/src/main.rs, crates/compat-harness/tests/provider_eval.rs, fixtures/compat/verification/typescript-responsibilities.v1.json, package.json, fixtures/compat/retrieval/capability-cases-expanded.v1.json</files>
  <read_first>
    - test/fixtures/capabilities/retrieval-cases-expanded.json — read-only transitional 175-case source retained in place until 14-01
    - fixtures/compat/retrieval/capability-cases-expanded.v1.json — immutable transfer target created by this task
    - crates/compat-harness/src/provider_eval.rs — runner/report contract from Task 1
    - crates/compat-harness/src/lib.rs — exports modified by this task
    - crates/compat-harness/src/main.rs — command dispatch and verify wiring modified by this task
    - package.json — `eval:provider` command modified by this task
    - fixtures/compat/verification/typescript-responsibilities.v1.json — Provider dispositions modified after proof
    - .planning/SPEC.md — Rust-failure-over-Node authority rule
  </read_first>
  <behavior>
    - `provider-eval --format json` writes only the report to stdout and exits non-zero on any failed check.
    - `verify` and `verify-candidate` call the evaluator and cannot be satisfied by a Node result.
    - `npm run eval:provider` is a convenience alias for the Rust command.
    - Corpus transfer preserves every case/query/expected ID and threshold, adds strict schema/fingerprint/stable IDs, and records its responsibility-matrix disposition while the original source remains hash-pinned in place until 14-01.
  </behavior>
  <action>
Export the evaluator, add exact command parsing in `main.rs`, and invoke the same evaluator from repository verification. Change the package eval alias to `cargo run -p minimax-compat-harness --locked -- provider-eval --format json`. Update Provider-related matrix entries to `rust_covered` with the exact command/test/golden evidence only after focused tests pass. In the same plan, read `test/fixtures/capabilities/retrieval-cases-expanded.json` without modifying it and create strict immutable `fixtures/compat/retrieval/capability-cases-expanded.v1.json`, preserving every logical case/query/expected ID and locked threshold while adding schema version, stable IDs, and a deterministic corpus fingerprint; update the corresponding responsibility-matrix entry to the immutable fixture/Rust-consumer disposition and prove old/new logical equivalence. Keep the old source path present, hash-pinned, and non-authoritative until 14-01 performs the reviewed deletion. Add a negative test that forces one Rust check false while a simulated package smoke succeeds and assert the overall verification still fails, satisfying D-11-03 and the Node non-authority prohibition.
  </action>
  <verify>
    <automated>cargo run -p minimax-compat-harness --locked -- provider-eval --format json &amp;&amp; cargo test -p minimax-compat-harness --test provider_eval --test coverage --locked &amp;&amp; cargo test -p minimax-retrieval --test lexical --locked existing_typescript_175_case_fixture_meets_capability_gates -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - The command output parses as the committed schema and has `passed: true` only when every Rust check passes.
    - Provider responsibilities no longer cite TS evaluator/test files as authority.
    - The evaluator makes zero Provider calls and reads zero credentials.
    - The immutable retrieval corpus is logically/fingerprint equivalent and its matrix disposition points to the new owner before 11-03; the old `test/` source remains present and unchanged until 14-01.
  </acceptance_criteria>
  <done>Rust Provider evaluation is the release-blocking authority.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-provider --locked`
- `cargo test -p minimax-compat-harness --test provider_eval --test coverage --locked`
- `npm run eval:provider`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`

## Success Criteria

- RVE-02 and D-11-03 pass with strict offline reports.
- Provider thresholds/fixture intent cannot be weakened silently.
- Rust failure blocks compatibility/release regardless of package smoke.

## Artifacts this phase produces

- `fixtures/compat/evaluations/provider.v1.json`: new Provider evaluation manifest.
- `fixtures/compat/evaluations/provider-report.expected.json`: new golden report.
- `crates/compat-harness/src/provider_eval.rs`: new runner/report API.
- `crates/compat-harness/tests/provider_eval.rs`: new evaluation/golden/negative test target.
- `crates/compat-harness/src/lib.rs`: modified exports for the Provider evaluation/report API.
- `crates/compat-harness/src/main.rs`: new `provider-eval --format json` command and verify link.
- `fixtures/compat/verification/typescript-responsibilities.v1.json`: modified Provider responsibility rows wired to exact Rust evaluator tests and golden evidence.
- `fixtures/compat/retrieval/capability-cases-expanded.v1.json`: new immutable expanded corpus transferred with strict schema/fingerprint/stable IDs; the hash-pinned `test/fixtures/...` source remains unchanged until 14-01.
- `package.json`: modified `eval:provider` alias; no Node Provider implementation.
