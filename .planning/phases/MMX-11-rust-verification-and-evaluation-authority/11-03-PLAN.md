---
phase: 11-rust-verification-and-evaluation-authority
plan: "03"
type: execute
wave: 6
depends_on:
  - "11-02"
files_modified:
  - fixtures/compat/evaluations/retrieval.v1.json
  - fixtures/compat/evaluations/retrieval-report.expected.json
  - crates/compat-harness/Cargo.toml
  - crates/compat-harness/src/retrieval_eval.rs
  - crates/compat-harness/src/lib.rs
  - crates/compat-harness/src/main.rs
  - crates/compat-harness/tests/retrieval_eval.rs
  - crates/retrieval/tests/lexical.rs
autonomous: true
requirements:
  - RVE-03
must_haves:
  truths:
    - "Rust evaluation proves exact/BM25 usefulness, mixed Chinese/English cases, stable ties, and truthful no-resource degradation."
    - "BM25 is authoritative recall and embedding observes/reranks only its bounded lexical candidates per D-11-04."
    - "The 175-case corpus and its responsibility-matrix disposition are already owned under immutable compatibility fixtures by 11-02 before this evaluator implementation begins."
    - "The 8-file plan atomically implements and verifies the Rust retrieval evaluator; package/CI authority changes are isolated in 11-04."
  prohibitions:
    - statement: "MUST NOT allow semantic evaluation to introduce an outsider or replace useful lexical results on failure."
      status: resolved
      verification: "Recording-runner outsider/failure cases assert preserved BM25 IDs/order and degraded reason."
    - statement: "MUST NOT silently lower ranking thresholds or make platform-dependent tie decisions."
      status: resolved
      verification: "Fixture metadata, stable-ID tie assertions, and golden metrics are reviewable and deterministic."
    - statement: "MUST NOT download an embedding model or call a real Provider."
      status: resolved
      verification: "Evaluation uses a deterministic in-memory candidate runner and records zero downloads/network/credentials."
  artifacts:
    - path: crates/compat-harness/src/retrieval_eval.rs
      provides: "Rust retrieval evaluator and machine report"
      exports: ["run_retrieval_evaluation", "retrieval_report_json"]
    - path: fixtures/compat/evaluations/retrieval-report.expected.json
      provides: "Deterministic exact/BM25/hybrid/degraded golden"
  key_links:
    - from: crates/compat-harness/src/retrieval_eval.rs
      to: crates/retrieval/src
      via: "production exact/BM25/capability workspace and candidate-only search APIs"
      pattern: "minimax_retrieval"
---

# Phase 11 Plan 03: Rust Retrieval Evaluator

## Goal / Objective

<objective>
Consume the labeled corpus already transferred by 11-02, implement the retrieval evaluation/report in Rust, and prove BM25-first/candidate-only behavior before package/CI authority changes.

Purpose: Implement the RVE-03 evaluator and D-11-04 behavior before 11-04 makes it release-authoritative.
Output: Rust evaluator/golden and focused retrieval tests over the 11-02 immutable corpus.
Dependency boundary: 11-02 must already have completed corpus equivalence/transfer and responsibility disposition. This plan modifies only the 8 evaluator/runtime/test files; package and CI remain untouched until 11-04.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-CONTEXT.md
@src/eval/capability-retrieval-report.ts
@crates/retrieval/tests/lexical.rs
@crates/retrieval/tests/project_discovery.rs
@crates/retrieval/tests/capability_workspace.rs
@fixtures/compat/retrieval
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove production lexical behavior against the transferred immutable corpus</name>
  <files>crates/retrieval/tests/lexical.rs</files>
  <read_first>
    - .planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-02-SUMMARY.md — completed corpus equivalence/transfer and responsibility disposition prerequisite
    - fixtures/compat/retrieval/capability-cases-expanded.v1.json — immutable corpus already created by 11-02; read-only input here
    - crates/retrieval/tests/lexical.rs — current include path and threshold assertions modified by this task
    - src/eval/capability-retrieval-report.ts — transitional metric/check responsibility inventory
  </read_first>
  <behavior>
    - The compatibility fixture supplied by 11-02 preserves every descriptor/case with explicit schema, corpus fingerprint, stable IDs, and checked metric thresholds.
    - Rust lexical tests read only the compatibility fixture, not `test/`.
    - Corpus mutations, unknown fields, duplicate IDs, invalid expected IDs, or case-count loss fail.
  </behavior>
  <action>
Consume only the immutable corpus transferred and dispositioned by 11-02. Update Rust lexical assertions as needed to enforce strict schema/fingerprint, exact/BM25 usefulness, stable ties, mixed-language cases, candidate-only rerank boundaries, and truthful degradation against that path. Do not copy, delete, or redisposition a source corpus in this plan; fail if the 11-02 summary/matrix prerequisite is missing.
  </action>
  <verify>
    <automated>cargo test -p minimax-retrieval --test lexical --locked existing_typescript_175_case_fixture_meets_capability_gates -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - The 11-02 immutable corpus passes its exact case-count/fingerprint and every labeled query/expected result remains exercised.
    - Rust tests no longer reference `test/fixtures`.
    - Existing thresholds are unchanged and checked from fixture metadata.
  </acceptance_criteria>
  <done>The deletion-safe retrieval corpus has one immutable, Rust-consumed owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact/BM25/hybrid/degraded Rust evaluation</name>
  <files>fixtures/compat/evaluations/retrieval.v1.json, fixtures/compat/evaluations/retrieval-report.expected.json, crates/compat-harness/Cargo.toml, crates/compat-harness/src/retrieval_eval.rs, crates/compat-harness/src/lib.rs, crates/compat-harness/src/main.rs, crates/compat-harness/tests/retrieval_eval.rs</files>
  <read_first>
    - fixtures/compat/retrieval/capability-cases-expanded.v1.json — read-only labeled corpus transferred by 11-02
    - fixtures/compat/retrieval/projects.v1.json — project discovery cases
    - crates/retrieval/tests/project_discovery.rs — recording-runner and outsider patterns
    - crates/retrieval/tests/capability_workspace.rs — bounded lexical-union and degradation patterns
    - crates/compat-harness/src/provider_eval.rs — machine-report command/golden pattern
    - crates/compat-harness/src/retrieval_eval.rs — create/modify target
    - crates/compat-harness/src/main.rs — command dispatch modified by this task
  </read_first>
  <behavior>
    - Report includes corpus fingerprint, case counts, exact/BM25/hybrid metrics, candidate set evidence, outsider result, degradation modes, thresholds, and pass.
    - Deterministic runner receives only lexical candidate IDs and cannot add an outsider.
    - Missing/damaged/slow/malformed/outsider semantic cases preserve BM25 output with an exact degraded reason.
  </behavior>
  <action>
Implement `retrieval_eval.rs` over production retrieval APIs and add only the runtime dependency needed to drive existing async search. Use stable IDs and deterministic vectors/runner; do not implement a second ranking kernel. Evaluate the expanded corpus, project cases, and capability-workspace cases across exact/BM25, verified candidate-only hybrid, no resource, runner failure, malformed response, timeout, and outsider paths. Serialize a strict report and compare a golden. Add `retrieval-eval --format json` plus repository-verification wiring, following the Provider evaluator command contract.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test retrieval_eval --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- retrieval-eval --format json</automated>
  </verify>
  <acceptance_criteria>
    - Metrics meet unchanged fixture thresholds in repeated runs on the same corpus.
    - Candidate observation proves lexical recall occurred first and semantic IDs are a subset.
    - Golden output is byte-stable after newline normalization and contains truthful degraded modes.
  </acceptance_criteria>
  <done>RVE-03 has a deterministic Rust machine report using production ranking behavior.</done>
</task>

</tasks>

## Verification

- Verify 11-02 corpus transfer/disposition first; then treat Cargo/lib/main wiring, Rust tests, and golden report as this plan's one evaluator boundary.
- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test -p minimax-retrieval -p minimax-compat-harness --locked`
- `cargo run -p minimax-compat-harness --locked -- retrieval-eval --format json`

## Success Criteria

- The RVE-03 Rust evaluator and D-11-04 are proven and ready for 11-04 authority cutover.
- EDGE-05 blocks rather than weakens threshold/fixture disagreement.
- The retrieval Rust report is deterministic and machine-readable; package/CI authority remains explicitly deferred to 11-04.

## Artifacts this phase produces

- `fixtures/compat/evaluations/retrieval.v1.json`: new evaluation manifest.
- `fixtures/compat/evaluations/retrieval-report.expected.json`: new golden report.
- `crates/compat-harness/src/retrieval_eval.rs`: new evaluator/report API.
- `crates/compat-harness/tests/retrieval_eval.rs`: new metrics/order/degradation/authority test target.
- `crates/compat-harness/Cargo.toml`: modified dependency/runtime wiring required by the evaluator.
- `crates/compat-harness/src/lib.rs`: modified exports for retrieval evaluation/report APIs.
- `crates/compat-harness/src/main.rs`: new `retrieval-eval --format json` command.
- `crates/retrieval/tests/lexical.rs`: modified BM25-first, stable-tie, candidate-only rerank/degradation assertions.
