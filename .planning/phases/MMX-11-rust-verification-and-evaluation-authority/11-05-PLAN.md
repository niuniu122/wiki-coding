---
phase: 11-rust-verification-and-evaluation-authority
plan: "05"
type: execute
gap_closure: true
wave: 8
depends_on:
  - "11-04"
files_modified:
  - fixtures/compat/verification/typescript-responsibilities.v1.json
  - crates/compat-harness/src/coverage.rs
  - crates/compat-harness/tests/coverage.rs
  - crates/cli/tests/restart.rs
  - crates/cli/tests/index_commands.rs
  - crates/cli/tests/tool_loop.rs
  - crates/core/tests/runtime_machine.rs
  - crates/core/tests/session_machine.rs
  - crates/core/tests/tool_machine.rs
  - crates/core/tests/compaction_trace.rs
  - crates/provider/tests/config_credentials.rs
  - crates/provider/tests/provider_fixtures.rs
  - crates/retrieval/tests/lexical.rs
  - crates/retrieval/tests/project_discovery.rs
  - crates/retrieval/tests/capability_workspace.rs
  - crates/tui/tests/command_render.rs
autonomous: true
requirements:
  - RVE-01
must_haves:
  truths:
    - "All 101 starting responsibility rows are semantically audited; split rows may increase the final count, but no source responsibility is lost or collapsed for filename parity."
    - "Every shipped public or safety responsibility names a behaviorally relevant Rust test owner, while retirement is limited to a specific reviewed dormant, internal, or unshipped outcome."
    - "Retry and continue are executed as distinct Rust behaviors with distinct durable/terminal outcomes; parser recognition is not outcome evidence."
    - "Coverage validation enforces responsibility-specific semantic evidence contracts and rejects unrelated reuse even when the cited Rust function exists."
  prohibitions:
    - statement: "MUST NOT accept one lexical ranking test as evidence for catalog, policy, dispatch, refresh, snapshot, adapter, and ranking behavior."
      status: resolved
      verification: "The semantic contract layer rejects one owner reused across incompatible evidence classes/contract IDs."
    - statement: "MUST NOT retire public or safety behavior with generic dormant/internal/unshipped boilerplate."
      status: resolved
      verification: "Retirement requires a structured reviewed outcome, exact scope, and evidence that the outcome is not shipped."
    - statement: "MUST NOT create filename-parity Rust ports or alter Provider/retrieval evaluator authority in this plan."
      status: resolved
      verification: "Tests are added only for uncovered behavior contracts; evaluator commands/reports and TS discovery remain unchanged."
    - statement: "MUST NOT use network, credentials, Provider calls, model downloads, publication, push, or hosted evidence."
      status: resolved
      verification: "All matrix and behavior checks use local Rust state machines and immutable fixtures."
  artifacts:
    - path: fixtures/compat/verification/typescript-responsibilities.v1.json
      provides: "Audited source responsibilities with semantic evidence class, contract ID, exact test owner, or specific reviewed retirement"
    - path: crates/compat-harness/src/coverage.rs
      provides: "Fail-closed semantic evidence and compatible-owner validation beyond test-name existence"
    - path: crates/compat-harness/tests/coverage.rs
      provides: "Collapsed mapping, parser-as-outcome, false-retirement, and unrelated-owner negative contracts"
    - path: crates/cli/tests/restart.rs
      provides: "Executed retry-versus-continue outcome evidence"
  key_links:
    - from: fixtures/compat/verification/typescript-responsibilities.v1.json
      to: crates/compat-harness/src/coverage.rs
      via: "closed evidence class/contract schema and exact path-plus-test ownership validation"
      pattern: "evidenceClass|evidenceContract"
    - from: fixtures/compat/verification/typescript-responsibilities.v1.json
      to: crates/cli/tests/restart.rs
      via: "retry/continue responsibility cites an executed behavior test, not the TUI parser"
      pattern: "ts-command-retry-continue-outcomes"
    - from: crates/compat-harness/src/main.rs
      to: crates/compat-harness/src/coverage.rs
      via: "existing shared verify/verify-candidate coverage preflight"
      pattern: "validate_coverage_matrix"
---

# Phase 11 Plan 05: Repair Semantic Responsibility Evidence

## Goal / Objective

<objective>
Repair the exact-but-semantically-hollow TypeScript responsibility matrix so every shipped public and safety outcome is backed by relevant Rust behavior.

Purpose: Close the RVE-01 evidence gap without porting TypeScript filenames or changing evaluation authority.
Output: An audited matrix, enforceable semantic evidence contracts, and only the missing deterministic Rust behavior tests.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-CONTEXT.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-VERIFICATION.md
@fixtures/compat/verification/typescript-responsibilities.v1.json
@crates/compat-harness/src/coverage.rs
@crates/compat-harness/tests/coverage.rs
@crates/cli/tests/restart.rs
@crates/core/tests
@crates/provider/tests
@crates/retrieval/tests
@crates/tui/tests/command_render.rs
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Reject collapsed mappings, parser aliases, and false retirements</name>
  <files>crates/compat-harness/tests/coverage.rs</files>
  <read_first>
    - .planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-VERIFICATION.md — authoritative semantic gaps and named false evidence
    - fixtures/compat/verification/typescript-responsibilities.v1.json — all 97 sources and 101 starting responsibility rows under audit
    - crates/compat-harness/src/coverage.rs — current function-name-only validation weakness
    - crates/compat-harness/tests/coverage.rs — focused RED target modified by this task
    - crates/tui/tests/command_render.rs — parser-only retry/continue evidence that must be rejected
  </read_first>
  <behavior>
    - The 18 retrieval/capability sources cannot all cite `existing_typescript_175_case_fixture_meets_capability_gates` for unrelated catalog, command, dispatcher, policy, refresh, snapshot, adapter, and ranking contracts.
    - `ts-command-retry-continue-outcomes` cannot cite `parser_covers_every_manifest_command_alias_and_argument_shape` as behavior-outcome evidence.
    - Agent loop/kernel, permission/tool, budget/fail-closed, model/profile/credential, and summary-redaction rows cannot be retired with the current generic no-shipped-outcome rationale when their public/safety outcome exists.
    - A named Rust function without a compatible responsibility contract and exact semantic owner is insufficient evidence.
  </behavior>
  <action>
Add focused tests over the committed matrix that enumerate the verifier's collapsed retrieval set and false-retirement families, then require a responsibility-level evidence class, stable contract ID/claim, and exact owner. Add mutations proving incompatible responsibilities cannot reuse one test owner merely because its function name exists. Add an explicit regression that outcome evidence rejects the current TUI parser owner. Run the named RED tests before modifying the matrix or validator and record failures caused by the current mappings, not parsing/setup errors.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const r=spawnSync('cargo',['test','-p','minimax-compat-harness','--test','coverage','--locked','semantic_audit_rejects_collapsed_unrelated_and_false_retirement_evidence','--','--exact'],{stdio:'inherit'});process.exit(r.status===0?1:0)"</automated>
    <source_assertions>The failing output identifies the shared lexical owner, parser-only retry/continue owner, and named public/safety retirement families.</source_assertions>
  </verify>
  <acceptance_criteria>
    - RED compiles and fails on committed semantic evidence, not missing files or malformed fixtures.
    - The negative set covers every verifier-named retrieval, agent/kernel, permission/tool, budget/fail-closed, model/profile/credential, and summary-redaction family.
    - No evaluator, product, package, or CI file changes in this task.
  </acceptance_criteria>
  <done>The verifier's semantic failures are reproducible as deterministic Rust coverage-gate failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Audit all responsibilities and bind exact behavioral owners</name>
  <files>fixtures/compat/verification/typescript-responsibilities.v1.json, crates/compat-harness/src/coverage.rs, crates/compat-harness/tests/coverage.rs, crates/cli/tests/restart.rs, crates/cli/tests/index_commands.rs, crates/cli/tests/tool_loop.rs, crates/core/tests/runtime_machine.rs, crates/core/tests/session_machine.rs, crates/core/tests/tool_machine.rs, crates/core/tests/compaction_trace.rs, crates/provider/tests/config_credentials.rs, crates/provider/tests/provider_fixtures.rs, crates/retrieval/tests/lexical.rs, crates/retrieval/tests/project_discovery.rs, crates/retrieval/tests/capability_workspace.rs, crates/tui/tests/command_render.rs</files>
  <read_first>
    - fixtures/compat/verification/typescript-responsibilities.v1.json — audit every starting row and split collapsed responsibilities here
    - crates/compat-harness/src/coverage.rs — semantic schema/validator modified by this task
    - crates/compat-harness/tests/coverage.rs — RED cases that must turn green without weakening
    - crates/cli/tests/restart.rs and crates/core/tests/session_machine.rs — executed continue/retry paths and durable identities
    - crates/core/tests/runtime_machine.rs and crates/core/tests/tool_machine.rs — agent ordering, budget, cancellation, and fail-closed evidence
    - crates/core/tests/compaction_trace.rs — structured summary and redaction evidence
    - crates/provider/tests/config_credentials.rs and crates/provider/tests/provider_fixtures.rs — credential, model/profile, and protocol evidence
    - crates/retrieval/tests/lexical.rs, project_discovery.rs, and capability_workspace.rs — distinct exact/BM25, catalog, snapshot, hybrid, and workspace contracts
    - crates/cli/tests/index_commands.rs and tool_loop.rs — public retrieval commands, dispatch, permission, and tool outcomes
    - crates/tui/tests/command_render.rs — parser/rendering evidence that remains parser/rendering-only
  </read_first>
  <behavior>
    - Each matrix responsibility has a closed semantic evidence class, stable contract ID and specific claim, plus an exact Rust path/test owner or structured reviewed retirement.
    - Reusing an owner is legal only for the same explicit contract/class/category; incompatible many-to-one mappings fail.
    - Every public/safety row is Rust-covered; retirement records the exact dormant/internal/unshipped outcome and why it is absent from the shipped contract.
    - Continue creates the next normal turn while retry creates a new turn identity from an immutable terminal source; both outcomes are executed and persisted.
  </behavior>
  <action>
Evolve the matrix/serde contract in place with a closed evidence-class enum and responsibility-specific evidence contract containing a stable ID, precise behavioral claim, and exact owner. Enforce disposition/class compatibility, exact named-test ownership, compatible reuse only for one semantic contract, and a structured retirement review instead of keyword boilerplate. Audit all 101 starting rows against the actual TS assertions and Rust suites: split any source row that contains distinct outcomes, rebind the 18 retrieval/capability sources across lexical, project-discovery, capability-workspace, CLI command, tool/dispatch, policy, snapshot, refresh, and adapter tests, and reclassify public/safety retirements across agent/kernel/permission/tool/budget/fail-closed/model/profile/credential/summary-redaction. Keep retirement only for a specifically reviewed dormant/internal/unshipped outcome such as non-product harness plumbing or unshipped live smoke. Add a Rust test only when no exact relevant test exists; in particular add a dedicated retry-versus-continue behavior test that asserts distinct requests, turn IDs, terminal records, source immutability, and persisted replay. Do not add one Rust file/test per TS filename and do not touch Provider/retrieval evaluator authority.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test coverage --locked &amp;&amp; cargo test -p minimax-cli --test restart --test index_commands --test tool_loop --locked &amp;&amp; cargo test -p minimax-core --test runtime_machine --test session_machine --test tool_machine --test compaction_trace --locked &amp;&amp; cargo test -p minimax-provider --test config_credentials --test provider_fixtures --locked &amp;&amp; cargo test -p minimax-retrieval --test lexical --test project_discovery --test capability_workspace --locked &amp;&amp; cargo test -p minimax-tui --test command_render --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - All RED cases pass and a mutation restoring the prior collapsed/parser/boilerplate evidence fails.
    - Every starting responsibility has been reviewed; split rows have unique IDs and no responsibility is dropped.
    - Matrix links point to behaviorally relevant existing tests except documented narrow new tests.
    - Coverage, CLI, core, Provider, retrieval, TUI, and `verify-candidate` are deterministic and offline.
  </acceptance_criteria>
  <done>RVE-01 is backed by reviewable semantic Rust evidence rather than path counts or function-name substrings.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-compat-harness --test coverage --locked`
- Focused CLI/core/Provider/retrieval/TUI commands from Task 2.
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- If the workstation requires it, run the Rust commands offline with installed `+1.97.0-x86_64-pc-windows-gnullvm` and bundled `rust-lld` as local development-only evidence; do not refresh or claim hosted evidence.

## Success Criteria

- RVE-01 has exact, semantically relevant Rust evidence or a specific reviewed non-public retirement for every responsibility.
- Retry and continue outcomes are executed, distinct, durable, and no longer self-bound to a parser test.
- Provider/retrieval evaluator authority and TypeScript evaluator discovery remain unchanged for 11-06.

## Artifacts this phase produces

- `fixtures/compat/verification/typescript-responsibilities.v1.json`: modified audited responsibilities, semantic evidence contracts, exact owners, and specific retirement reviews.
- `crates/compat-harness/src/coverage.rs`: modified semantic contract/owner validation; no product behavior.
- `crates/compat-harness/tests/coverage.rs`: modified semantic negative and full-matrix audit tests.
- Listed Rust behavior suites: modified only where an exact responsibility lacked relevant evidence, especially retry/continue outcomes.
