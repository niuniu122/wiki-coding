---
phase: 11-rust-verification-and-evaluation-authority
plan: "01"
type: execute
wave: 4
depends_on:
  - "10-03"
files_modified:
  - fixtures/compat/verification/typescript-responsibilities.v1.json
  - crates/compat-harness/src/coverage.rs
  - crates/compat-harness/src/lib.rs
  - crates/compat-harness/src/main.rs
  - crates/compat-harness/tests/coverage.rs
  - crates/cli/tests/headless.rs
  - crates/cli/tests/lifecycle_wiki.rs
  - crates/cli/tests/tool_loop.rs
  - crates/tui/tests/command_render.rs
autonomous: true
requirements:
  - RVE-01
must_haves:
  truths:
    - "Every transitional TS test/evaluation responsibility has one reviewable disposition and Rust/package owner."
    - "Public CLI, lifecycle, tool, and rendering contracts are backed by deterministic Rust evidence; dormant TS internals are explicitly retired per D-11-01."
    - "A removed TS test does not require a one-to-one Rust filename, but it always has written evidence per D-11-02."
  prohibitions:
    - statement: "MUST NOT port undocumented TS internals merely to preserve file-count parity."
      status: resolved
      verification: "Coverage entries require a public-contract citation or an explicit retirement rationale."
    - statement: "MUST NOT leave a responsibility with `requires_port`, unknown ownership, or missing evidence at plan completion."
      status: resolved
      verification: "The coverage validator rejects unresolved dispositions and missing evidence paths."
    - statement: "MUST NOT delete TS source/tests before the replacement matrix and Rust gates pass."
      status: resolved
      verification: "This plan records the hashed baseline and performs no source deletion."
  artifacts:
    - path: fixtures/compat/verification/typescript-responsibilities.v1.json
      provides: "Historical TS responsibility-to-Rust/package/retirement disposition matrix"
    - path: crates/compat-harness/src/coverage.rs
      provides: "Completeness, evidence, hash, and disposition validator"
      exports: ["load_coverage_matrix", "validate_coverage_matrix"]
    - path: crates/compat-harness/tests/coverage.rs
      provides: "Matrix completeness and unresolved/invalid-retirement negative cases"
  key_links:
    - from: fixtures/compat/verification/typescript-responsibilities.v1.json
      to: fixtures/compat/source-authority.v1.json
      via: "every transitional test/eval path is matched by path plus hash"
      pattern: "sourceSha256"
    - from: crates/compat-harness/src/main.rs
      to: crates/compat-harness/src/coverage.rs
      via: "compat verify blocks on unresolved coverage"
      pattern: "validate_coverage_matrix"
---

# Phase 11 Plan 01: Coverage Disposition and Behavioral Gaps

## Goal / Objective

<objective>
Turn the transitional TS test/evaluation tree into a complete historical responsibility matrix and close required public CLI/lifecycle/tool/rendering gaps in Rust.

Purpose: Make D-11-01 and D-11-02 enforceable before evaluators and source deletion.
Output: Coverage matrix, Rust validator, and focused Rust behavior assertions.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-SPEC.md
@.planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-CONTEXT.md
@fixtures/compat/source-authority.v1.json
@test/run-tests.ts
@src/eval/provider-conformance.ts
@src/eval/capability-retrieval-report.ts
@crates/cli/tests
@crates/provider/tests
@crates/retrieval/tests
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze and validate every TS test/evaluation responsibility</name>
  <files>fixtures/compat/verification/typescript-responsibilities.v1.json, crates/compat-harness/src/coverage.rs, crates/compat-harness/src/lib.rs, crates/compat-harness/src/main.rs, crates/compat-harness/tests/coverage.rs</files>
  <read_first>
    - fixtures/compat/source-authority.v1.json — hashed transitional path baseline from Phase 10
    - test/run-tests.ts — TS test discovery source of truth
    - src/eval/provider-conformance.ts — Provider evaluation responsibility
    - src/eval/capability-retrieval-report.ts — retrieval evaluation responsibility
    - fixtures/compat/verification/typescript-responsibilities.v1.json — create/modify target
    - crates/compat-harness/src/coverage.rs — create/modify target
    - crates/compat-harness/src/main.rs — verification wiring modified by this task
    - .planning/phases/MMX-11-rust-verification-and-evaluation-authority/11-CONTEXT.md — D-11-01/D-11-02
  </read_first>
  <behavior>
    - Every TS/TSX file under `test/` plus every TS evaluator/smoke responsibility has a path, hash, category, public-contract reference, disposition, evidence list, and rationale.
    - Allowed dispositions are `rust_covered`, `package_smoke`, and `retired`; no unresolved value is accepted.
    - `retired` requires a concrete dormant/internal/unshipped rationale and cannot cite a locked public contract.
    - Evidence paths must exist, be Rust or allowed package orchestration, and cannot point back to TS authority.
  </behavior>
  <action>
Create the complete matrix from the Phase 10 hashed baseline, grouping assertions by responsibility without collapsing distinct public outcomes. Per D-11-01, port only documented public/safety behavior; per D-11-02, write an explicit disposition for every removed test rather than manufacturing one Rust file per TS file. Implement strict serde loading and validation, including exact baseline coverage, unique responsibility IDs, evidence existence, retirement rationale rules, and schema/unknown-field rejection. Wire it into both compatibility verification modes before evaluation/release checks.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test coverage --locked</automated>
  </verify>
  <acceptance_criteria>
    - Matrix path/hash set exactly covers the transitional test/eval baseline.
    - No entry is unresolved, and no behavioral authority points to TS source.
    - Removing an entry, falsifying a hash, or retiring a locked public command makes the test fail.
  </acceptance_criteria>
  <done>Maintainers can review the fate and replacement evidence of every TS verification responsibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close public CLI, lifecycle, tool, and rendering evidence gaps</name>
  <files>crates/cli/tests/headless.rs, crates/cli/tests/lifecycle_wiki.rs, crates/cli/tests/tool_loop.rs, crates/tui/tests/command_render.rs, fixtures/compat/verification/typescript-responsibilities.v1.json</files>
  <read_first>
    - fixtures/compat/verification/typescript-responsibilities.v1.json — gap/disposition list modified by this task
    - fixtures/compat/commands.v1.json — locked slash-command outcomes
    - fixtures/compat/command-differences.v1.json — approved behavioral differences
    - crates/cli/tests/headless.rs — CLI/JSONL/package contract target
    - crates/cli/tests/lifecycle_wiki.rs — lifecycle/finalization target
    - crates/cli/tests/tool_loop.rs — tool identity/approval/terminal target
    - crates/tui/tests/command_render.rs — command and safe rendering target
    - test/cli-lifecycle.test.ts and test/chat-input-policy.test.ts — transitional responsibility sources, not implementation authority
  </read_first>
  <behavior>
    - Every required CLI name/outcome and text/JSONL terminal behavior has deterministic Rust assertions.
    - Lifecycle restart/finalization, tool call ordering/denial, permissions reset, rendering control sanitization, and retry/continue outcomes remain covered.
    - Matrix evidence is updated only after the exact focused Rust test passes.
  </behavior>
  <action>
Use the matrix to identify entries in the CLI/lifecycle/tool/rendering categories whose evidence does not yet prove the public outcome. Add only the missing assertions to the four named Rust suites, reusing existing fixtures and production paths. Do not copy TS internal architecture or helper shapes. For every added or confirmed assertion, update the matrix with the exact Rust test file and test name; convert no item to `rust_covered` until that focused test is green.
  </action>
  <verify>
    <automated>cargo test -p minimax-cli --test headless --test lifecycle_wiki --test tool_loop --locked &amp;&amp; cargo test -p minimax-tui --test command_render --locked &amp;&amp; cargo test -p minimax-compat-harness --test coverage --locked</automated>
  </verify>
  <acceptance_criteria>
    - All non-evaluator public behavior entries are `rust_covered` or explicitly retired with a valid non-public rationale.
    - Rust tests are deterministic, offline, credential-free, and use stable fixtures/IDs.
    - A Rust test failure leaves the coverage gate and release decision failing.
  </acceptance_criteria>
  <done>RVE-01 no longer depends on TS behavioral tests for CLI, lifecycle, tools, or rendering.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-compat-harness --test coverage --locked`
- `cargo test -p minimax-cli --test headless --test lifecycle_wiki --test tool_loop --locked`
- `cargo test -p minimax-tui --test command_render --locked`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`

## Success Criteria

- RVE-01 and D-11-01/D-11-02 are complete and reviewable.
- EDGE-04 is resolved without porting dormant internals.
- TS test/eval files remain present but cease to be decision authority.

## Artifacts this phase produces

- `fixtures/compat/verification/typescript-responsibilities.v1.json`: new historical matrix fields `sourcePath`, `sourceSha256`, `responsibilities`, `contractRefs`, `disposition`, `evidence`, and `rationale`.
- `crates/compat-harness/src/coverage.rs`: new matrix loader/validator symbols.
- `crates/compat-harness/tests/coverage.rs`: new completeness/retirement negative test target.
- `crates/compat-harness/src/lib.rs`: modified exports for `load_coverage_matrix`/`validate_coverage_matrix`.
- `crates/compat-harness/src/main.rs`: modified `verify`/`verify-candidate` wiring so the matrix blocks before evaluator/release decisions.
- `fixtures/compat/verification/typescript-responsibilities.v1.json`: material wiring from each source path/hash and responsibility to exact Rust test file/test name or reviewed retirement evidence.
- Existing CLI/TUI Rust suites: modified assertions only; no new product command or format.
