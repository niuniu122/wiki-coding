---
phase: MMX-05-retrieval-and-project-discovery
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [RETR-01, RETR-02]
files_modified:
  - crates/protocol/src/retrieval.rs
  - crates/protocol/src/lib.rs
  - crates/retrieval/src/domain.rs
  - crates/retrieval/src/normalize.rs
  - crates/retrieval/src/exact.rs
  - crates/retrieval/src/bm25.rs
  - crates/retrieval/src/rrf.rs
  - crates/retrieval/src/snapshot.rs
  - crates/retrieval/src/lib.rs
  - crates/retrieval/tests/lexical.rs
  - fixtures/compat/retrieval/lexical.v1.json
must_haves:
  truths:
    - "Capability, project, and Wiki documents share algorithms but cannot cross index/result/snapshot types."
    - "Exact wins; BM25/RRF are deterministic; no-resource and no-match behavior is useful and truthful."
    - "Mixed Chinese/English normalization is versioned and stable."
    - "D-501: Shared generic retrieval engine with typed capability, project, and Wiki domains."
    - "D-502: Public types and serialized snapshots reject cross-domain use."
    - "D-503: Versioned deterministic Chinese/English normalization."
    - "D-504: Exact precedence and stable BM25 k1=1.2/b=0.75 ranking."
    - "D-505: Empty and no-overlap queries do not manufacture results."
    - "D-506: Existing 175-case TypeScript retrieval fixture remains the parity gate."
    - "D-517: Ordinary Wiki retrieval accepts current pages only."
    - "D-518: Immutable expected-hash snapshots retain the last-known-good publication."
  prohibitions:
    - "MUST NOT add a database, HTTP path, model download, or cross-domain untyped search result."
---

<objective>Build the dependency-light shared retrieval kernel and three schema-isolated lexical index types.</objective>

<tasks>

<task type="auto">
  <name>Task 1: Define typed retrieval contracts and deterministic mixed-language normalization</name>
  <action>Add strict domain/mode/degradation/result/explanation records; implement bounded normalization and Han character/bigram plus Latin separator tokenization. Domain marker types and snapshot IDs must reject cross-domain serialized data.</action>
  <verify>cargo test -p minimax-protocol --locked; cargo test -p minimax-retrieval --locked lexical</verify>
</task>

<task type="auto">
  <name>Task 2: Implement exact, BM25, contribution keywords, cosine, and RRF primitives</name>
  <action>Port exact precedence and BM25 k1=1.2/b=0.75 with finite-score/ID tie-breaks. Record per-term score contribution for explanations. Add candidate-bounded cosine and stable RRF.</action>
  <verify>cargo test -p minimax-retrieval --locked lexical</verify>
</task>

<task type="auto">
  <name>Task 3: Publish typed capability/project/Wiki indexes and immutable snapshots</name>
  <action>Wrap the generic kernel in three public typed index aliases/adapters; enforce current-only ordinary Wiki input; publish expected-hash immutable snapshot records and last-known-good recovery. Add parity fixture coverage.</action>
  <verify>cargo clippy -p minimax-retrieval --all-targets --locked -- -D warnings; cargo test -p minimax-retrieval --locked</verify>
</task>

</tasks>

<success_criteria>RETR-01 and RETR-02 have executable offline evidence and no semantic resource is needed for normal retrieval.</success_criteria>
