---
phase: MMX-05-retrieval-and-project-discovery
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
autonomous: true
requirements: [RETR-03, RETR-04, RETR-05]
files_modified:
  - crates/protocol/src/retrieval.rs
  - crates/retrieval/src/catalog.rs
  - crates/retrieval/src/embedding.rs
  - crates/retrieval/src/discovery.rs
  - crates/retrieval/src/lib.rs
  - crates/retrieval/tests/project_discovery.rs
  - crates/retrieval/tests/embedding_resource.rs
  - fixtures/compat/retrieval/projects.v1.json
  - fixtures/compat/retrieval/embedding-resource.v1.json
must_haves:
  truths:
    - "BM25 produces keywords/candidates before any embedding operation, and semantic output remains a candidate subset."
    - "Hybrid is reported only after exact resource, helper, vector, and fingerprint verification."
    - "Every semantic failure preserves BM25 output with one stable degraded reason."
    - "D-507: BM25 candidates and keywords always exist before embedding is invoked."
    - "D-508: Keywords and explanations come from lexical contributions and catalog facts."
    - "D-509: Project facts come from a strict local offline catalog snapshot."
    - "D-510: Missing license and maintenance facts remain unknown."
    - "D-511: Embedding resources stay separately installed and are never auto-downloaded."
    - "D-512: Granite multilingual qint8 x64-AVX2 identity, hashes, ABI, and metadata all verify."
    - "D-513: Embedding uses a bounded fixed-argument helper process with a deadline."
    - "D-514: Hybrid mode requires the complete resource, helper, vector, and fingerprint proof."
    - "D-515: Every semantic failure keeps BM25 output and reports one stable reason."
    - "D-516: RRF can reorder only the BM25 candidate set."
  prohibitions:
    - "MUST NOT bundle/download weights, invoke a shell/network, read credentials, or search the full catalog semantically."
---

<objective>Restore the non-programmer project finder and complete a concrete separately installed local embedding boundary.</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add a strict local open-source project catalog</name>
  <action>Validate a finite schema-v1 catalog with source/repository/license/platform/topics/activity/release/maintenance facts and stable fingerprint. Unknown facts remain unknown. Reject unknown fields, duplicate IDs, unsafe URLs, and catalog drift.</action>
  <verify>cargo test -p minimax-retrieval --locked project_discovery</verify>
</task>

<task type="auto">
  <name>Task 2: Verify the separately installed embedding resource and helper contract</name>
  <action>Implement strict Granite resource manifest/hash/ABI/CPU/platform/license/vector checks and a fixed executable/stdin/stdout/deadline adapter behind a testable runner port. Validate finite dimensions and catalog/vector fingerprint before ready.</action>
  <verify>cargo test -p minimax-retrieval --locked embedding_resource</verify>
</task>

<task type="auto">
  <name>Task 3: Compose BM25-first candidate-bounded project reranking</name>
  <action>Return lexical result first, then optionally call the verified semantic port with only candidate IDs/text. Fuse within that set; on any failure return unchanged BM25 candidates and a stable reason. Record ordering in tests.</action>
  <verify>cargo test -p minimax-retrieval --locked project_discovery</verify>
</task>

</tasks>

<success_criteria>Project discovery visibly preserves BM25-first behavior and truthful optional semantic degradation.</success_criteria>
