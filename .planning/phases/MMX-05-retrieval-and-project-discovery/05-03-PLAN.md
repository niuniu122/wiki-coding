---
phase: MMX-05-retrieval-and-project-discovery
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
autonomous: true
requirements: [RETR-05, RETR-06]
files_modified:
  - crates/cli/src/app.rs
  - crates/cli/src/main.rs
  - crates/cli/src/headless.rs
  - crates/cli/tests/index_commands.rs
  - crates/tui/src/command.rs
  - crates/tui/src/render.rs
  - crates/tui/tests/command_render.rs
  - crates/compat-harness/src/baseline.rs
  - crates/compat-harness/tests/compat_report.rs
  - crates/retrieval/tests/benchmark.rs
  - fixtures/compat/retrieval/explanations.v1.json
  - package.json
must_haves:
  truths:
    - "Text/JSONL show identical actual mode, degradation, keywords, sources, license, maintenance, and explanations."
    - "Capability, project, and current-Wiki searches are explicit read-only commands and discovered software is never executed automatically."
    - "Offline parity and 10k-page latency gates pass while the npm product entry stays TypeScript."
    - "D-519: Text and JSONL render the same facts, mode, degradation, and explanation."
    - "D-520: Capability and project searches are explicit read-only interfaces and never execute results."
    - "D-521: All evaluation remains offline, deterministic, credential-free, and spend-free."
    - "D-522: The npm product entry remains dist/cli.js until Phase 6."
  prohibitions:
    - "MUST NOT claim unknown catalog facts, hide degraded mode, execute a result, change npm entry, or use live network/model resources."
---

<objective>Expose truthful retrieval surfaces and close Phase 5 with compatibility, explanation, and performance evidence.</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit index status/search CLI and TUI routes</name>
  <action>Route capability, project, and Wiki status/search through CLI-owned composition. Make `/capabilities` available. Emit typed text/JSONL with stable mode/degradation and no automatic execution.</action>
  <verify>cargo test -p minimax-cli --locked index_commands; cargo test -p minimax-tui --locked command_render</verify>
</task>

<task type="auto">
  <name>Task 2: Render project evidence and deterministic explanations</name>
  <action>Show matched keywords, source/repository, license or unknown, platform, last activity/release, maintenance signals, lexical/semantic ranks, and the actual runtime mode. Sanitize and bound all fields.</action>
  <verify>cargo test -p minimax-cli --locked index_commands</verify>
</task>

<task type="auto">
  <name>Task 3: Close compatibility, evaluation, and performance gates</name>
  <action>Run Rust parity over the existing 175-case fixture, project/Wiki/failure matrices, and a recorded 10k-page BM25 p95 benchmark. Extend compat evidence and architecture gates; run full Rust/TypeScript/eval/diff suite without live resources.</action>
  <verify>cargo test --workspace --locked; npm run eval:retrieval; npm run verify:rust-contracts</verify>
</task>

</tasks>

<success_criteria>RETR-01..06 are executable, explainable, offline-first, and ready for Phase 6 release gates.</success_criteria>
