---
phase: 10-rust-authority-and-source-boundaries
plan: "04"
type: execute
gap_closure: true
wave: 4
depends_on:
  - "10-03"
files_modified:
  - package.json
  - crates/compat-harness/src/baseline.rs
  - crates/compat-harness/src/source_authority.rs
  - crates/compat-harness/tests/compat_report.rs
  - crates/compat-harness/tests/source_authority.rs
  - crates/cli/tests/headless.rs
  - test/ci-contract.test.ts
  - fixtures/compat/source-authority.v1.json
autonomous: true
requirements:
  - RUST-01
  - RUST-03
must_haves:
  truths:
    - "Every product-facing package route starts Rust: `start` stays the thin packaged launcher and `dev` runs the Rust CLI source with argv forwarding."
    - "Rust-owned package and source-authority contracts reject `src/cli.tsx`, another TS/TSX product entry, `dist/cli.js`, or another legacy CLI behind `dev`, `start`, a supported bin, or an equivalent product script."
    - "Transitional TypeScript test, static-check, and evaluation scripts remain available until Phase 11; the guard classifies product execution instead of banning every `tsx` command."
    - "Both `verify` and `verify-candidate` share the fail-closed source-authority preflight, so a package route capable of reaching the legacy `.mini-codex` writer cannot pass."
  prohibitions:
    - statement: "MUST NOT leave a package script that can start the TypeScript/legacy product or its `.mini-codex` writer."
      status: resolved
      verification: "Table-driven Rust mutations cover product script names, direct TS/TSX CLI paths, compiled legacy paths, and equivalent aliases."
    - statement: "MUST NOT broadly ban transitional TypeScript test/evaluation commands before Phase 11."
      status: resolved
      verification: "Positive package fixtures preserve the current test, static-check, and eval script classes while product-entry negatives fail."
    - statement: "MUST NOT delete or port the transitional TypeScript tree in this gap closure."
      status: resolved
      verification: "The plan changes one transitional contract test only and refreshes only its exact source-authority hash."
    - statement: "MUST NOT refresh hosted fingerprints or present GNU-LLVM development evidence as hosted Windows MSVC/Linux GNU release evidence."
      status: resolved
      verification: "Verification is local/offline and explicitly development-only; hosted evidence files remain untouched."
  artifacts:
    - path: package.json
      provides: "Rust-only `dev` and `start` product routes while transitional checks/evaluations remain"
    - path: crates/compat-harness/src/source_authority.rs
      provides: "Mandatory product-script classification used by both repository verification modes"
    - path: crates/compat-harness/src/baseline.rs
      provides: "Independent Rust-owned package product-entry baseline"
    - path: crates/compat-harness/tests/source_authority.rs
      provides: "Adversarial package-route mutations proving TS/legacy entries fail closed"
    - path: test/ci-contract.test.ts
      provides: "Transitional synchronization contract for the Rust `dev` route and retained test/eval scripts"
  key_links:
    - from: package.json
      to: crates/cli
      via: "`dev` invokes Cargo for `minimax-cli` with `--` argv forwarding"
      pattern: "cargo run -p minimax-cli --locked --"
    - from: crates/compat-harness/src/source_authority.rs
      to: package.json
      via: "`validate_executable_links` validates bins and classifies product-facing scripts"
      pattern: "validate_executable_links"
    - from: crates/compat-harness/src/main.rs
      to: crates/compat-harness/src/source_authority.rs
      via: "shared `verify_repository` preflight used by `verify` and `verify-candidate`"
      pattern: "validate_source_authority"
    - from: test/ci-contract.test.ts
      to: fixtures/compat/source-authority.v1.json
      via: "the edited transitional test keeps its exact reviewed SHA-256 entry synchronized"
      pattern: "test/ci-contract.test.ts"
---

# Phase 10 Plan 04: Close the TypeScript Product-Route Escape

## Goal / Objective

<objective>
Close the single Phase 10 verification gap by removing the live TypeScript development route and making future TS/legacy product routes fail Rust-owned authority checks.

Purpose: Restore RUST-01 and RUST-03 without pulling Phase 11 test/evaluation retirement or Phase 14 source deletion into Phase 10.
Output: A Rust source-development command, precise package-route validators, and synchronized Rust/transitional regression contracts.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-SPEC.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-CONTEXT.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-VERIFICATION.md
@package.json
@crates/compat-harness/src/baseline.rs
@crates/compat-harness/src/source_authority.rs
@crates/compat-harness/tests/source_authority.rs
@crates/cli/tests/headless.rs
@test/ci-contract.test.ts
@fixtures/compat/source-authority.v1.json
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Lock the product-facing package route contract</name>
  <files>crates/compat-harness/tests/source_authority.rs, crates/compat-harness/tests/compat_report.rs, crates/cli/tests/headless.rs, test/ci-contract.test.ts, fixtures/compat/source-authority.v1.json</files>
  <read_first>
    - .planning/phases/MMX-10-rust-authority-and-source-boundaries/10-VERIFICATION.md — exact escaped `dev` route and `.mini-codex` consequence being closed
    - package.json — current live `dev`, thin `start`, and transitional test/eval script surface under test
    - crates/compat-harness/tests/source_authority.rs — synthetic repository and mutation-test helpers to extend
    - crates/compat-harness/tests/compat_report.rs — Rust product-baseline integration test to extend
    - crates/cli/tests/headless.rs — current package assertion that catches only `dist/cli.js`
    - test/ci-contract.test.ts — transitional package/CI synchronization assertions to update
    - fixtures/compat/source-authority.v1.json — exact hash entry for the edited transitional test
  </read_first>
  <behavior>
    - The committed package fails the new regression before implementation because `dev` executes `tsx src/cli.tsx`.
    - Mutating `dev`, `start`, or an equivalent product alias to execute `src/cli.tsx`, another direct TS/TSX product entry, `dist/cli.js`, or a named legacy CLI is rejected.
    - Supported bin mappings remain restricted to the fixed JavaScript launcher that starts the sibling Rust binary.
    - Positive fixtures retain `tsx test/...`, `tsx --test ...`, `tsx src/eval/...`, static TypeScript checks, and other explicitly transitional verification commands through Phase 11.
    - A product route that reaches `src/cli.tsx` is treated as a route to the legacy `.mini-codex` writer and fails without executing that writer.
  </behavior>
  <action>
Add table-driven Rust-owned negative contracts before changing `package.json`. Extend the source-authority synthetic repository to include representative product and transitional scripts, then mutate product routes across `dev`, `start`, and an arbitrary product alias. Require a stable rejection for direct TS/TSX CLI execution and known legacy entries, while positive cases prove the current transitional test/static-check/evaluation script classes remain legal. Strengthen the existing baseline/headless assertions so the real repository is red on `tsx src/cli.tsx`, not only on `dist/cli.js`. Update `test/ci-contract.test.ts` with the same intended final `dev`/`start` contract and retained test/eval expectations. Run the focused regressions once against the unchanged package and record the expected RED failure before Task 2. Because this task edits one hash-pinned TS file, update only the `test/ci-contract.test.ts` SHA-256 entry after its contents are final; do not refresh unrelated transitional hashes.
  </action>
  <verify>
    <automated>Run the named focused Rust package-route tests before Task 2 and require them to fail specifically on the committed `dev = tsx src/cli.tsx` route; unrelated assertions must remain green.</automated>
    <source_assertions>The negative matrix includes `dev`, `start`, an equivalent alias, TS and TSX product entries, `dist/cli.js`, and a legacy CLI; positive fixtures retain transitional test/eval commands.</source_assertions>
  </verify>
  <acceptance_criteria>
    - The RED output identifies the live TypeScript product route, not a fixture/setup error.
    - Rust-owned contracts cover both the baseline verifier and the mandatory source-authority verifier.
    - Transitional tests/evaluations are explicitly accepted and no TS/TSX file is deleted or broadly disabled.
    - Only the final hash for `test/ci-contract.test.ts` changes in the source-authority inventory.
  </acceptance_criteria>
  <done>The escaped package route is represented by a precise failing contract before production configuration or validators change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Route development through Rust and enforce the boundary</name>
  <files>package.json, crates/compat-harness/src/baseline.rs, crates/compat-harness/src/source_authority.rs, crates/compat-harness/tests/compat_report.rs, crates/compat-harness/tests/source_authority.rs, crates/cli/tests/headless.rs, test/ci-contract.test.ts, fixtures/compat/source-authority.v1.json</files>
  <read_first>
    - package.json — change `dev` only after Task 1 proves the current route is red; preserve thin `start` and transitional scripts
    - crates/compat-harness/src/baseline.rs — `validate_product_entry` package baseline modified by this task
    - crates/compat-harness/src/source_authority.rs — `validate_executable_links` shared `verify`/`verify-candidate` gate modified by this task
    - crates/compat-harness/tests/source_authority.rs — Task 1 negative and positive package fixtures to make green
    - crates/compat-harness/tests/compat_report.rs — focused baseline verification modified by Task 1
    - crates/cli/tests/headless.rs — final real-package route assertion modified by Task 1
    - test/ci-contract.test.ts — final transitional synchronization assertions and retained evaluation expectations
    - fixtures/compat/source-authority.v1.json — exact final hash update for the edited transitional test only
  </read_first>
  <behavior>
    - `npm run dev -- <args>` reaches `cargo run -p minimax-cli --locked -- <args>` and never executes TypeScript.
    - `npm start -- <args>` remains `node bin/minimax-codex.cjs` and reaches the packaged sibling Rust binary.
    - Both compatibility verification modes reject any future package mutation that restores a TS/TSX/legacy product entry or `.mini-codex`-writing product route.
    - Transitional tests, checks, and evaluations still run until Phase 11 replaces their coverage.
  </behavior>
  <action>
Change `scripts.dev` to the exact Rust source-development command `cargo run -p minimax-cli --locked --`, preserving npm argv forwarding, and keep `scripts.start` exactly `node bin/minimax-codex.cjs`. Extend `validate_product_entry` and `validate_executable_links` with one normalized, deterministic package-script policy: require the supported bin and `start` launcher, require `dev` to be the Rust Cargo route (or reject it), reject known compiled/source legacy CLI paths and direct TS/TSX product execution behind any product-equivalent alias, and explicitly permit the transitional test/static-check/eval script classes until Phase 11. Do not execute scripts during validation and do not infer safety from the absence of generated `dist/`. Make source-authority errors stable enough for the Task 1 mutation table, retain its shared placement ahead of compatibility loading for `verify` and `verify-candidate`, and keep `.mini-codex` read-only migration input. Finish the exact `test/ci-contract.test.ts` hash update only after all test edits settle; do not touch other source hashes or hosted evidence.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test source_authority --locked &amp;&amp; cargo test -p minimax-compat-harness --test compat_report --locked rust_command_permission_provider_and_product_baselines_are_executable -- --exact &amp;&amp; cargo test -p minimax-cli --test headless --locked npm_product_entry_uses_only_rust_launcher -- --exact &amp;&amp; npx tsx --test test/ci-contract.test.ts &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - The RED cases from Task 1 are green without weakening any negative mutation.
    - `dev` and `start` are exact Rust routes and preserve argument forwarding.
    - `verify-candidate` passes the committed tree and the structural test proves both verification modes retain the shared gate.
    - Transitional test/eval scripts remain present, and no Phase 11-14 scope is implemented early.
  </acceptance_criteria>
  <done>No supported or equivalent package route can start the legacy product or its `.mini-codex` writer, while the intended transitional verification surface remains intact.</done>
</task>

</tasks>

## Verification

- Focused contracts: `cargo test -p minimax-compat-harness --test source_authority --locked`, the exact `compat_report` product-baseline test, the exact `headless` npm-product test, and `npx tsx --test test/ci-contract.test.ts`.
- Source authority: run the complete source-authority suite, confirm the edited TS test hash is exact, and run `cargo run -p minimax-compat-harness --locked -- verify-candidate`.
- Full regression: run `npm test` exactly once after every focused check is green.
- Rust quality: `cargo fmt --all -- --check`, a Rust CLI build, and strict `cargo clippy -p minimax-cli -p minimax-compat-harness --all-targets --locked -- -D warnings`.
- On this workstation, use installed `1.97.0-x86_64-pc-windows-gnullvm` with bundled `rust-lld` for the Rust build/tests/Clippy only as local `windows-x86_64-gnullvm-dev` evidence. Do not modify product/release configuration, refresh hosted fingerprints, or claim Windows MSVC/Linux GNU release authority.
- Do not run a hosted workflow, Provider call, network/model download, package publication, push, or PR action.

## Success Criteria

- RUST-01: package `dev`, `start`, bin, and equivalent product routes can execute only Rust.
- RUST-03: no supported or legacy package route can reach the TypeScript runtime that writes `.mini-codex`; Rust state/migration boundaries remain unchanged.
- Phase 11 still owns replacement/retirement of transitional TypeScript tests and evaluations; Phase 14 still owns source deletion and final hosted fingerprint closure.

## Artifacts this phase produces

- `package.json`: modified `dev` Rust source-development route; `start` remains the thin packaged launcher and transitional scripts remain.
- `crates/compat-harness/src/baseline.rs`: modified Rust-only product-entry baseline.
- `crates/compat-harness/src/source_authority.rs`: modified fail-closed package-script authority shared by `verify` and `verify-candidate`.
- `crates/compat-harness/tests/source_authority.rs`: modified adversarial product-route mutation matrix plus transitional-script positive cases.
- `crates/compat-harness/tests/compat_report.rs` and `crates/cli/tests/headless.rs`: modified real-package Rust route assertions.
- `test/ci-contract.test.ts`: modified transitional synchronization contract; no product authority.
- `fixtures/compat/source-authority.v1.json`: modified only for the final hash of `test/ci-contract.test.ts`; no inventory, target, or hosted-evidence expansion.
