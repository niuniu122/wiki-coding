---
phase: 10-rust-authority-and-source-boundaries
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - scripts/release/package-rust.mjs
  - scripts/release/verify-rust-release.mjs
  - scripts/release/verify-milestone-flow.mjs
  - crates/compat-harness/src/source_authority.rs
  - crates/compat-harness/tests/source_authority.rs
  - crates/cli/tests/product_identity.rs
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - RUST-01
  - RUST-02
  - RUST-03
must_haves:
  truths:
    - "Native and npm candidate artifacts contain one executable Rust product path and no packaged legacy entry."
    - "Direct Rust and extracted npm launcher smokes report the same version/product identity."
    - "CI runs the Rust-owned source-authority gate before packaging and never treats transitional Node checks as product authority."
    - "Every Phase 10 plan ends with the Rust CLI and npm-installed path usable."
  prohibitions:
    - statement: "MUST NOT package or invoke the compiled TypeScript CLI as a release entry."
      status: resolved
      verification: "Archive manifest assertions and installed smoke reject a legacy bin/path."
    - statement: "MUST NOT let JavaScript download a runtime, import product source, or implement product behavior."
      status: resolved
      verification: "The Phase 10 source-authority validator scans every allowlisted JS file in CI."
    - statement: "MUST NOT call a real Provider, publish a package, push, or open a PR."
      status: resolved
      verification: "All smokes are local/offline candidates and CI permissions remain contents:read."
  artifacts:
    - path: crates/cli/tests/product_identity.rs
      provides: "Direct-versus-launcher Rust identity test"
    - path: scripts/release/package-rust.mjs
      provides: "Transitional candidate assembly with a single product entry"
    - path: .github/workflows/ci.yml
      provides: "Mandatory source-authority and installed-entry gates"
  key_links:
    - from: scripts/release/verify-rust-release.mjs
      to: bin/minimax-codex.cjs
      via: "extract package, execute launcher, compare version to source Rust binary"
      pattern: "--version"
    - from: .github/workflows/ci.yml
      to: crates/compat-harness/src/source_authority.rs
      via: "verify-rust-contracts candidate/strict commands"
      pattern: "verify:rust-contracts"
---

# Phase 10 Plan 03: Sole-Authority Release and Entry Gates

## Goal / Objective

<objective>
Carry the Rust-only execution and source boundary through candidate packaging, installed-command smoke, and CI.

Purpose: Close every Phase 10 integration edge so later verification ports can trust a single product authority.
Output: Single-entry release manifests, identity smoke, and CI enforcement.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-SPEC.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-CONTEXT.md
@scripts/release/package-rust.mjs
@scripts/release/verify-rust-release.mjs
@scripts/release/verify-milestone-flow.mjs
@.github/workflows/ci.yml
@fixtures/compat/source-authority.v1.json
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove legacy product identity from candidate archives and evidence</name>
  <files>scripts/release/package-rust.mjs, scripts/release/verify-rust-release.mjs, scripts/release/verify-milestone-flow.mjs</files>
  <read_first>
    - scripts/release/package-rust.mjs — npm/native entries and release manifest fields modified by this task
    - scripts/release/verify-rust-release.mjs — archive and npm assertions modified by this task
    - scripts/release/verify-milestone-flow.mjs — complete-flow evidence fields modified by this task
    - package.json — sole bin contract from 10-02
    - bin/minimax-codex.cjs — fixed launcher included in both artifacts
    - .planning/phases/MMX-10-rust-authority-and-source-boundaries/10-SPEC.md — stale-package edge and phase boundary
  </read_first>
  <behavior>
    - Native/npm release manifests describe only launcher plus one Rust binary as the product entry.
    - Candidate tarballs omit generated legacy output and expose no legacy bin.
    - Evidence records `installedRustIdentity` and no legacy field.
  </behavior>
  <action>
Update deterministic archive entry construction and manifest validation to remove the legacy product field and generated legacy payload from candidate artifacts. Keep documentation/licenses and the exact platform Rust binary. Make verification reject any extra executable entry, legacy bin mapping, or generated application payload while continuing checksum, safe-tar-type, size, embedding-exclusion, license, security, and performance checks. Update milestone-flow evidence to require the installed Rust identity instead of a dual-runtime assertion. This is the execution-authority cut, not Phase 13 dependency/metadata minimization.
  </action>
  <verify>
    <automated>npm run build:rust:release &amp;&amp; npm run package:rust &amp;&amp; npm run verify:rust-release</automated>
  </verify>
  <acceptance_criteria>
    - Candidate native/npm archives contain the expected launcher and one native binary and no second product entry.
    - Archive hashes and manifests remain deterministic across two runs.
    - Existing size/security/license/performance gates remain active.
  </acceptance_criteria>
  <done>Release candidates no longer ship or describe a second executable product.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove direct and npm-installed commands share the Rust identity</name>
  <files>crates/cli/tests/product_identity.rs, scripts/release/verify-rust-release.mjs</files>
  <read_first>
    - crates/cli/tests/product_identity.rs — create/modify target
    - crates/cli/src/app.rs — Clap version source
    - scripts/release/verify-rust-release.mjs — extracted npm smoke modified by this task
    - bin/minimax-codex.cjs — argument and exit forwarding under test
    - fixtures/compat/release/thresholds.v1.json — release limits retained by smoke
  </read_first>
  <behavior>
    - Direct binary and extracted npm launcher `--version` output identify the same Rust package version.
    - The smoke observes the exact packaged binary hash, not another executable found on the host.
    - Missing/wrong launcher sibling fails before any fallback child starts.
  </behavior>
  <action>
Add `product_identity.rs` to exercise the compiled CLI identity and package metadata contract. Extend installed npm smoke to capture launcher `--version`, compare it with the source binary output and manifest hash, and preserve a read-only capability status smoke as functional proof. Use isolated extraction directories and an empty/controlled environment so `PATH`, credentials, network, and user state cannot satisfy the test accidentally.
  </action>
  <verify>
    <automated>cargo test -p minimax-cli --test product_identity --locked &amp;&amp; npm run verify:rust-release</automated>
  </verify>
  <acceptance_criteria>
    - Version output, binary SHA-256, and product entry agree across direct, native archive, and npm launcher paths.
    - Smoke reads no credentials, makes no Provider call, and downloads nothing.
  </acceptance_criteria>
  <done>The two supported installation surfaces demonstrably reach the same Rust product.</done>
</task>

<task type="auto">
  <name>Task 3: Make sole-authority gates mandatory in CI</name>
  <files>.github/workflows/ci.yml, crates/compat-harness/src/source_authority.rs, crates/compat-harness/tests/source_authority.rs</files>
  <read_first>
    - .github/workflows/ci.yml — verification step order modified by this task
    - crates/compat-harness/src/source_authority.rs — mandatory gate modified by this task
    - crates/compat-harness/tests/source_authority.rs — CI/source negative assertions modified by this task
    - package.json — exact Rust contract and package scripts
    - .planning/SPEC.md — Node-smoke non-authority rule and authorization prohibitions
  </read_first>
  <action>
Place source-authority verification before candidate packaging in both matrix jobs and keep the strict/candidate hosted branching limited to hosted-evidence freshness. Add structural tests that fail if CI reintroduces a TypeScript build/product command, skips Rust contracts, reverses Rust/package failure authority, injects credentials, or expands write permissions. Preserve the Linux sandbox canary and Windows/Linux matrix unchanged.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test source_authority --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - CI rejects a new TS/JS executable or fallback path before packaging.
    - A failing Rust contract prevents all later packaging/smoke steps.
    - CI still has read-only permissions and performs no publish/push operation.
  </acceptance_criteria>
  <done>Phase 10 authority is enforced at source, package, installed-command, and CI boundaries.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-cli --test product_identity --test state_authority --locked`
- `cargo test -p minimax-compat-harness --test source_authority --test compat_report --locked`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- `npm run build:rust:release && npm run package:rust && npm run verify:rust-release && npm run verify:milestone-flow`

## Success Criteria

- RUST-01/RUST-02/RUST-03 are jointly gated.
- Direct and npm-installed paths remain usable and identity-equal.
- No live API, publication, push, PR, platform expansion, or TS deletion occurs.

## Artifacts this phase produces

- `crates/cli/tests/product_identity.rs`: new direct/npm identity test target.
- `scripts/release/package-rust.mjs`: modified single-product candidate entries and manifest.
- `scripts/release/verify-rust-release.mjs`: modified single-entry archive and installed identity assertions.
- `scripts/release/verify-milestone-flow.mjs`: modified `installedRustIdentity` evidence contract; no new command.
- `.github/workflows/ci.yml`: modified source-authority ordering; no new job or permission.
- `source_authority.rs`/test: modified CI contract checks; no new product behavior.
