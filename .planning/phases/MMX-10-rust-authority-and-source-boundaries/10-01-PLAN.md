---
phase: 10-rust-authority-and-source-boundaries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/compat/source-authority.v1.json
  - crates/compat-harness/src/source_authority.rs
  - crates/compat-harness/src/lib.rs
  - crates/compat-harness/src/main.rs
  - crates/compat-harness/tests/source_authority.rs
autonomous: true
requirements:
  - RUST-02
must_haves:
  truths:
    - "A checked-in manifest classifies every Rust product root, executable/package entry, allowed JavaScript file, transitional TypeScript file, transitional legacy JavaScript test fixture, immutable fixture root, and runtime state root."
    - "The manifest permits transitional TypeScript only as inert, shrinking evidence and rejects an unknown executable or JavaScript path."
    - "The JavaScript allowlist is exact and reviewable; allowed files are limited to the launcher and release/package orchestration per D-10-03 and D-10-04."
    - "`test/fixtures/executors/diag-ok.js`, `diag-slow.js`, and `diag-large.js` are independently hash-pinned in `transitionalLegacyTestFixtures`, excluded from executable/JavaScript authority, dispositioned in Phase 11, and driven to zero in Phase 14."
  prohibitions:
    - statement: "MUST NOT delete the transitional TypeScript tree in Phase 10."
      status: resolved
      verification: "The source-authority test requires every current TS/TSX path to remain represented in transitionalTypeScript and performs no deletion."
    - statement: "MUST NOT allow JavaScript to own Provider, retrieval, session, Vault, tool, migration, runtime-download, or fallback behavior."
      status: resolved
      verification: "Negative manifest/source fixtures are rejected by validate_source_authority."
    - statement: "MUST NOT place legacy executor test fixtures in `javascriptAllowlist` or `executableEntries`."
      status: resolved
      verification: "The manifest and focused tests require the three `diag-*.js` files only in the hash-pinned `transitionalLegacyTestFixtures` class."
    - statement: "MUST NOT add macOS, ARM, a Provider, a tool, or an installer."
      status: resolved
      verification: "The manifest fixes the supported release identities to the existing Windows x64 and Linux x64 contract."
  artifacts:
    - path: fixtures/compat/source-authority.v1.json
      provides: "Schema-versioned ownership, JavaScript allowlist, transitional TS and legacy-test-fixture baselines, and state-root policy"
      contains: "transitionalLegacyTestFixtures"
    - path: crates/compat-harness/src/source_authority.rs
      provides: "Strict manifest loader and deterministic repository/source-boundary validator"
      exports: ["load_source_authority", "validate_source_authority"]
    - path: crates/compat-harness/tests/source_authority.rs
      provides: "Positive completeness and negative unknown-path/business/fallback/download cases"
  key_links:
    - from: crates/compat-harness/src/main.rs
      to: crates/compat-harness/src/source_authority.rs
      via: "verify and verify-candidate call the same source-authority validator"
      pattern: "validate_source_authority"
    - from: crates/compat-harness/src/source_authority.rs
      to: fixtures/compat/source-authority.v1.json
      via: "strict serde schema plus repository enumeration and content checks"
      pattern: "source-authority.v1.json"
    - from: fixtures/compat/source-authority.v1.json
      to: fixtures/compat/verification/typescript-responsibilities.v1.json
      via: "Phase 11 records a disposition for every hash-pinned transitional legacy executor fixture before Phase 14 removes and zeros the class"
      pattern: "transitionalLegacyTestFixtures"
---

# Phase 10 Plan 01: Source Authority Contract

## Goal / Objective

<objective>
Create the reviewable source-ownership contract and a Rust-owned gate that makes the existing authority boundary measurable before any legacy source is removed.

Purpose: Establish D-10-03 and D-10-04 as deterministic repository invariants while preserving the current Rust CLI and npm launcher.
Output: `source-authority.v1.json`, its Rust validator, focused negative tests, and compat-harness wiring.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/SPEC.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-SPEC.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-CONTEXT.md
@package.json
@bin/minimax-codex.cjs
@crates/compat-harness/src/architecture.rs
@crates/compat-harness/src/main.rs
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the complete source and state authority manifest</name>
  <files>fixtures/compat/source-authority.v1.json, crates/compat-harness/src/source_authority.rs, crates/compat-harness/tests/source_authority.rs</files>
  <read_first>
    - .planning/SPEC.md — master npm, Rust authority, edge, and prohibition contract
    - .planning/phases/MMX-10-rust-authority-and-source-boundaries/10-CONTEXT.md — D-10-03 and D-10-04
    - package.json — current bins, scripts, files, and JavaScript entry surface
    - bin/minimax-codex.cjs — current fixed-binary launcher behavior
    - crates/compat-harness/src/architecture.rs — existing deterministic repository-boundary style
    - fixtures/compat/source-authority.v1.json — create/modify target; inspect if a prior task retry left it present
    - crates/compat-harness/src/source_authority.rs — create/modify target; inspect if a prior task retry left it present
    - crates/compat-harness/tests/source_authority.rs — create the focused manifest-schema test before invoking it
  </read_first>
  <behavior>
    - The manifest has strict schema/version fields and rejects unknown fields.
    - `rustProductRoots`, `executableEntries`, `javascriptAllowlist`, `transitionalTypeScript`, `transitionalLegacyTestFixtures`, `immutableFixtureRoots`, and `stateAuthority` are complete and duplicate-free.
    - Each transitional TS/TSX path is pinned by repository-relative path and content hash so additions or edits require review.
    - The three legacy `test/fixtures/executors/diag-{ok,slow,large}.js` files are path/hash/purpose pinned only in `transitionalLegacyTestFixtures`; the class has an explicit Phase 11 disposition and Phase 14 zeroing contract.
    - JavaScript entries declare one allowed orchestration purpose and forbidden domain capabilities; unknown `.cjs/.mjs/.js` paths fail closed.
  </behavior>
  <action>
Define serde types and `load_source_authority(root)` in `source_authority.rs`, then inventory the current tracked product tree into `source-authority.v1.json`. Per D-10-03, allow only the fixed npm launcher and `scripts/release/*.mjs` orchestration files. Per D-10-04, record all current TS/TSX files as an inert transitional baseline and record `test/fixtures/executors/diag-ok.js`, `diag-slow.js`, and `diag-large.js` separately as path/hash/purpose-pinned `transitionalLegacyTestFixtures`; exclude those three fixtures from both `javascriptAllowlist` and `executableEntries`, require a Phase 11 responsibility disposition, and declare that Phase 14 must delete them and set the class/count to zero. Include the exact supported command entry, Windows/Linux target identities, `.minimax` writable root, and `.mini-codex` migration-input classification. Reject absolute paths, path traversal, duplicates across classes, symlinks where regular files are required, unrecognized keys, unsupported executable extensions, and baseline hash drift. Create `crates/compat-harness/tests/source_authority.rs` in this task with the positive `manifest_schema` test and schema/path/hash/class-separation negatives so Task 1 verifies only artifacts it already owns.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test source_authority --locked manifest_schema -- --exact</automated>
    <source_assertions>`source-authority.v1.json` enumerates every tracked TS/TSX and allowed JS path exactly once and contains no credential values.</source_assertions>
  </verify>
  <acceptance_criteria>
    - Strict parsing succeeds for the committed manifest and fails for unknown fields, duplicate paths, unsafe paths, and hash drift.
    - The three `diag-*.js` fixtures appear exactly once in `transitionalLegacyTestFixtures`, never in `javascriptAllowlist`/`executableEntries`, and carry Phase 11/14 lifecycle metadata.
    - The manifest declares only `.minimax` writable and classifies `.mini-codex` as migration input.
    - No tracked source file is deleted or renamed by this task.
  </acceptance_criteria>
  <done>The repository has one complete, hashed, reviewable ownership inventory with no unclassified executable or JavaScript path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce source authority in every compatibility verification mode</name>
  <files>crates/compat-harness/src/source_authority.rs, crates/compat-harness/src/lib.rs, crates/compat-harness/src/main.rs, crates/compat-harness/tests/source_authority.rs</files>
  <read_first>
    - crates/compat-harness/src/source_authority.rs — validator contract created by Task 1
    - fixtures/compat/source-authority.v1.json — authoritative allowlist and baseline
    - crates/compat-harness/src/lib.rs — public exports modified by this task
    - crates/compat-harness/src/main.rs — `verify_repository` wiring modified by this task
    - crates/compat-harness/tests/source_authority.rs — focused test target modified by this task
    - .planning/phases/MMX-10-rust-authority-and-source-boundaries/10-SPEC.md — JavaScript and unknown-entry negative cases
  </read_first>
  <behavior>
    - Both `verify` and `verify-candidate` invoke the same source-authority gate.
    - Synthetic JavaScript importing product source, spawning a non-Rust fallback, downloading a runtime, or containing domain tokens is rejected.
    - A newly added TS/TSX or JS executable path is rejected until the manifest is explicitly reviewed.
  </behavior>
  <action>
Implement `validate_source_authority(root, manifest)` and export it through `lib.rs`. Scan tracked and present repository inputs deterministically, validate entry-to-file links, require the exact three legacy executor fixtures only in `transitionalLegacyTestFixtures`, and apply executable structural/content rules only to the true JavaScript allowlist so fixture text does not create false positives. Wire the validator before report/hosted-evidence checks in `verify_repository`. Extend the Task 1 test target with table-driven negative fixtures for imports from product-domain source, child interpreter fallback, network/runtime retrieval, a new TS file, an unknown executable entry, a legacy fixture smuggled into the executable/allowlist class, and a second writable state root. Keep the validator offline and independent of generated `dist/` contents.
  </action>
  <verify>
    <automated>cargo test -p minimax-compat-harness --test source_authority --locked &amp;&amp; cargo run -p minimax-compat-harness --locked -- verify-candidate</automated>
  </verify>
  <acceptance_criteria>
    - Focused positive and negative tests pass on Windows and Linux path forms.
    - `verify-candidate` fails if the source-authority call is removed or its manifest is incomplete.
    - The launcher remains present and the Rust CLI still builds.
  </acceptance_criteria>
  <done>The compat harness makes the manifest a mandatory, deterministic preflight for all later convergence work.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-compat-harness --test source_authority --locked`
- `cargo run -p minimax-compat-harness --locked -- verify-candidate`
- `cargo build -p minimax-cli --locked`

## Success Criteria

- RUST-02 has a Rust-owned, reviewable allowlist gate.
- D-10-03/D-10-04 and every Phase 10 source-boundary edge are encoded without deleting transitional source.
- The Rust binary and existing npm launcher remain usable.

## Artifacts this phase produces

- `fixtures/compat/source-authority.v1.json`: new manifest fields `schemaVersion`, `rustProductRoots`, `executableEntries`, `javascriptAllowlist`, `transitionalTypeScript`, `transitionalLegacyTestFixtures`, `immutableFixtureRoots`, `supportedTargets`, and `stateAuthority`; the legacy fixture class is Phase 11-dispositioned and Phase 14-zeroed.
- `crates/compat-harness/src/source_authority.rs`: new `load_source_authority` and `validate_source_authority` symbols.
- `crates/compat-harness/tests/source_authority.rs`: new source-boundary test target.
- `crates/compat-harness/src/lib.rs` and `src/main.rs`: modified exports and mandatory `verify_repository` link; no new product command.
