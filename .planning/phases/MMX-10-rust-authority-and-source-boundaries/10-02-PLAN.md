---
phase: 10-rust-authority-and-source-boundaries
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - package.json
  - bin/minimax-codex.cjs
  - crates/compat-harness/src/baseline.rs
  - crates/compat-harness/tests/compat_report.rs
  - crates/cli/tests/headless.rs
  - crates/cli/tests/state_authority.rs
autonomous: true
requirements:
  - RUST-01
  - RUST-03
must_haves:
  truths:
    - "The only supported package command is `minimax-codex`, and it starts the fixed sibling Rust binary per D-10-01."
    - "Current Rust behavior and documented public contracts remain authoritative; dormant TS internals are not ported per D-10-02."
    - "Supported runtime commands create or mutate only `.minimax`; `.mini-codex` remains read-only migration input per D-10-05."
    - "A missing Rust binary produces a stable actionable failure without suggesting or invoking a legacy runtime."
  prohibitions:
    - statement: "MUST NOT keep a runnable supported legacy CLI or fallback."
      status: resolved
      verification: "Rust package-contract tests require one bin entry and launcher failure tests observe no child fallback."
    - statement: "MUST NOT write `.mini-codex` from supported commands."
      status: resolved
      verification: "State-authority integration tests compare directory trees before and after representative commands."
    - statement: "MUST NOT delete transitional TS source in this plan."
      status: resolved
      verification: "The Phase 10 source-authority baseline remains complete and unchanged except for package-entry classification."
  artifacts:
    - path: package.json
      provides: "Single supported `minimax-codex` bin with legacy start route removed"
      contains: "bin/minimax-codex.cjs"
    - path: bin/minimax-codex.cjs
      provides: "No-fallback fixed sibling Rust launcher and stable actionable failures"
    - path: crates/cli/tests/state_authority.rs
      provides: "Black-box proof that runtime writes stay under `.minimax` and migration treats `.mini-codex` as source"
  key_links:
    - from: package.json
      to: bin/minimax-codex.cjs
      via: "the sole npm bin mapping"
      pattern: "minimax-codex.*bin/minimax-codex.cjs"
    - from: crates/cli/tests/state_authority.rs
      to: crates/cli/src/migration.rs
      via: "source-hash checks around inventory/dry-run/apply/rollback"
      pattern: "source_unchanged"
---

# Phase 10 Plan 02: Sole Executable and Writable Authority

## Goal / Objective

<objective>
Remove the supported legacy entry and prove that Rust is the only executable product and `.minimax` the only writable runtime state root.

Purpose: Enforce D-10-01, D-10-02, and D-10-05 before source deletion.
Output: One package bin, corrected launcher diagnostics, Rust-owned package assertions, and state-authority integration tests.
</objective>

<execution_context>
@E:/Agenc/.codex/gsd-core/workflows/execute-plan.md
@E:/Agenc/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/SPEC.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-SPEC.md
@.planning/phases/MMX-10-rust-authority-and-source-boundaries/10-CONTEXT.md
@package.json
@bin/minimax-codex.cjs
@crates/cli/src/app.rs
@crates/cli/src/migration.rs
@crates/cli/tests/migration.rs
@crates/compat-harness/src/baseline.rs
</context>

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cut package and launcher execution authority to one Rust command</name>
  <files>package.json, bin/minimax-codex.cjs, crates/compat-harness/src/baseline.rs, crates/compat-harness/tests/compat_report.rs, crates/cli/tests/headless.rs</files>
  <read_first>
    - package.json — dual-bin and legacy script targets modified by this task
    - bin/minimax-codex.cjs — failure text and fixed binary map modified by this task
    - crates/compat-harness/src/baseline.rs — `validate_product_entry` source of truth modified by this task
    - crates/compat-harness/tests/compat_report.rs — product-entry expectations modified by this task
    - crates/cli/tests/headless.rs — `npm_product_entry_*` assertion modified by this task
    - .planning/SPEC.md — one-executable-authority and EDGE-01 contract
  </read_first>
  <behavior>
    - `package.json.bin` has exactly one key, `minimax-codex`.
    - `start:legacy` is absent and no supported script directly starts the compiled TypeScript CLI.
    - Launcher missing/unsafe/non-executable/start/signal failures return non-zero with reinstall/platform guidance and no alternate runtime guidance.
    - Argument forwarding remains shell-free and preserves the Rust child exit status.
  </behavior>
  <action>
Per D-10-01, remove the legacy bin and supported legacy start script while keeping the normal `minimax-codex` mapping intact. Per D-10-02, do not add ports for dormant TS-only behavior. Update the launcher `fail` contract so it names the supported platform/reinstall remedy only. Rewrite `validate_product_entry` and its Rust tests to require exactly one bin and to exercise missing, unsafe, non-executable, unsupported-platform, argv-forwarding, and child-exit cases with fixture executables; do not rely on the transitional TS launcher test as authority.
  </action>
  <verify>
    <automated>cargo test -p minimax-cli --test headless --locked npm_product_entry_uses_only_rust_launcher -- --exact &amp;&amp; cargo test -p minimax-compat-harness --test compat_report --locked rust_command_permission_provider_and_product_baselines_are_executable -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - Package JSON exposes only `minimax-codex` and the launcher resolves only the two locked native filenames.
    - All launcher error paths are non-zero and never start another interpreter or search `PATH`.
    - Direct `cargo run -p minimax-cli --locked -- --version` still reports the Rust package version.
  </acceptance_criteria>
  <done>No supported npm or script route can start a second product implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove `.minimax` is the only writable runtime authority</name>
  <files>crates/cli/tests/state_authority.rs, crates/cli/tests/headless.rs</files>
  <read_first>
    - crates/cli/tests/state_authority.rs — create/modify target
    - crates/cli/tests/headless.rs — existing command parsing and product-entry integration patterns
    - crates/cli/src/app.rs — migration source defaults and supported command surface
    - crates/cli/src/migration.rs — only permitted reader of `.mini-codex` and `.minimax` target writer
    - crates/cli/tests/migration.rs — source-preserving hash/idempotency patterns
    - fixtures/compat/source-authority.v1.json — stateAuthority source of truth from 10-01
  </read_first>
  <behavior>
    - Representative doctor, index status/search, session, Vault binding, and migration inventory/dry-run commands never create `.mini-codex`.
    - Migration apply writes declared `.minimax` targets only and leaves the complete source tree byte-identical.
    - Rollback removes only receipt-owned unchanged targets and never mutates migration input.
  </behavior>
  <action>
Create a temp-project integration suite that snapshots both state roots, invokes representative read-only and writable Rust command/library paths, and compares recursive file hashes. Treat `.mini-codex` only as the explicit source argument to inventory/dry-run/apply. Assert every created runtime path is a normalized descendant of `.minimax`, all migration source hashes remain stable through apply/verify/rollback, and no legacy process or command is spawned. Extend headless route assertions only where needed to prove migration defaults are source paths rather than writable configuration.
  </action>
  <verify>
    <automated>cargo test -p minimax-cli --test state_authority --locked &amp;&amp; cargo test -p minimax-cli --test migration --locked</automated>
  </verify>
  <acceptance_criteria>
    - The test fails on any write, rename, truncation, or deletion beneath the source root.
    - The test fails on a runtime write outside `.minimax`.
    - Inventory and dry-run remain write-free; apply/rollback retain existing confirmation and receipt gates.
  </acceptance_criteria>
  <done>RUST-03 is proven by deterministic filesystem evidence, not by naming convention.</done>
</task>

</tasks>

## Verification

- `cargo fmt --all -- --check`
- `cargo test -p minimax-cli --test headless --test state_authority --test migration --locked`
- `cargo test -p minimax-compat-harness --test compat_report --locked`
- Build a local Rust binary, copy it beside `bin/minimax-codex.cjs`, and verify both direct and launcher `--version` identities match.

## Success Criteria

- RUST-01 and RUST-03 are enforced before any TS source deletion.
- EDGE-01 and the Phase 10 package/state prohibitions are covered.
- Rust CLI and normal npm-installed launcher use remain functional.

## Artifacts this phase produces

- `crates/cli/tests/state_authority.rs`: new black-box state-root test target.
- `package.json`: modified `bin` and scripts contracts; no new command.
- `bin/minimax-codex.cjs`: modified stable no-fallback failure contract; no business logic.
- `crates/compat-harness/src/baseline.rs`, `tests/compat_report.rs`, and `crates/cli/tests/headless.rs`: modified one-entry assertions; no new public Rust API.
